# PITFALLS.md — the standing ledger of bugs, traps and settled decisions

**Re-read this at the start of every session. Nothing here is a plan; every row is a thing that
already bit, was already measured, or was already settled.**

Status words mean different things and are never upgraded silently:
**PROVEN** = executed test or traced to vendored source · **MEASURED** = a number was produced ·
**REASONED** = argued from proven facts, not executed · **UNVERIFIED** = asserted, no evidence here ·
**OPINION** = judgement call, held by a person.

Sources mined: `AGENTS.md`, `PLAN.md` (§B, §D.1, §E), `BUSINESS.md`, `PROGRESS.md`,
`docs/research/protocol-fee/*`, `docs/research/premise-review/*`, `archive/2026-08-26/CLAUDE.md`
§5–§7. Where two of them disagree, see §7.

**How to reach this file:** it is row 5 of `AGENTS.md` §1's read order, row 4 of `PLAN.md` §I.1, and
is linked from `PROGRESS.md`'s OPEN table, `BUSINESS.md` §14 and `README.md`. If you got here by
knowing the path, that wiring has regressed — fix it.

---

## 1. v4 platform facts that bite

| # | The fact | Consequence if forgotten | Status | Pointer |
|---|---|---|---|---|
| 1.1 | The protocol fee stacks **in front of** the LP fee, it does not carve out of it: `swapFee = pf + lp − pf·lp/1e6`, i.e. `1−swapFee = (1−pf)(1−lp)`. The swapper pays more; LP rate is ~unchanged | "LPs lose a third of their income" — false framing of the bug | PROVEN (source) | `ProtocolFeeLibrary.sol:34-46`; v4-mechanics §1; `test_E5_swapperPaysMore_lpIncomeRoughlyUnchanged` |
| 1.2 | **The swap delta a hook reads is NOT net of the protocol fee; the LP position is.** Delta is computed from `amountIn+feeAmount` first, the skim happens after, fee growth is credited from the reduced amount | Credit from the pre-skim side, backing from the post-skim side = silent insolvency. This is the §E.5 bug | PROVEN (source) | `Pool.sol:369-381`, `:384-396`, `:398-403`; VERDICT §1 |
| 1.3 | **`protocolFeesAccrued` stays inside PoolManager's ERC20 balance until collected** — it is a *global per-currency* counter, and `collectProtocolFees` can move those tokens out at any time | Any invariant of the form "PoolManager balance == what my accounting says" is blind, then breaks | PROVEN (source) + MEASURED | `ProtocolFees.sol:21,:56`; v4-mechanics §8.2; experiment.md F3 |
| 1.4 | `MAX_PROTOCOL_FEE = 1000` pips = **0.1% of swap input, per direction** — an absolute cap, NOT a fraction of the LP fee. On a 5 bp pool that is 200% of LP fee revenue; on 1 bp, 1000% | Sizing the hazard off the LP fee understates it by an order of magnitude on low-fee pools | PROVEN (source) | `ProtocolFeeLibrary.sol:6-8,:15`; v4-mechanics §3 |
| 1.5 | **`lpFee == 0` ⇒ `swapFee == protocolFee` ⇒ v4 takes the *entire* `feeAmount`** via a different formula, including the rounded-up dust | A netted allocator (P2) that assumes the normal formula mis-nets at zero LP fee | PROVEN (source) | `Pool.sol:391-392`; PLAN §E.5; PROGRESS 2026-08-26 |
| 1.6 | A new pool always initializes with protocol fee **exactly 0**; it becomes nonzero **only** via `setProtocolFee`, callable **only** by `protocolFeeController` | Explains why every spike measurement was taken at fee 0 and proves nothing about the fee-on case | PROVEN (source) | `Pool.sol:100-107`; `PoolManager.sol:117-142`; `ProtocolFees.sol:35-41` |
| 1.7 | **A hook cannot refuse, veto, observe or be notified of a protocol-fee change.** None of the 14 hook permission flags relates to protocol fees; `setProtocolFee` calls no hook; there is no per-pool opt-out. A `beforeSwap` `lpFeeOverride` does not escape it either | Any "we control our pool" reasoning is false. The fee can flip to 1000/1000 without notice | PROVEN (source) | `Hooks.sol:27-47`; `Pool.sol:303-307`; v4-mechanics §4 |
| 1.8 | The protocol fee is read **once per swap** from `slot0Start`, before any tick crossing — stable within a swap. But the vendored `ProtocolFees.sol` has **no lock guard** (`_isUnlocked` is declared and never called), so a controller can change it *between two swaps in one tx* | A cached-at-swap-start assumption is fine; a cached-across-the-tx assumption is not | PROVEN (source) for the read; PROVEN (source) + UNVERIFIED upstream for the missing guard | `Pool.sol:283-287`; `ProtocolFees.sol:60`, `PoolManager.sol:392`; v4-mechanics §4, §8.5/8.8 |
| 1.9 | **`swap` is the only protocol-fee accrual site** — `_updateProtocolFees` has exactly one call site, at `PoolManager.sol:238`, which runs **before** `afterSwap` at `:221`. `donate()` and `modifyLiquidity()` are untaxed | This is precisely why P2 can diff `protocolFeesAccrued` inside `_afterSwap` with **no extra hook permission** | PROVEN (source) + MEASURED | `PoolManager.sol:238,:221`; v4-mechanics §8.4; `test_M3` |
| 1.10 | A `beforeSwapReturnDelta` hook that swallows the whole specified amount pays **zero** protocol fee (`Pool.swap` short-circuits at `amountSpecified == 0`) | Custom-accounting hooks are structurally outside the fee — do not generalise our exposure to them, or theirs to us | PROVEN (source) | `Hooks.sol:273-279`; `Pool.sol:320`; v4-mechanics §8.3 |
| 1.11 | **A hook-initiated swap skips the hook's own `afterSwap` entirely** (`if (msg.sender == address(self)) return ...`) | Any guard or allocator living in `afterSwap` silently does not run on hook-internal swaps — a leak invisible to both P1 and P2 | PROVEN (source) | `Hooks.sol:294`; queue-exposure §A2(b) |
| 1.12 | Exact-output differs from exact-input in three ways: LP fee growth is **bit-identical** under exact-out; a composed `swapFee >= 100%` reverts **exact-out only** (`InvalidFeeForExactOut`); exact-out `feeAmount` rounds **up** while the protocol split rounds **down** | Turning a protocol fee on can break exact-output swaps that previously worked, on a high dynamic LP fee | PROVEN (source) | `Pool.sol:310-316`; `SwapMath.sol:63,:104`; v4-mechanics §7 |
| 1.13 | **The redemption residual is NOT fee-growth truncation.** ⚠ *The "~0.26 wei/swap" magnitude this row used to lead with is CORRECTED by 5.80 — it holds at the seeded price and does not generalise. The CAUSE below is unchanged and still right.* A zero-fee pool retains 88% of the drift. Real cause: v4 computes a swap's amounts and a position's redeemable value with two differently-rounded formulas, both in the pool's favour | Someone re-derives "fee rounding", writes it in a comment, and re-runs a settled experiment | MEASURED (drift) + FALSIFIED (hypothesis) | PLAN §B.7, §E.4; **PITFALLS 5.80 for the live magnitude** |
| 1.14 | **`sender` in every callback is the router, `hookData` is caller-supplied, and the hook never sees the recipient** — `take(currency, recipient, amt)` happens after the last callback | Any design ending in "and we refund/gate/pay the swapper" is not hard, it is **impossible**. Uniswap Labs hit this wall and shipped a forked router + token-level restrictions instead | PROVEN (in-repo assertion) | `test_Q3_senderIsTheRouterNotTheTrader`; PLAN §E.8; archive §5.16/§5.18 |
| 1.15 | `PoolManager` keys positions by **the unlock's `msg.sender`** (usually the router), plus tickLower/tickUpper/salt | Modifying liquidity inside someone else's unlock touches a position that is not yours. For QUEUE this is satisfied by construction — the hook is the unlocker | PROVEN | PLAN §E.7; archive §5.12 |
| 1.16 | `poolManager.donate()` pays whoever holds in-range liquidity **at the moment of the call**. There is no primitive paying "liquidity present at block open" and no way to direct a donation | OZ's own `LiquidityPenaltyHook` NatSpec documents the bypass: dust at an empty tick, push price there, collect your own donation | PROVEN (source) + SOURCED | PLAN §E.6; archive §5.11 |
| 1.17 | v4's callback **order** solves the donate-refund problem: donate in `beforeAddLiquidity` ⇒ 100% lands on incumbents; in `afterRemoveLiquidity` ⇒ 0% refunds the exiter. Requires the `*_RETURNS_DELTA` flags | Useful if ever needed — but the archive itself says **verify the flags by execution before relying on it** | REASONED / UNVERIFIED | archive §5.24 |
| 1.18 | Hook permissions live in the **low 14 bits of the address** and PoolManager enforces them (`validateHookPermissions` reverts loudly). QUEUE's required low bits are **`0x18C0`** = `afterInitialize` `1<<12` + `beforeAddLiquidity` `1<<11` + `beforeSwap` `1<<7` + `afterSwap` `1<<6` | **CORRECTED 2026-08-29. This row said `0x0840` — the Phase-1 pair — for four phases, so the LEDGER carried the same stale mask the plan did (5.82).** And the failure is NOT as loud as this row claimed: `BaseHook` validates in ITS constructor, which runs before QUEUE's, so a hook mined for a stale mask reverts at DEPLOY time with nothing attached to say which bit is wrong. **Derive the mask from `getHookPermissions()`; never copy it from a document — including this one.** Adding an unneeded permission still changes the address and widens attack surface | PROVEN (source) + asserted | PLAN §B.2, §H.3; `test_7_1`; PITFALLS 5.82 |
| 1.19 | A NoOp'd swap returning zero output **reverts** through the standard `V4Router` (`V4TooLittleReceived`) and through the hookmate router | Kills batching / delayed / uniform-price-clearing ideas: all of them need a NoOp | PROVEN (execution, in-repo) | PLAN §E.9 |
| 1.20 | Solidity's `(bool ok, bytes memory ret) = target.call(...)` copies **all** returndata; a hostile callee returns a gigabyte and you pay quadratic memory-expansion gas | **This bug was introduced three times in this repo by people who already knew about it.** Use bounded assembly copies | PROVEN (recurrence) | AGENTS.md §6; PLAN §E.10; archive §5.5 |
| 1.21 | Proxy upgrades are **not** detectable on-chain — a contract cannot read another contract's storage. Codehash pinning catches metamorphic redeploy only | Never claim a hook can verify another contract has not been upgraded | PROVEN (structural) | archive §5.8 |
| 1.22 | Transient flash-accounting deltas live at `keccak256(abi.encode(account, currency))` and `exttload` is `external view` — mid-callback anyone can read a hook's true debt from PoolManager's ledger | A v4-only property; the hook cannot misreport that number | PROVEN (in-repo) | archive §5, "two v4-only properties" |
| 1.23 | Hook-callback gas budgets (300k / 500k / 1M) are **economic/UX choices, not protocol constants** — v4 imposes no per-hook gas limit | Do not present the ~44-seat ceiling as a protocol-imposed number | REASONED | `BUSINESS.md` §5.2 |

---

## 2. Testing traps — every way a green result here proved nothing

| # | The trap | The bug it was paid for | Status | Pointer |
|---|---|---|---|---|
| 2.1 | **LAW 1 — never test at 1:1.** Default fixture `SQRT_PRICE_1_4`; conservation shown additionally at 1:1000, 1000:1 and 18/6 decimals; every fixture asserts `s0 != s1` | A prior project here shipped a metric subtracting token1 units from token0 units. On its 1:1/18-dec fixture the error read `0.003`; on 1:4 the same flow read **`0.607`**; on ETH/USDC it would have pinned at max fee forever. **The fixture, not the code, was why the bug lived** | PROVEN | AGENTS.md §3.1; PLAN §D.1 LAW 1, §E.1; archive §5.10 |
| 2.2 | **LAW 2 — every control must go RED with the *exact* asserted reason** (string or selector), never `reason.length > 0` | A test here asserted only that the reason was non-empty and **passed under a mutation that reverted with a completely different error** | PROVEN | AGENTS.md §3.2; PLAN §D.1 LAW 2 |
| 2.3 | **Positive control on the controls**: the *unmutated* harness must PASS through the identical external-call path the mutants fail through | Without it `_expectRed` may be passing because `harness()` always reverts | PROVEN (pattern in use) | PLAN §D.1 LAW 2 |
| 2.4 | **Write down the revert reason you expect before running.** In the original spike the author's first guess at all three reasons was **wrong** | Asserting the reason caught a misunderstanding of the author's own mechanism | PROVEN | PLAN §D.1 LAW 2 |
| 2.5 | **LAW 3 — measure conservation on PoolManager's own ERC-20 balances**, never on the hook's bookkeeping. `hook.internalTotal() == hook.internalSum()` is worth nothing: both sides come from the defendant | A hook that miscounts will happily agree with itself | PROVEN | AGENTS.md §3.3; PLAN §D.1 LAW 3 |
| 2.6 | **LAW 3 AMENDED 2026-08-26 — conservation must be measured on `PoolManager ERC20 balance − protocolFeesAccrued(currency)`, or directly against `redeemAll()`** | **The spike's own LAW 3 conservation test passes at 0 wei error while the position is short 0.354e18 token0**, because the skim is still sitting in PoolManager's balance. Every test written the old way is blind to every protocol-fee-shaped bug | PROVEN (executed, 7/7 with mutations) | experiment.md F3/§6; PLAN §E.5; PROGRESS 2026-08-26 |
| 2.7 | **PLAN's own §E.5 test instruction was BLIND to the bug it was written to catch.** "Set a protocol fee, run the conservation test" executes GREEN over a 0.354e18 shortfall | A LAW-5 trap of the plan's own making. PLAN already contained the right instrument (LAW 3's second corollary) and never wired it to criterion 1.10 | PROVEN (executed) | PROGRESS 2026-08-26; PLAN §E.5, `PLAN.md:991` (criterion 1.10) |
| 2.8 | **Conservation ≠ solvency.** `assertEq(ledger, expected)` and `assertLe(ledger, actuallyRedeemable + K)` are different tests and both are required. The spike proved the first and **disproved** the second | Claiming "face value is redeemable" off a conservation pass | PROVEN | PLAN §D.1 LAW 3, second corollary |
| 2.9 | **LAW 4 — gas only with `vm.cool()`.** Forge keeps storage warm for the whole test body | A real measurement here was **2.5× optimistic** (125k vs a true 309k at 50 seats) | MEASURED | AGENTS.md §3.4; PLAN §D.1 LAW 4 |
| 2.10 | **Every gas claim must name its baseline.** This repo published a "23.5k gas overhead" figure that was an artefact of comparing a hook that transfers tokens against one that does not — **retracted** | A number without a stated baseline is not a measurement | PROVEN (retraction) | PLAN §D.1 LAW 4; archive §6 |
| 2.11 | **LAW 5 — a first-run pass is grounds for suspicion.** Break the code deliberately and watch the suite go red, at three scales: per assertion, per phase gate, per campaign (≥5 mutations caught by the *invariant* campaign) | **Mutation testing or a negative control caught a defective test in 7 of 7 cases where one was run** — several times the control's own author's test | PROVEN | AGENTS.md §2, §3.5; PLAN §D.1 LAW 5 |
| 2.12 | **`targetSelector` without `targetContract`** leaves the fuzz target set as *every* contract deployed in `setUp` — the campaign fuzzes mocks, tokens and the test contract and "passes" without touching the handler | This is how an invariant suite passes on luck, **and it has happened here** | PROVEN | PLAN §E.12; archive §5.7 |
| 2.13 | **A protocol-fee test must assert `protocolFeesAccrued > 0` before any other claim** — `setProtocolFee` silently no-ops for a non-controller caller | Without the guard the test proves nothing at all | PROVEN (source) | PLAN §E.5; VERDICT §6.3 |
| 2.14 | **The remainder line survives swap 1 and only dies at swap 2.** A suite with only single-seat fills does not test it | Multi-seat fill, or the load-bearing line of the whole allocator is untested | PROVEN (negative control) | PLAN §B.5 step 4, §E.3 |
| 2.15 | Mutation pattern to copy: **one contract, one immutable `mode`, an identical test body** — the mutation is a constructor argument, so mutant and real thing run through exactly the same assertions on exactly the same state | Removes the "the control used a different setUp" escape hatch | PROVEN (pattern) | PLAN §D.1 LAW 2 |

---

## 3. Design decisions and WHY — the record of rejected alternatives

| # | Decision | Rejected alternative | Why | Status | Pointer |
|---|---|---|---|---|---|
| 3.1 | **Ship P2 (net out `protocolFeesAccrued` in `_afterSwap`); do NOT ship a refusal** — owner decision 2026-08-26 | P1 (revert while the protocol fee is nonzero), which PLAN originally recommended | P1 hands the PoolManager owner's controller a **permanent off-switch for the product**, and its stated justification ("we control the pool we deploy") was factually wrong — we control deployment, not the fee. **Governance Proposal 100 is reportedly already executed (2026-07-27)**, hook pools excluded *for now*, classification governance-permissioned, no opt-out — so P1 is no longer a hedge against a remote contingency | Decision: taken. Proposal-100 premise: **UNVERIFIED (press only)**. P2 mechanics: MEASURED | PROGRESS 2026-08-26; v4-mechanics §9 |
| 3.2 | **P2 consequence to remember:** with P2 the ledger no longer over-credits, so the "33% of LP income" figure and the "tail eats the shortfall" behaviour **both disappear** | Quoting them as properties of the mechanism | They were artefacts of the bug, not of QUEUE | MEASURED | PROGRESS 2026-08-26 |
| 3.3 | **If P1 is ever built, the check lives in `_afterSwap` ONLY** — never in `unlockCallback`, never in a shared modifier, never on a removal path | Criterion 1.10's own wording, *"the hook refuses to operate"*, steers an implementer to `unlockCallback` — which both deposit and withdraw transit | A guard there converts a governance fee-set into **permanent total loss of principal**. Withdrawal safety is proven only because permissions are `{beforeAddLiquidity, afterSwap}` and removal is unhooked | PROVEN (execution + structure) | queue-exposure §A4; `test_E5_P1_refuseInAfterSwap_doesNotBrickWithdrawal` |
| 3.4 | ⚠ **SUPERSEDED 2026-08-31 by 3.21.** The hook is the sole LP *inside its band*; disjoint wings are ordinary Uniswap. Overlap is still the free lane and is still refused. | Allowing overlapping adds | Same N5 insolvency as before, now named `OverlappingLiquidity` | SUPERSEDED | `test_wing_*`; 3.21 |
| 3.5 | **Permissions: `afterInitialize` + `beforeAddLiquidity` + `beforeSwap` + `afterSwap` (`0x18C0`).** Explicitly NOT `AFTER_SWAP_RETURNS_DELTA`, NOT `beforeRemoveLiquidity` | Enabling returns-delta "for flexibility"; hooking remove | Returns-delta invites settlement bugs. Remove stays unhooked so **wing LPs can exit** (3.21) and so a protocol-fee refusal cannot brick withdrawal. `beforeSwap` exists for the protocol-fee window (P2), not for a fee override. | REASONED (+ PROVEN dependency) | PLAN §B.2; PITFALLS 3.21; 5.82 |
| 3.6 | **The allocation price is the swap's own realised average**, taken from PoolManager's returned `BalanceDelta` | A mark, a mid, a TWAP, an oracle | **There is nothing an attacker can manipulate.** Every mark-to-market design in this repo died to a settlement-instant price push. Any change introducing a price the hook chooses re-opens that surface | PROVEN (structural) | PLAN §E.11 |
| 3.7 | **Harberger's self-price is denominated in ONE token (`currency0`); rent recipients are pro-rata by `currency0` balance only; a seat transfers EMPTY (capital evacuated)** | Valuing a two-token basket | Pricing a basket requires a price, which §E.11 forbids. Evacuation means a buyout covers **rank only** — and it removes an entire class of theft | REASONED | PLAN §B.8, §B.10 |
| 3.8 | **Rank must be BOUGHT or Harberger-held, never granted by deposit order or first-come** | Model (D), permissionless rank-by-deposit-order | A seat that is free to occupy has no price, and the head is dust-griefable. **QUEUE without a price for the seat is not QUEUE.** Phase 2's append-at-tail is PROVISIONAL and must be deleted in Phase 3 (with `test_knownHole_phase2SeatsAreGrantedByArrivalOrder` documenting it meanwhile) | REASONED (hard rule) | PLAN §B.8, §E.16; `BUSINESS.md` §3 |
| 3.9 | **Bounded roster, `MAX_SEATS = 32` recommended** | An unbounded / open retail LP book | ⚠ **THE NUMBERS THIS ROW ORIGINALLY CARRIED WERE MEASURED ON THE SPIKE AND WERE WRONG — see 5.68.** "~6,753 gas each ⇒ ~44 seats" came from a contract with no cursors, no owners, no seat tokens and no lease. **Live figures: 8,070 gas per seat swept** (after Phase 5b's packing; 12,254 before it), a 32-seat sweep is 278,110 gas of queue work, and **the real bound is `addToSeat` at 2,610,805 gas — quadratic in the roster, not the sweep (5.72).** Scarcity is also what makes a seat *priced*, and Phase 6's reframe makes that the LEAD argument rather than a consolation: free rank is griefable rank, unbounded rank is worthless rank. **Concede the departure from permissionless access in the first sentence of the pitch, and then explain why it is the mechanism** | MEASURED, re-measured on the shipping hook | PLAN §B.9 CORRECTED; `README.md`; PITFALLS 5.68, 5.72 |
| 3.10 | **Redemption dust fix F1 — settle against actual holdings**, face value becomes an upper bound | F2, a dust buffer topped up from a fee cut | QUEUE takes no fee, so F2 has no funding source. F1 needs no new state and is honest about where the number comes from | REASONED (fix UNBUILT) | PLAN §B.7 |
| 3.11 | **Keep hook mixin inheritance a CHAIN, never a diamond** | Sibling mixins both overriding `_afterSwap` | A diamond forces every integrator to disambiguate by hand — which invites them to resolve it by disabling the safety feature | PROVEN (recurrence in prior repo) | PLAN §E.17; archive §5.6 |
| 3.12 | **Rent uses `block.timestamp`, not block numbers**; τ and `RENT_PERIOD` are immutables | Block-denominated rate | Block times differ by chain — a block-denominated rate silently changes meaning on deploy | REASONED | PLAN §B.10 |
| 3.13 | **Do not defend a specific τ.** Show the mechanism at several values; the *sign* of the seat price is the finding | Presenting τ as a discovered constant | "A load-bearing parameter is where a pitch dies" — this project's own history | OPINION (informed) | PLAN §B.10 |
| 3.14 | **Harberger foreclosure is a demotion, not a seizure** — no collateral, no oracle, no mark, no health factor | A margin/liquidation design | A prior candidate (`TENANT`) died for exactly this: *"the kind of build that produces a green suite and a drained contract"* | REASONED | PLAN §B.10, §E.19 |
| 3.15 | **Override BOTH `ERC6909.transfer` and `transferFrom`** to `_evacuate(id, from)` first | Overriding only `transfer` | The inherited functions are `virtual` and only move balances. **`transfer` guarded / `transferFrom` unguarded is the classic form of this bug** — mandatory negative control in §D.6 | REASONED (control mandated) | PLAN §B.8 ("FOOTGUN") |
| 3.16 | **Ship the O(N) allocator + bounded roster; the O(1) prefix-sum redesign only ships against a wei-exact differential test** | Shipping the O(1) redesign for gas | The exactness comes from the remainder line applied to *the last seat filled*; a lazy accumulator has no such notion, so it **may not be exactly conservative**. Trading the only thing QUEUE has for gas on a 32-seat book is a bad trade | UNPROVEN, and explicitly doubted | PLAN §B.11 |
| 3.17 | **Price the seat rather than classify the flow** | Building a toxicity classifier, or a better curve | No forgeable-proof toxicity signal exists for a v4 hook, and `ℓ = ½σ²·d` closes the curve route. QUEUE **reallocates** adverse selection; it does not reduce it | PROVEN (both impossibility results) | PLAN §E.19; `BUSINESS.md` §15 |
| 3.18 | **Lead the pitch with pro-rata-vs-queue and never use the word "auction."** Harberger is a self-assessed always-for-sale lease with continuous rent, not an auction | Framing as an auction | The nearest neighbours (am-AMM family: UHI1/2/6/7) auction pool-management rights to **one winner per block**. The distinction is one sentence deep and a judge will probe it. Atrium's own deck puts LVR auctions on the AVOID list | REASONED + SOURCED | PLAN §E.14; `BUSINESS.md` §15 |
| 3.19 | **Fairness: build NO new mechanism; ship the Fairness Theorem as an argument** | B-1 Harberger, B-2 two-sided rank, and 6 killed variants | QUEUE is **exactly zero-sum vs pro-rata**, so the tail's excess return has the OPPOSITE SIGN to the pool's net LP P&L — profitable pool ⇒ tail worse off; loss-making pool ⇒ tail better off. **Both seats cannot beat pro-rata; no ordering/banding/rotation/blend changes that.** Fairness is a TRANSFER problem, not an allocation problem, and the design already names the transfer (sell/rent rank). The argument costs zero engineering days and beats any mechanism. GATE: build B-1 or B-2 ONLY if Phase 3 is green with real time to spare — a half-built Harberger on an unfinished allocator scores WORSE than zero | PROVEN (zero-sum identity) / DECISION | premise-review/fairness.md §A.1, "The gate" |
| 3.20 | **Front-of-queue is an ASSET, not a liability — the cancel objection does not transfer** | "no cancel ⇒ front concentrates adverse selection ⇒ rank worth negative" | A CLOB order rests at a **fixed price** — cancel manages a STALE QUOTE. A QUEUE seat has **no price of its own**: every seat is filled at the swap's own realised average price (`PLAN.md:503`), so front and tail get **identical execution** and only quantities differ. Front buys **priority over QUANTITY, not priority at a stale PRICE.** Leverage identity: `L(s) = (C/c₁)·min(1, c₁/s)` — levered on everything, MOST on small swaps. QUEUE is a **leverage instrument on the pool's LP return** | REASONED, confidence HIGH (~85% on the sign) | premise-review/economics.md §2, §3 |
| 3.21 | **Refuse overlapping LP; allow disjoint wings; clip `afterSwap` to the in-band fill.** | Keep the hook as sole LP of the whole curve (3.4); a deposit-tail vault; 32 NFT ranges | Overlap is N5 and is refused (M71). In-band wing theft is ticks, not the clip. Crossing attribution is one `computeSwapStep` over the band; leftover is Uniswap. **MEASURED:** exact-in both directions, re-entry, full traverse, exact-out, protocol-fee crossing, salt, overlap-from-above; wei-equal to an independent SwapMath replay on the two traverse tests. **Not claimed:** invariant campaign with wings (ghost is whole-pool delta); PositionManager mint path (same callback, untested glue); band recenter when spot leaves ±10% (product, not a tick bug). 1-wei slack is I1 rounding, not a material free lane. | MEASURED (`test/queue/Wings.t.sol`, M70/M71) | `QueueHook::_beforeAddLiquidity`, `_queueShare` |

---


## 4. Proven-impossible — settled dead, do not re-litigate

| # | The idea | Why it is impossible | Status | Pointer |
|---|---|---|---|---|
| 4.1 | **Detect toxic flow in a v4 hook** | Every readable signal is forgeable or invisible: `sqrtPriceLimitX96` (sentinel, free to forge); `amountOutMinimum` (**the hook cannot see it** — enforced in the router after the last callback); exact-in/out sign (free); priority fee (zero under private orderflow); `tx.origin`/EOA (sender is the router); trade size (splitting lemma); address/history (value moves between addresses inside one unlock for **52,700 gas**) | PROVEN | PLAN §E.19; archive §5.19; `archive/2026-08-26/docs/research/IDEAS_CONSTRAINED.md` §1 |
| 4.2 | **A curve that reduces LVR** | `ℓ(σ,p) = ½σ²p²·\|x'(p)\|` and marginal depth `d(p) = p²·\|x'(p)\|` are **the same quantity**: `ℓ = ½σ²·d`, for every invariant at every price (verified by recovering `σ²V/8` for constant product from the depth side). Halving LVR halves depth by the same factor | PROVEN (theorem) | PLAN §E.19; archive §5.22; `IDEAS_CURVES.md` §0 |
| 4.3 | **A hook that sees, gates, refunds or pays the trader** | `sender` is the router, `hookData` is caller-supplied, the recipient is taken after the last callback. Only three escapes exist — be the periphery (kills aggregator routing), a per-trade issuer signature (an off-chain censor on the swap path), or the token enforces it (ERC-3643/1400, which makes the hook redundant). **Uniswap Labs shipped escapes 1 and 3 with a forked router + forked posm; their hook is the redundant half** | PROVEN | PLAN §E.8, §E.19; archive §5.16/§5.18 |
| 4.4 | **Any mechanism keyed on a per-swap / per-address magnitude** (the splitting lemma) | Split the quantity into k parts at a cost of (k−1)×marginal gas — fractions of a cent. Kills max-price-impact, per-address JIT caps, concave fee splits, min time between trades, per-block move bounds. Directory: `circuit break\|price band\|rate.?limit\|max.{0,6}impact` → **6 built, 0 prized** | PROVEN | PLAN §E.15; archive §5.17 |
| 4.5 | **Any mechanism anchored on a per-block reference** | Waiting one block boundary costs **200 ms** on Unichain. Also: reflexivity-immunity and the cross-block exemption are the **same knob** — a reference an attacker cannot reset also cannot exempt the honest arb (the EMA variant closed the cross-block route at 7.2% of LP P&L) | PROVEN / MEASURED | PLAN §B.12; archive §5.19-20 |
| 4.6 | **Uniform-price batching / delayed or cleared swaps** | Every version needs a NoOp, and a NoOp'd swap returning zero output reverts through the standard router. One instance in a 662-row directory, and it needed FHE *and* custom periphery | PROVEN (execution) | PLAN §E.9 |
| 4.7 | **Fairness as an on-chain property** ("this swap was unfairly priced", "the hook stole in block N") | Not a present-state property, therefore never expressible as a predicate. QUEUE makes no fairness claim — rank is present state with no history and no markout, which is exactly why it *is* expressible | PROVEN (structural) | PLAN §E.18; archive §5.25 |
| 4.8 | **On-chain randomness for sandwich resistance** | Every input to a same-transaction draw is known to the searcher who chooses when to submit | REASONED | PLAN §E.19 |
| 4.9 | **A variance / LVR-index / LP-loss perpetual** | Creating a tick move δ costs fees ~linear in δ while a variance payout is quadratic in δ — for large enough δ the manipulator always wins, on the very venue that publishes the index | REASONED (+ occupied lane) | PLAN §E.19 |
| 4.10 | **A compliance gate / permissioned pool / KYC hook** | Fails the delete test for the same reason as 4.3; true of all 78 compliance-tagged submissions in the directory | PROVEN | PLAN §E.19; archive §5.16 |

---

## 5. Open hazards not yet closed

| # | Hazard | Why it matters | Owner | Status | Pointer |
|---|---|---|---|---|---|
| 5.1 | ✅ **CLOSED 2026-08-27 — P2 is BUILT. `QueueHook._beforeSwap`/`_afterSwap` net out the fee; 6 multi-pool tests, all RED against the superseded mechanism** *(historical)* **Protocol-fee remedy P2 is decided but UNBUILT.** No mechanism code exists — Phase 0 has not started | Until built, the ledger over-credits by exactly `protocolFeesAccrued` whenever a fee is on | Phase 1 (acceptance 1.10) | Decided; MEASURED prototype only (`test_M3`) | PROGRESS 2026-08-26; PLAN §E.5 |
| 5.2 | ✅ **CLOSED 2026-08-27 — criterion 1.10 now has a test that CAN fail: it asserts `protocolFeesAccrued > 0` first, checks the ledger, and separately asserts solvency via `redeemAll()`** *(historical)* **Criterion 1.10's assertion, as written, cannot fail** | It is the §E.5 blind test (2.7). Must assert against `PoolManager balance − protocolFeesAccrued` or `redeemAll()`, **and** assert `protocolFeesAccrued > 0` | Phase 1 | **PLAN CORRECTED 2026-08-26** — criterion 1.10 now names P2, the `balance − protocolFeesAccrued` / `redeemAll()` assertion and the `protocolFeesAccrued > 0` guard. The *test* is still unwritten (Phase 1) | PLAN §C.1 criterion 1.10; PROGRESS 2026-08-26 |
| 5.3 | ✅ **CLOSED 2026-08-27 — `lpFee == 0` needs NO special case, because the fee is READ not derived. `test_5_3_lpFeeZeroUnderProtocolFee`** *(historical)* **`lpFee == 0` handling under P2** | The whole-`feeAmount` formula path (1.5) must be handled or explicitly refused | Phase 1 | Open | PLAN §E.5 |
| 5.4 | ✅ **CLOSED 2026-08-27 — `test_1_10_positionStaysRedeemableWithProtocolFeeOn` redeems the whole position with the fee ON and asserts withdrawal accrues no protocol fee** *(historical)* **A negative control proving withdrawal still works with the fee on is mandatory** | Placement of any fee check is load-bearing; the wrong place is total permanent loss of funds | Phase 1 | Mandated, not written | PROGRESS 2026-08-26; queue-exposure §A4 |
| 5.5 | ✅ **CLOSED 2026-08-27 — Phase 2 SHIPS the two-slot float. `withdraw` pays any seat its exact ledger composition with no `poolManager.swap`, in all six orderings** *(historical)* ✅ **RESOLVED 2026-08-26 — FEASIBLE via a two-slot shared float** (22/22 tests, `docs/research/withdrawal/`). INVARIANT F proven: `Σ q[i].aX == redeemable X + floatX`. The original probe's apparent 1.062e18 shortfall was **principal-only measurement**, not a real gap. *(historical)* §B.7's withdraw spec is impossible as literally specified. Measured: after the spike's own 4-swap scenario, seat 1 holds 258.877 token0 / 0 token1 and can withdraw **nothing** via `modifyLiquidity(−Δ)`; seats 0 and 2 leave their token1 legs unreachable | A position releases tokens in a ratio fixed by price and range; front-first allocation deliberately drives seats to single-token composition. **This is the steady state, not an edge case.** The swap-to-rebalance "fix" is a trap (1.11) | Phase 2 | MEASURED (`WithdrawalPathProbe.t.sol`) — PLAN §B.7 not yet corrected | queue-exposure §A2 |
| 5.6 | ✅ **CLOSED 2026-08-27 — `withdraw` reaches `modifyLiquidity` only through `_burnPosition`; there is no `poolManager.swap` anywhere in the contract** *(historical)* ✅ **CONSTRAINT HELD** — the float design never calls `poolManager.swap` (asserted: `test_NC_H_withdrawEmitsNoSwapEvent`). The float state it needs is now specified (PROGRESS 2026-08-26 close). *(standing rule)* **`withdraw` must never call `poolManager.swap`** — and the float state that the swap-free resolution needs (§A2 resolution (a)) **does not exist in PLAN's state layout** | Otherwise the allocator does not run and the protocol fee is skimmed off the rebalance swap, unguarded and unaccounted | Phase 2 | Constraint identified, not written into PLAN | queue-exposure §A2, §A4.4 |
| 5.7 | ✅ **F1 IS BUILT (Phase 2) — but the claim is STILL BARRED.** `_applyDustPolicy` pays `min(face, available)`, so the residual is spread over withdrawers instead of dumped on the last one to call. *"The queue's face value is redeemable"* must still not be claimed — not in README, not on video, not in a comment | Face value is an **upper bound**. Phase 6 measured the bound honestly and found the per-swap figure does not generalise: see **5.80**. Worst surplus **exactly 0 wei**, worst shortfall **under 1 ppb of lifetime inflow**, borne by the LAST holders to exit | Standing | MEASURED; unfarmable (cost-to-damage off by ~20 orders of magnitude) | PLAN §B.7, §A.4; PITFALLS 5.80 |
| 5.8 | ✅ **CLOSED 2026-08-27 — but read the caveat, do not quote the headline.** The arrival-order path is DELETED: there is no runtime path that creates a seat, `deposit()` is gone, and the roster is minted once in the constructor. Dusting buys nothing at any price. **What is NOT true is "rank is bought":** the FOUNDING roster is an endowment chosen by whoever deploys, exactly as an exchange's founding memberships were granted and then traded. Rank is only *bought* on the secondary market, which 5.11 says does not exist. **Phase 4's Harberger lease is what makes rank continuously priced, and until it lands, say the narrow thing on camera: rank cannot be obtained by dusting, by being early, or at any price the incumbent has not accepted.** *(historical)* **Phase 2 seats are granted by arrival order** — provisional and not shippable | Violates the hard rule 3.8; the head is dust-griefable while it stands | Phase 3 ✅ / Phase 4 for the rest | **FIXED**, with 8 mutations red and the named hole test deleted | `src/queue/QueueHook.sol` constructor; `test/queue/Rank.t.sol` |
| 5.9 | ⚠️ **RE-SCOPED 2026-09-02 — SEE 5.122. WHAT PHASE 4 CLOSED WAS THE MULTI-DAY ABANDONMENT ONLY; THE ATOMIC ONE WAS NEVER PRICED AND WAS WIDE OPEN UNTIL `withdraw` BEGAN TO DEMOTE.** Every arm of `test_4_7`, the test cited below as the closure, carries `vm.warp(block.timestamp + 30 days)`. Rent is a time integral, so the bill it measures is proportional to a duration the attack does not have. Read the row below as true of a holder who leaves for a month and silent about one who leaves for zero seconds. ✅ **CLOSED 2026-08-27 by Phase 4 — read the precise claim, not the headline.** Harberger closes it in the only sense it can be closed: **you can leave whenever somebody will take the seat at your own number, and you cannot leave unilaterally.** Holding the front through an event now costs rent, or costs the seat. Executed as three arms on one fixture in `test_4_7`: unpriced rank (= the Phase 3 state) lets the holder withdraw before the event, wait, and refund after, at a cost of ZERO and with the rank kept throughout; priced, the same window costs exactly thirty days of rent; unpriced *in order to dodge that bill*, the seat is taken for free. *(historical)* **Rank-then-run**: under plain transferable rank, buy the front cheaply, dump the token before a scheduled event. You eat the gap **only if there is a bid** | In a thin secondary market the front seat is abandonable at exactly the moment it matters. Two obligations: disclose on camera if shipping plain rank, and build Harberger if there is time | Phase 4 | REASONED, unclosed | PLAN §E.13 |
| 5.10 | **Harberger cannot express a negative seat value.** Under toxic flow everyone declares near-zero and no rent flows | The variant that closes 5.9 has its own unsolved gap — disclose it too | Phase 4 | ANALYSIS / UNVERIFIED, "not solved today" | `BUSINESS.md` §14.6 |
| 5.11 | **The liquid rank secondary market does not exist and there is zero measurement that it would form** | It is the load-bearing assumption of the toxicity-price claim **and** the compounding factor on 5.9. `BUSINESS.md` §14.7 still calls it the weakest point | Pitch / Phase 3+ | UNVERIFIED | `BUSINESS.md` §14.7 |
| 5.12 | **O(1) prefix-sum redesign** — named, unbuilt, unverified, and doubted for a specific reason (3.16). Do not quote "sweeps are O(1)" | Shipping it without the differential test trades exactness for gas | Phase 5 | UNPROVEN | PLAN §B.11, §A.4 |
| 5.13 | **Cursors maintained O(1) under bidirectional flow without leading** | Riskiest-assumption rank 4 | Phase 1–2 | UNPROVEN (N4 control, §D.3) | PLAN §G.2 |
| 5.14 | ✅ **CLOSED 2026-08-27 — and the price-free property survived, but only after the specified design was replaced (5.51).** A transfer reads no price: it evacuates the seat by paying the departing holder its own ledger amounts, in the pool's own two currencies, with no valuation of anything. Capital-evacuation controls red through `transfer`, `transferFrom` and an operator. *(historical)* **Rank transfers safe without any price** | Riskiest-assumption rank 5 | Phase 3 | **PROVEN** (`test_3_2`, `test_3_3`, control `test_3_8`) | `test/queue/Rank.t.sol` |
| 5.15 | **Proposal 100 / live protocol fees are press-sourced only.** Verify on-chain with `poolManager.protocolFeeController()` and `StateLibrary.getSlot0(poolId)` before relying on it | The P1-vs-P2 rationale leans on it. Whether a hook like ours would today be classified into a nonzero family is UNVERIFIED | Phase 1 / Phase 7 | UNVERIFIED (one independent consistency check: `500/(3000×0.9995) = 16.7%` matches the reported "1/6") | v4-mechanics §9; PROGRESS 2026-08-26 |
| 5.16 | **Theme framing** — the bridge from QUEUE to "Sustainable Liquidity and MEV Protection" is the weakest part of the pitch. QUEUE addresses open problem #3 and only #3 | Overstating it is the fastest way to lose a technical judge | Phase 7 | OPINION / SOURCED | `BUSINESS.md` §15; PROGRESS OPEN table |
| 5.17 | **Full-range capital efficiency — the sharpest attack, and as of Phase 7 it has HALF an answer.** One full-range position offers ~1/200th the depth per dollar of a ±1% concentrated position. A $1M pool quotes ~$5–10k of depth; a $10k swap costs ~4% slippage. The economic consequence stands: no aggregator routes retail to it, retail is the ENTIRE benign side of the P&L, and without it the pool is arb-only | **UPGRADED 2026-08-29 from ANALYSIS to PROVEN on the narrow half.** This row always said the range was "a reversible design choice, not a v4 constraint", and that was an assertion. It is now executed: `tickLower`/`tickUpper` appear in exactly TWO places — the liquidity-sizing helpers and the `modifyLiquidity` call — and nowhere in `_allocate` or `Allocation.sol`, which see only the realised deltas of a swap that already happened. `test_1_11` seeds the identical roster over a **±10% band** and runs both directions against the INDEPENDENT witness: conservation, per-seat composition and INVARIANT C all hold to the wei, and the sweep exhausts the head exactly as at full range. **So concentrating the custodied position is a v2 PARAMETER, not a redesign.** What is still ANALYSIS: out-of-range behaviour, rebalancing, and the depth-versus-coverage trade-off. Do not claim a narrow-range QUEUE is a finished product — claim only that the queue is orthogonal to the range | Pitch / design | **PROVEN** (orthogonality) + ANALYSIS (the economics) | `test_1_11`; `QueueFixture._openRange`; premise-review/economics.md §6 |
| 5.18 | **The Ratchet.** Front-first applies both directions, so the head unwinds first: head = market maker with recycling inventory, **tail = one-way accumulator filled at extremes with no priority to exit** | Moderate, not fatal (tail can withdraw + re-deposit, rank survives §B.7) — but it is a cost of ACTIVE management imposed on the party the pitch calls PASSIVE ("treasury, LST issuer, yield vault"). A real contradiction in the sales story | Pitch / Phase 4 | ANALYSIS — now named in `BUSINESS.md` §8 (back-seat cons). Only B-2 (two-sided rank) addresses it | premise-review/fairness.md §A.2c; `BUSINESS.md` §8 |
| 5.19 | ✅ **CLOSED 2026-08-27 by Phase 4.** Rent flows from the front to the seats behind, pro-rata by their `currency0` balance, summing exactly to what was charged. **The residual honesty: only a tail holding `currency0` is an eligible recipient** — weighting a two-token basket needs a price and §E.11 forbids one — so a `currency1`-only tail is paid nothing and the rent waits in `unallocatedRent0`. Asserted rather than hidden: `test_4_43`. *(historical)* **Phase 3 — the SUBMITTABLE state — is a ONE-SIDED MARKET.** The tail's compensation channel is Harberger rent, which is Phase 4 | The honest Phase 3 answer to *"why would anyone hold seat 5?"* is **"they wouldn't."** Phase 4 is not optional for rank-then-run alone — it is what makes the tail's participation rational at all. The Phase 3 transfer channel is a secondary market `BUSINESS.md` §14.7 concedes does not exist | Phase 4 / pitch | ANALYSIS — disclose out loud if shipping Phase 3 | premise-review/economics.md §7, fairness.md |
| 5.20 | **"The seat price IS the toxicity" is OVERCLAIMED** | It measures `C/c₁ × expected LP return` — already computable from public swap data. Novel part is that it is forward-looking and on-chain, not that it reveals the unobservable. Under Harberger the signal is **censored at zero on exactly the toxic side** | Pitch | ANALYSIS — quotable against you as written | premise-review/economics.md §5 |
| 5.21 | ✅ **CLOSED 2026-08-27 — `sweepFloatIntoPosition()` is BUILT, permissionless, credits nobody. But see 5.43 — it is BOUNDED BY THE SMALLER LEG and cannot reclaim a stranded majority leg** *(historical)* **`sweepFloatIntoPosition()` is REQUIRED and UNBUILT — the biggest gap in the withdrawal result** | Without it **pool depth degrades monotonically** as seats withdraw imbalanced legs. Permissionless, no privileged role: re-add `Δ' = min(float0/f0, float1/f1)`, credit nobody, decrement floats by actual consumed | Phase 2 | UNPROVEN — not written, not tested | withdrawal/feasibility.md §9.3 |
| 5.22 | ✅ **CLOSED / MEASURED 2026-08-26 — they COMPOSE.** INVARIANT F survives a MAX protocol fee: residual **0 wei** (balance form), 3–8 wei vs raw ledger — the same bounds as fee-off, and it does **not** scale with the fee (19/19 tests, `withdrawal/fee-float-composition.md`). *(historical)* Protocol fee ≠ 0 interacting with the FLOAT was untested | The whole withdrawal result ran at `protocolFeesAccrued == 0` (asserted). The amended LAW 3 form is used but **inert**. P2 and the float have never been exercised together | Phase 1–2 | UNTESTED | withdrawal/feasibility.md §9.5 |
| 5.23 | ✅ **CLOSED 2026-08-27 — `_burnPosition` decrements `liquidity` before every removal. Mutation P1 confirmed red across 4 suites** *(historical)* **`withdraw` MUST decrement `liquidity` by Δ — PLAN §B.7 never does** | Every later sizing computation is wrong without it. Silent, compounding | Phase 2 | MEASURED requirement; PLAN not yet corrected | withdrawal/feasibility.md §9.1 |
| 5.24 | ✅ **CLOSED 2026-08-27 — OWNER DECISION 2026-08-27 — **ABSORB into float and credit the seat**, not refund. Cheaper, shrinks the float, depositor keeps full value as ledger credit** *(historical)* **OWNER DECISION PENDING: deposit remainder — refund vs absorb into float** | §B.7 says refund to `msg.sender`; under a float, absorbing into `floatX` and crediting the seat is cheaper and reduces the float. **Changes who gets what ⇒ AGENTS.md §4 escalation** | Phase 2 | Not built, not tested — ASK FIRST | withdrawal/feasibility.md §9.2 |
| 5.25 | ✅ **CLOSED 2026-08-27 — the global counter is no longer diffed across transactions. X1 and X2 are both dead and both have executed regression tests that go RED against the old mechanism** *(historical)* 🚨 **PHASE 1 BLOCKER — P2's IMPLEMENTATION IS DEFECTIVE. `protocolFeesAccrued` is GLOBAL PER CURRENCY, not per pool** (`ProtocolFees.sol:21` — `mapping(Currency => uint256)`, no `PoolId`). Diffing it absorbs **every other v4 pool's** protocol fees on either currency | **X1 silent under-credit** — ordinary volume on any other pool of the same token strands value owed to nobody, no attacker needed. **X2 PERMANENT BRICK** — `amtIn -= pfDelta` underflows in `afterSwap`, the swap reverts, `pfSeen` never advances, **the pool is dead forever**. `pfSeen` starting at 0 also bricks/mis-credits the FIRST EVER swap | Phase 1 — **before any build** | **PROVEN** (`test_X1_foreignPoolSharingACurrencyCorruptsTheP2Diff`, `test_X2_foreignPoolCanBrickTheHookEntirely`) | withdrawal/fee-float-composition.md §6 |
| 5.26 | ✅ **CLOSED 2026-08-27 — the replacement needed NO arithmetic derivation. Snapshotting the fee across the `beforeSwap`->`afterSwap` window reads v4's own per-step sum, so every listed difficulty is moot** *(historical)* **The P2 replacement is NOT easy — it needs its own experiment + negative control** | Deriving the fee arithmetically must handle: `lpFee == 0` takes the ENTIRE `feeAmount` (§1.5); exact-out rounds the other way (§1.12); `amountToProtocol` is summed **per swap step** with per-step rounding across tick crossings — so `amtIn * pf / 1e6` will NOT be wei-exact. **Clamping removes the BRICK but not the UNDER-CREDIT** | Phase 1 | OPEN — mechanism undecided | withdrawal/fee-float-composition.md §6 |
| 5.27 | **DOCTRINE: `test_M3` "measured" P2 working in a SINGLE-POOL fixture** | The mechanism was right about *what* to subtract; the **instrument** was wrong and the fixture could not see it. **A green test in a one-pool fixture proved nothing about a global counter.** Before trusting any measurement, ask what the fixture cannot represent | Standing | PROVEN by counter-example | PROGRESS 2026-08-26 final |

| 5.28 | ✅ **CLOSED 2026-08-27 — the Phase 1 fixture measures `PoolManager balance - protocolFeesAccrued` and asserts solvency separately via `redeemAll()`** *(historical)* **The spike's conservation harness is the BLIND raw-balance form.** `expT0/expT1` track `MockERC20.balanceOf(address(poolManager))` deltas only (`test/spike/QueueAllocator.t.sol:293-317`). Correct *there* solely because `protocolFeesAccrued == 0` — **which the spike never asserts** | **Phase 1 inherits the blindness if it copies this `setUp`/`_swap` pair**, and that is exactly the §E.5 trap (2.7 / 5.2). Must become `balance − protocolFeesAccrued(currency)` or `redeemAll()`, **plus** an assertion that `protocolFeesAccrued > 0` | Phase 1 | **PROVEN** (read at source 2026-08-27) | PROGRESS 2026-08-27; LAW 3 as amended |
| 5.29 | ✅ **CLOSED 2026-08-27 — Phase 1 carries an independently written `_refAllocate` in `QueueFixture`, asserted seat by seat** *(historical)* **Conservation does NOT catch a misallocated wei — only an independent reference allocator does.** MUT-B (head under-fills by 1 wei) left aggregate totals tying out perfectly and died on `entry a0`, never on conservation | LAW 3's second corollary, landing on the allocator: **conservation and composition are two different claims needing two different assertions.** If Phase 1 ships without its own independently-written `_refAllocate`, a whole bug class is invisible | Phase 1 | **PROVEN** by executed mutation 2026-08-27 | PROGRESS 2026-08-27 |
| 5.30 | **"9 tests passed" overstates the assurance.** `test_Q2_gasProfileVersusQueueDepth` passed under **both** fresh mutations — a gas test is entirely correctness-blind — and `test_Q1b_residualWithZeroSwaps` never invokes `_allocate` at all | Only **6** of the 9 carry arithmetic signal and only **4** exercise a multi-seat sweep. Do not quote the test count as units of assurance, on camera or in a commit message | Standing | **PROVEN** by executed mutation 2026-08-27 | PROGRESS 2026-08-27 |
| 5.31 | ✅ **CLOSED 2026-08-27 — Phase 1 runs 18/6 AND 6/18 decimals (`test_1_3_*`) as well as 1:4, 1:1000 and 1000:1** *(historical)* **LAW 1 is only HALF-satisfied by the spike fixture.** Price is 1:4 and guarded (`require(s0 != s1, "fixture is unit-priced")`), but `Deployers.deployToken()` hardcodes **18 decimals for both** tokens | The unequal-decimals half of LAW 1 is untested in the spike. Copying the spike's `setUp` into Phase 1 silently inherits 18/18. `docs/research/withdrawal/FloatWithdraw.t.sol` already runs 18/6 — **Phase 1 must too** | Phase 1 | **PROVEN** (source, `test/utils/Deployers.sol:38`) | PROGRESS 2026-08-27 |
| 5.32 | ✅ **CLOSED 2026-08-27 — PHASE 0 GREEN.** Every §D.2 number reproduced **exactly** (not within tolerance) on `forge 1.5.0-stable` @ `1c578544`, and the suite went red under two mutations its own author did not anticipate | The ground is solid; §C.0's divergence-escalation rule was not triggered. The reference allocator is trustworthy as the Phase 1 oracle | — | **PROVEN** | PROGRESS 2026-08-27 |
| 5.33 | ✅ **SUPERSEDED BY 5.40 (2026-08-27) — both functions were REMOVED from production entirely, not merely flagged.** *(historical)* **`QueueHook.seed()` is PHASE 1 SCAFFOLDING and MUST BE DELETED IN PHASE 2.** It is permissionless and callable once per deployment (`liquidity == 0`); whoever calls it first becomes the entire queue | It is not a privileged role and not an admin function, so it breaks no hard rule — but it is **not the shipping deposit path** and must never be demoed or described as one. Phase 2's `deposit()` replaces it outright | Phase 2 | **KNOWN, by construction** | `src/queue/QueueHook.sol::seed` |
| 5.34 | **A test that measures the protocol fee with the FIXTURE's own instruments is tautological.** An early draft of `test_measurementWindowSeesExactlyOneSwapsFee` compared two fixture-side numbers and **passed against the broken mechanism** | Assertions about the hook's arithmetic must read the HOOK's ledger movement (`lastHookCreditedIn`), never the fixture's own `lastPfDelta`. This is PITFALLS 5.27 one layer down, and it was caught only by executing the superseded mechanism against the new suite | Standing | **PROVEN** by executed mutation 2026-08-27 | `test/queue/QueueFixture.sol`; PROGRESS 2026-08-27 |
| 5.35 | **Foreign volume must land BEFORE the measurement, not after.** The same test also passed against the broken mechanism purely because its foreign swap came after the window it was checking | A multi-pool fixture is necessary but NOT sufficient — the contamination has to exist at the moment the instrument reads | Standing | **PROVEN** by executed mutation 2026-08-27 | `test/queue/ProtocolFee.t.sol` |
| 5.36 | **`redeemAll()` is the only assertion that catches an outside LP.** N5 (sole-LP guard removed) does NOT break ledger conservation — the fixture's expected totals and the hook's credit both move by the whole swap | PLAN §D.3 predicted "conservation, immediately" and was wrong; corrected 2026-08-27. Ledger conservation and position redeemability are different claims | Standing | **PROVEN** (`test_N5_externalLpBreaksSolvency`) | PLAN §D.3 |
| 5.37 | ✅ **FIXED 2026-08-27 — but the class is standing. PAIRED-BRANCH ASYMMETRY IN THE CURSOR PULL-BACK.** The pull-back exists TWICE, once per direction. Deleting `if (start < cursor1) cursor1 = start;` was caught by **7 tests**; deleting its mirror `if (start < cursor0) cursor0 = start;` was caught by **ZERO** | Every scenario happened to END on the reverse leg, so cursor0 never got the chance to lead. Found only by mutating each branch SEPARATELY. **Whenever a rule appears once per direction, mutate BOTH copies — a suite can cover one perfectly and the other not at all** | Standing | **PROVEN** by executed mutation 2026-08-27 | `test_invariantC_cursor0IsPulledBackAfterAReverseFill` |
| 5.38 | ✅ **FIXED 2026-08-27. Correctness tests are BLIND to a performance regression that throws the design away.** The first `_allocate` loaded EVERY seat into a memory array before allocating, so a head-only swap read the whole roster and the cursors bought nothing. Measured: **98,294 gas @1 seat -> 215,296 @50**, growing linearly | **All 31 correctness tests stayed green under it.** The cursor design's entire value was silently discarded and nothing noticed. Fixed by driving `Allocation.step` directly over storage with early exit; now flat (97,737 -> 95,220). Carries a gas regression test | Standing | **PROVEN** (measured both ways) | `test_headOnlySwapCostIsFlatInQueueDepth` |
| 5.39 | ✅ **FIXED 2026-08-27. NEVER derive the swap direction from the SIGN of the balance delta.** Use `params.zeroForOne`, which is what PoolManager itself uses to pick the input currency | A dust swap whose output rounds to zero leaves `e1 == 0`, and a sign test then reads the direction backwards. **Measured: swaps of 1, 2 and 3 wei on a 0.30% pool at 1:4 reverted `DirectionMismatch`** — a liveness bug on swaps v4 itself accepts. The zero-output fill must also CREDIT the input (to the seat at the cursor), or the ledger under-counts the position and strands value owed to nobody | Standing | **PROVEN** (measured, then fixed, with 3 tests + a mutation) | `test/queue/Dust.t.sol`; `QueueHook::_afterSwap` step 2c |
| 5.40 | ✅ **FIXED 2026-08-27. `seed()` and `redeemAll()` were PERMISSIONLESS on the production hook.** `seed()` funds the position from THE HOOK'S OWN BALANCE, so the first caller of a pre-funded deployment claimed the whole queue for free; `redeemAll()` burns the ENTIRE position and anyone could call it | Both moved to `test/queue/QueueHarness.sol` (test-only, ADDS entry points and overrides nothing). **The shipping contract now has NO permissionless state-changing entry point at all** — only `unlockCallback` (guarded to PoolManager) and views. Supersedes 5.33 | Phase 2 | **FIXED** | `src/queue/QueueHook.sol`; `test/queue/QueueHarness.sol` |
| 5.41 | **The `QueueUnderflow` guard exists in TWO places** — `Allocation.allocate` (array form, fuzzed) and `QueueHook._allocate` (storage form, live). They are separate call paths, not duplication that can be removed | Mutating only the library's copy leaves the hook's copy untested and vice versa. **Mutate both.** Confirmed 2026-08-27: library copy caught by the fuzz, hook copy caught by `test_1_9` — each by exactly ONE test | Standing | **PROVEN** by executed mutation | `test_1_9_swapLargerThanTheQueueReverts`, `testFuzz_oversizedAllocationAlwaysReverts` |
| 5.42 | **Settlement must go through `CurrencySettler` (SafeERC20), never a raw `IERC20Minimal.transfer`.** The raw form ignores the return value, so a token that fails by returning `false` is treated as paid | The spike and the first draft of `QueueHook` both used the raw form. Also fixes native-currency support and the `-type(int128).min` negation overflow. `forge lint src/` is now CLEAN — keep it that way | Standing | **FIXED 2026-08-27** | `QueueHook::_resolve` |
| 5.43 | **`sweepFloatIntoPosition` IS BOUNDED BY THE SMALLER LEG.** Adding to a range straddling the price needs BOTH tokens, so a lopsided float is reinjected only in proportion to its MINORITY token and the majority stays stranded until its counterpart arrives | Depth recovery is PARTIAL. Do not claim the sweep "restores depth" — it restores as much as the float is balanced enough to pair up. It is a near-no-op, not a revert. Measured: a 1000e18/1e6 float reclaimed <0.1% of the majority leg | Phase 2+ | **MEASURED** | `test_2_17_sweepIsBoundedByTheSmallerLeg` |
| 5.44 | **A deposit far off the pool's RAW ratio becomes FLOAT, not depth.** Under the ABSORB decision the depositor is credited in full and loses nothing, but the pool gains almost no depth from them | Measured: a 1000e18 token0 deposit against 1e6 token1 moved position liquidity by <1% while >900e18 landed in float. A real consequence of ABSORB-over-REFUND; under REFUND the depositor would have been handed the surplus back and known immediately | Pitch / UX | **MEASURED** | `test_2_16_lopsidedDepositBecomesFloatNotDepth` |
| 5.45 | ✅ **FIXED 2026-08-27. FRONT-RUNNABLE POOL BINDING WAS A FREE, UNRECOVERABLE DoS.** `afterInitialize` originally bound the hook to whichever pool initialized FIRST. Anyone could bind a freshly deployed hook to a junk pool — permanently, since there is no admin to unbind it | Fixed by committing `(currency0, currency1, fee, tickSpacing)` at CONSTRUCTION and rejecting any other key with `WrongPool`. Costs nothing: the hook serves exactly one pool by design | Standing | **FIXED**, with tests | `test_2_20_cannotBindTheHookToAForeignPool` |
| 5.46 | **MEASURING INVARIANT F WITHOUT ACCRUED FEES IS A 9.6e15-WEI-PER-SWAP PHANTOM.** v4 accrues LP fees into `feeGrowthInside` and only realises them on `modifyLiquidity`, so a principal-only position valuation understates it by every fee ever earned | The "residual" read ~80% of the LP fee per swap and looked exactly like catastrophic ledger corruption. **It was the instrument.** Any position valuation must add `L * (feeGrowthInside - feeGrowthInsideLast) / 2^128`. Third instance of PITFALLS 5.27 this session | Standing | **PROVEN** (measured both ways) | `QueueFixture::_positionValue` |
| 5.47 | ⚠ **SUPERSEDED BY 5.80 — the figure below is correct AT THE SEEDED PRICE and does not generalise.** The §E.4 residual, re-measured on an on-ratio fixture: **~0.15 wei per swap per token** (17 wei after 120 swaps), growing LINEARLY and converging — it does NOT compound | An earlier figure of ~6.8 wei/swap came from a fixture whose deposits were wildly off the pool's raw ratio. **The safety property is linearity, not zero**: at 0.15 wei/swap a single token of shortfall needs ~10^18 swaps. F1 makes face value an upper bound and spreads the gap. **Phase 6 showed the truncation scales with price displacement — a drained pool at the tick floor loses ~1e9 wei on ONE swap — and that what generalises is the ratio against LIFETIME INFLOW, not a per-swap constant (5.80).** | Superseded by 5.80 | **MEASURED**, asserted, and the generalisation FALSIFIED | `test_2_13_residualIsLinearNotCompounding`; `Invariant.t.sol` |
| 5.48 | **Unit confusion in a decimals fixture cuts BOTH ways.** Supplying token1 in HUMAN units (1e6) against token0 in RAW units at a 4:1 raw price sent 98% of every deposit to float and produced a fictitious residual | LAW 1 says use unequal decimals — but v4 works entirely in RAW units, so fixture amounts must be in the pool's RAW ratio regardless of decimals. Getting this wrong makes the mechanism look broken when it is not | Standing | **PROVEN** (measured, then corrected) | PROGRESS 2026-08-27 |
| 5.49 | **An unnecessary "safety" line is indistinguishable from an untested one.** `_liquidityForAmounts` shaved a unit off the computed liquidity as insurance; mutation testing showed nothing could detect its removal | 5,000 fuzz runs without it found no counterexample, and the round trip is provably `ceil(floor(x*k)/k) <= x`. **Removed**, with `DepositOversized` kept as the loud backstop. When mutation testing says a line is undetectable, the honest question is whether it should exist | Standing | **PROVEN**, removed | `testFuzz_2_19_depositNeverChargesMoreThanSupplied` |
| 5.51 | 🚨 **PLAN §B.8's SPECIFIED EVACUATION IS A FREE DoS ON THE SWAP PATH, and it was found by executing the spec.** §B.8 said a seat transfer moves `(a0,a1)` into the sender's `pendingWithdraw` and leaves the position untouched. The allocator sources every swap's output FROM THE SEATS while the swap's SIZE is set by the POSITION, so a ledger-only evacuation makes the pool quote depth the queue cannot source | `transfer(self, id, 1)` is legal and costs only gas. A tail holder sitting on most of one token makes **every swap above the surviving balance revert `QueueUnderflow`**, for as long as they like, undoable at will. **FIXED by paying the capital out for real**, which burns the matching liquidity so the position falls in step with the ledger. Refusing to transfer a funded seat was rejected instead: Phase 4's buyout must be unblockable, or every incumbent holds a veto over their own buyout | Phase 3 | **PROVEN** by an executed control that runs the spec's design and the shipped one side by side | `test_3_11_negativeControl_ledgerOnlyEvacuationBricksTheSwapPath`; PLAN §B.8 CORRECTED |
| 5.52 | 🚨 **PAIRED-RULE ASYMMETRY, THIRD INSTANCE — and the first that was a straight theft.** The seat-ownership check exists once in `transfer` and once in `transferFrom`. `transferFrom` had a test; **`transfer` had NOTHING**, and removing its check survived all 71 tests. Removing `transferFrom`'s check also survived, because the only test there used an unapproved third party — and naming YOURSELF as `sender` skips the allowance branch entirely | Either mutant lets anyone take any seat for free **and be paid its capital on the way out**. PITFALLS 5.37 and 5.50 were lost wei; this one was funds. **The rule is now four-for-four: whenever a rule appears once per branch, per direction, or per entry point, mutate EVERY copy separately** | Standing | **PROVEN** by executed mutation; both now covered | `test_3_2b`, `test_3_3c` |
| 5.53 | **A BOUND IN THE RIGHT DIRECTION IS NOT A CORRECTNESS ASSERTION.** `withdraw` debiting the REQUEST instead of the PAYMENT survived the whole suite. The residual tests assert the unpaid leftover is SMALL (`assertLt(t0, 1_000)`) — and over-debiting makes it smaller, so the mutant passed *more comfortably* than the real code | Any assertion of the form "X is under a bound" is blind to every defect that moves X the same way as the fix. **Assert the identity (`seatAfter == seatBefore - paid`), not the magnitude** | Standing | **PROVEN** by executed mutation | `test_3_13_aClampedWithdrawalDebitsOnlyWhatWasPaid` |
| 5.54 | **A PATH THAT NEVER EXECUTES UNDER TEST IS ASSERTED-ABOUT DEAD CODE.** Every `pendingWithdraw` line survived mutation because no test had ever produced a NON-ZERO pending balance — while `test_3_2` was busy asserting things about it, all of which held vacuously at zero | The clamped path only runs when the position cannot release the ledger in full, which needs ~40 swaps of accumulated §E.4 residual AND the seats in front drained first. **Before believing a path is covered, check that it was ENTERED** — and make the test say so (`assertTrue(w0 != 0 \|\| w1 != 0, "nothing was retained: this test proves nothing")`) | Standing | **PROVEN** by executed mutation; 5 lines now red | `test_3_14`, `test_3_15` |
| 5.55 | **THE REENTRANCY WINDOW IS REAL AND PoolManager'"'"'S LOCK DOES NOT CLOSE IT.** `take` calls `IERC20.transfer`, handing a pool currency control mid-unlock. The lock stops any reentrant path needing a second `unlock` — but a withdrawal on the leg the FLOAT already covers needs none and executes in full | What it corrupts is `unlockCallback`'"'"'s measurement, which differences the hook'"'"'s own ERC-20 balances (it must: a fee-on-transfer currency delivers less than `callerDelta`). In the executed control it happens to underflow and revert — **an accident of direction, not a defence.** `nonReentrant` on every external ledger path is what makes the measurement'"'"'s precondition true. **NOT CLAIMED: no value-extracting sequence was found without the guard.** The guard stays because a correctness argument resting on an accidental underflow is not one, and Phase 4 adds more callable ledger paths into the same window | Standing | **MEASURED** (window reachable, corruption executed); exploitability **UNPROVEN** | `test_3_12_negativeControl_reentrantEvacuationCorruptsTheUnlockMeasurement` |
| 5.56 | **A SEAT TRANSFER IS A SECOND TRIGGER FOR THE 5.43 DEPTH DRAIN.** Evacuating a single-token seat sizes the removal on the BINDING leg, so surplus of the other token lands in the float, which the sweep can only partially reinject | Not new and not free — the churner pays gas and the §E.4 residual each time, and the value stays inside the queue via INVARIANT F. But **do not describe seat trading as depth-neutral**: it moves position into float exactly as withdrawal churn does | Phase 3+ | **REASONED** (same mechanism as 5.43, measured there) | PITFALLS 5.43; `QueueHook::_onSeatTransfer` |
| 5.50 | 🚨 **PAIRED-BRANCH ASYMMETRY RECURRED — a second time, in a second function.** The top-up cursor pull-back exists once per direction; the `cursor1` copy was covered, the `cursor0` copy was covered by NOTHING. Exactly 5.37, one phase later | This is now a confirmed repeating failure mode on this project, not a one-off. **Mutate every direction-symmetric rule separately, in every function that carries one.** Both are now covered | Standing | **PROVEN** by executed mutation | `test_2_8b_topUpBelowCursor0PullsItBack` |
| 5.57 | 🚨 **PLAN §B.10'S SPECIFIED RENT PAYMENT SOURCE IS BROKEN, AND IT WAS FOUND BY EXECUTING THE SPEC — the second time a plan row has failed this way (cf. 5.51).** §B.10 said rent is *"deducted from the seat's own `a0`"*. Front-first allocation drives seats to single-token composition ON PURPOSE, and INVARIANT C says the consequence outright: every seat below `cursor0` holds `a0 == 0` | Under any sustained run of one-for-zero flow the FRONT seats hold exactly zero of the rent currency and are foreclosed one after another **because of the direction the market traded** — rank set by flow instead of by price, which is the one thing QUEUE claims it is not. Second, independent argument: an EMPTY seat is pure rank, which Phase 3 exists to make holdable, and under `a0` rent it cannot be held at any price above zero at all. **FIXED with a per-seat prepaid meter in `currency0`, outside the position, outside `float0` and outside the allocator** — so Phase 4 adds nothing to the proven Phase 1–3 arithmetic. It is a meter, not collateral: no mark, no oracle, no bounty, and running it dry costs a place in the queue rather than the seat or its capital | Phase 4 | **PROVEN** by two controls running the spec's own implementation beside the shipped one | `test_4_13`, `test_4_13b`; PLAN §B.10 CORRECTED |
| 5.58 | 🚨 **"ALWAYS FOR SALE" IS A SLOGAN UNTIL THE ASK IS FIRM.** A buyout is an ordinary transaction in an ordinary mempool, so the holder sees it coming and raises the price. Repricing costs only rent for the seconds the raise is in effect: at τ = 10%/yr that is **four parts in ten million of the price per block** | Effectively free, so **every buyout is vetoable** and Harberger degenerates into a rent tax with a slogan attached. **FIXED:** a seat is always available at the LOWEST price it has been asked at, or paid for, within `FIRM_WINDOW`; a raise takes effect immediately for RENT and only after the window for the SALE. The three dodges are self-destructive rather than refused — raising leaves the old price firm; dropping to zero and re-raising atomically makes the window minimum ZERO; a self-transfer arms the window at what was paid, which for a plain transfer is zero | Phase 4 | **PROVEN** by an executed control (`NoFirmQuoteHook`) plus all three dodges executed | `test_4_11`, `test_4_12`, `test_4_26`, `test_4_27` |
| 5.59 | 🚨 **A PUSH DISTRIBUTION WEIGHTED BY A BALANCE THE RECIPIENT CONTROLS IS FLASH-LOANABLE.** Rent is split by the recipients' `currency0` balance READ AT SETTLEMENT, and `addToSeat` is the only way a holder can raise that number at will | `addToSeat(tail, huge) → settleRent(everyoneAhead) → withdraw(tail)` captures rent that accrued over a period the depositor was not there for, in ONE transaction, with borrowed money — **measured at 16× the honest share.** **FIXED by settling every seat AHEAD first**, which drains the pot before the new balance can weigh on it. A per-block cooldown would also close it and is the WRONG instrument: PLAN §B.12 counts "waiting one block boundary" as a proven evasion, and QUEUE passes that table precisely because it has no per-block reference anywhere. Cost: worst-case `addToSeat` measured at **2,337,576 gas** on a full roster with every seat ahead priced | Phase 4 | **PROVEN** by an executed control, both arms numerically | `test_4_14`, `test_4_44` |
| 5.60 | 🚨 **A GAS OPTIMISATION RE-OPENED THE HOLE IT SAT NEXT TO, BECAUSE ITS GUARD WAS EVALUATED AFTER THE THING THAT INVALIDATES IT.** `_onSeatTransfer` skips the lease reset when the seat is already in the post-transfer state. The predicate was read AFTER `_settleSeat` — and settlement can FORECLOSE the seat, which zeroes `selfPrice` | So a holder with a priced seat and an empty meter could transfer it to their own second address, have the foreclosure wipe the evidence on the way through, hit the skip, and reprice with **no firm quote against them** — exactly the dodge 5.58 exists to stop. **FIXED by reading the predicate before settling**, which is sound because settlement on an unpriced seat is a no-op. **The standing rule: any guard that decides whether to write state must be evaluated before anything that can change what it reads.** Found by executing the dodge, not by reviewing the diff | Standing | **PROVEN** by executed mutation and by the dodge itself | `test_4_12`; `QueueHook::_onSeatTransfer` |
| 5.61 | 🚨 **A FIXTURE THAT ONLY EVER EXERCISES SEAT 0 AT RANK 0 CANNOT SEE A RANK BUG, BECAUSE `0 << anything` IS `0`.** Every foreclosure test in the first Phase 4 suite demoted seat 0 from rank 0 | A demotion writing the seat id back at the WRONG byte offset was invisible to all of them; so was cursor1's copy of the demotion pull-back; so was the funding pull-back comparing a seat ID where it must compare a RANK, which leaves a cursor LEADING a funded seat — silent theft of rank. Four defects behind one convenient fixture. **Before believing an index is under test, check that the index and the value are DIFFERENT NUMBERS in at least one test** | Standing | **PROVEN** by executed mutation; all four now covered | `test_4_32`, `test_4_34`, `test_4_35`, `test_4_36` |
| 5.62 | 🚨 **NEW EXTERNAL ENTRY POINTS SHIPPED WITH NO OWNERSHIP CHECK UNDER TEST — the fifth instance of the paired-rule family, and the first found on a brand-new function rather than a forgotten twin.** `withdrawRent` and `setSelfPrice` both had their `NotSeatOwner` check removed by mutation and **both survived a 99/99 green suite** | Draining anyone's meter is theft outright; repricing anyone's seat is worse, because setting a rival's assessment to zero lets you buy their seat for nothing. Neither was caught by the review lenses, by reading the diff, or by any correctness test. **The rule: every new external function gets its authorisation mutated on the day it is written, not at the gate** | Standing | **PROVEN** by executed mutation; both now covered | `test_4_19`, `test_4_20` |
| 5.63 | **AN AGGREGATE IS A SECOND WRITER OF THE FACT ITS MEMBERS HOLD.** `escrowTotal` is debited by a distribution whether or not the payer's own meter was, so a settlement that credits without charging leaves the currency0 balance identity **perfectly happy** while the seats collectively own more than the hook holds | INVARIANT R only catches it because it also asserts the aggregate AGAINST THE SUM it claims to be. Without that line the mandatory §D.6 control passes. Same family as 5.37 / 5.50 / 5.52 / 5.62: a fact kept in two places | Standing | **PROVEN** by executed control; the consequence — a holder whose withdrawal cannot be paid — is executed too | `test_4_9a`; `QueueFixture::_checkInvariantR` |
| 5.64 | **HARBERGER ENFORCEMENT NEEDS SOMEBODY TO CALL `settleRent`, AND NOTHING SHIPS THAT DOES.** Rent accrues in `lastSettled` and moves only when the seat is poked or touched | This is NOT an off-chain component and none is required: the seats behind are paid by the poke, and any would-be buyer must poke to clear a delinquent incumbent out of the way before buying at the post-foreclosure price. Settlement is also FORCED at every point the lease is touched — repricing, funding, draining, selling, being bought. **But a seat nobody wants and nobody pokes accrues a debt nothing collects**, which is exactly the case where holding it is worth nothing. Say this, do not claim continuous collection | Standing | **REASONED** from the code; the forced-settlement half is PROVEN | `test_4_22`–`test_4_25`, `test_4_40` |
| 5.65 | **`selfPrice` MUST BE BOUNDED, AND THE REASON IS A DENIAL OF SERVICE RATHER THAN AN ARITHMETIC CURIOSITY.** An unbounded assessment overflows the rent product | A settlement that reverts is a seat that can never be foreclosed OR bought — and `_settleAhead` carries that revert into **every deposit behind it**. Bounded at `type(uint128).max`, which is above every amount v4 itself can represent (its deltas are `int128`), so the bound costs nothing real | Phase 4 | **PROVEN**; the maximum price is settled without reverting in the test | `test_4_18`; `Rent.MAX_SELF_PRICE` |
| 5.66 | 🚨 **`vm.cool()` DOES NOT MAKE A WRITE COLD, AND EVERY GAS NUMBER THIS PROJECT HAD RECORDED WAS OPTIMISTIC BECAUSE OF IT.** `vm.cool()` resets the EIP-2929 ACCESS LIST — the next `SLOAD` costs 2,100 again. It does NOT reset the value EIP-2200 meters a WRITE against. A slot the current test body already wrote is "dirty", and writing it again costs **100 gas** instead of 2,900 or 20,000 | So a suite that deploys, seeds and then measures inside one test body is measuring a contract whose entire storage is free to write. **Measured, same swap, same `vm.cool()`: 172,263 in-body vs 252,966 from `setUp()` — 47% optimistic.** Forge commits `setUp()` as its own transaction; state built there is metered honestly. `vm.snapshotState()` + `vm.revertToState()` is NOT a substitute — tested, identical numbers. Three tests were affected and all three were re-measured: the head-only flatness table, the pure-rank transfer (24,969 -> 23,208, and it went DOWN because Phase 5b's packing more than paid for the correction) and the worst-case deposit (2,337,576 -> 2,610,805). **LAW 4 amended** | Standing | **PROVEN** by executed A/B | AGENTS.md LAW 4; `test/queue/Gas.t.sol` |
| 5.67 | 🚨 **THE FIRST MEASUREMENT IN A TEST BODY COSTS ~21,200 GAS MORE THAN EVERY LATER ONE, AND IT LOOKS EXACTLY LIKE A PROPERTY OF WHATEVER IS MEASURED FIRST.** `vm.cool` is per-account; a v4 swap crosses six stateful contracts (hook, PoolManager, router, Permit2, both ERC20s) and the first call warms whatever was not named | The first draft of the Phase 5 table read a 21,179-gas excess on its depth-1 row as a real depth effect. It survived TWO wrong explanations — swap size, then the cursor's first non-zero write — each of which was plausible and produced a confident story. **What settled it was running the identical pair in BOTH ORDERS**: the excess followed the ORDER, not the depth. The rule: before attributing a gas difference to the variable you were varying, swap the order and check it does not follow the order instead. Comparative series now burn one swap on a roster they never measure | Standing | **PROVEN** by an order-swapped A/B | `GasTest._warmUp` |
| 5.68 | 🚨 **PLAN §B.9'S GAS TABLE WAS MEASURED ON A DIFFERENT CONTRACT, AND THE `MAX_SEATS = 32` JUSTIFICATION IT SUPPORTED WAS FALSE.** §B.9 quoted 6,753 gas per seat from the Phase-0 spike — which had no cursors, no owners, no seat tokens and no lease — and chose the roster bound as "comfortably inside a 300k budget" | Re-measured against the shipping hook the figure was **12,254**, so a full 32-seat sweep cost 412,028 gas of queue work: **37% OVER the budget the roster bound was picked to fit.** The bound was never justified by the numbers written next to it. Phase 5b's packing brought it to 8,070 and 278,110, which is inside the budget with 7% to spare, and `test_5_3b` now DERIVES the supportable depth (34 seats) from the measurement so the two cannot drift apart again. **The rule: a measurement inherited from an earlier prototype is not a measurement of the thing you are shipping** | Phase 5 | **PROVEN** by re-measurement; both numbers executed | PLAN §B.9 CORRECTED; `test_5_1`, `test_5_3`, `test_5_3b` |
| 5.69 | **AN UNUSED SECOND COPY OF INVARIANT C WAS LIVING IN A PRODUCTION LIBRARY.** `Allocation.allocate` returned a `nextCursor` computed by its own copy of the "advance only if the seat was exhausted" rule. Every call site discarded it, and no production path calls the function at all | Found by mutation (M61 SURVIVED): breaking the rule changed nothing anywhere. The honest answer was (b) DELETE the line — the second time on this project, after `_demoteToTail`'s early return. Testing it would have entrenched a duplicate of the single rule whose failure mode is silent theft of rank, in the exact family that has been wrong four times here. The cursor already has an INDEPENDENT witness in `QueueFixture`, written from §B.6's prose rather than from `src/`, asserted after every swap | Phase 5 | **PROVEN** by executed mutation; the replacement M61 targets the hook's real copy and goes RED | `Allocation.allocate`; `script/mutate.py` M61 |
| 5.70 | **TWO GAS OPTIMISATIONS WERE MEASURED AND REJECTED, AND THE MEASUREMENTS ARE THE POINT.** (a) Copying the packed seat through memory (`Seat memory sm = q[idx]`) to force one `SLOAD`/one `SSTORE`: **worse**, 8,477 vs 8,070 per seat — the memory round-trip costs more than the compiler's masking. (b) Hoisting `q.length` into an immutable, since the roster is fixed at construction: **85 gas per swap, 0.07%**, and it broke `test_3_9` | (b) is the instructive one. It created a SECOND COPY of the roster size, so the negative control that mints a seat at runtime — the control proving Phase 3's fixed roster is load-bearing — silently stopped seeing the new seat. The project's own immune system caught a 0.07% optimisation introducing the writer/reader class that has been wrong four times here. **Do not re-propose either without a number that beats these** | Phase 5 | **PROVEN** by executed measurement, both reverted | PROGRESS 2026-08-28 |
| 5.71 | **QUEUE COSTS A TRADER 36% MORE THAN A BARE v4 POOL ON THE COMMON SWAP, AND THAT NUMBER MUST BE SAID RATHER THAN ROUNDED AWAY.** Measured against a hookless pool on the same tokens, fee, spacing, price and full-range liquidity: 117,989 vs 86,820, **+31,169 gas** | The defensible claim is NOT "the common case is free". It is that the overhead is a CONSTANT a trader can price — flat from 1 to 32 seats, a spread of 21 gas — rather than something that grows with book depth. Two extra callbacks, two `protocolFeesAccrued` reads, and the allocator's cold reads of the order word, both cursors and one seat. **Do not claim parity with an ordinary swap; the first draft of `test_5_7` was named as if it had proven parity and had measured +36%** | Standing | **PROVEN**, measured against a real hookless pool | `test_5_7` |
| 5.72 | **`addToSeat` IS QUADRATIC IN THE ROSTER, AND AT 2,610,805 GAS IT IS THE STRONGEST ARGUMENT FOR THE ROSTER BOUND — STRONGER THAN THE SWEEP.** `_settleAhead` charges every priced seat in front, and each charge is distributed over every funded seat behind: O(priced ahead x roster) | The sweep is LINEAR and comfortable; this one is not. At `MAX_SEATS = 32` it is 8.7% of a 30M block, which is fine; at 64 it would be roughly four times that. **Any future proposal to raise `MAX_SEATS` must be argued against THIS number, not against the sweep.** The configuration is not free to an attacker — every settlement it forces also drains the payer's own meter, so griefing a depositor means paying rent to the seat being griefed — but the cost is real and it is on the honest-user path | Standing | **MEASURED** honestly (state built in `setUp()`) | `test_5_6`; PITFALLS 5.59 |
| 5.73 | 🚨 **THE DEGENERATE FILL CREDITED A SEAT AND LEFT THE OTHER TOKEN'S CURSOR LEADING IT — the FIFTH instance of "one rule, two places, right in only one of them".** A swap whose output rounds to zero credits its whole input to the seat at the OTHER token's cursor rank. `_allocate` has always pulled the incoming token's cursor back after crediting; the degenerate path never did | Once `cursor0` leads rank 0, every later one-for-zero swap starts BEHIND a funded seat and sources from the seats further back: the head is passed over, which is exactly the theft of rank the mechanism exists to prevent, and it persists until something pulls the cursor back. The credited amount is dust; the CURSOR CORRUPTION is not dust-bounded. **Found by the Phase 6 invariant campaign — `invariant_I3` and `invariant_I4` both go red — and by nothing else in 135 tests.** Family: 5.37, 5.50, 5.52 x2, 5.62 | Phase 6 | **PROVEN** by executed reproduction and by mutation M62/M63 | `test_6_1`; `QueueHook::_afterSwap` |
| 5.74 | 🚨 **`modifyLiquidity` REALISES ACCRUED FEES ON EVERY CALL, SO AN *ADD* CAN CREDIT THE CALLER — AND `unlockCallback` CHOSE ITS SUBTRACTION DIRECTION FROM THE SIGN OF `delta`.** `callerDelta = principalDelta + feesAccrued`. Whenever the position's accrued fees exceed the principal being added, the hook's balance goes UP on an ADD, and `b0Before - balanceAfter` underflowed | **`addToSeat` and `sweepFloatIntoPosition` — the only two paths capital has INTO the queue — reverted with an arithmetic panic for as long as the fees stood.** That is the ordinary state of a busy pool between two deposits, not an exotic corner: a free, self-healing-only-by-luck denial of service on the deposit path. **FIXED by making the measurement SIGNED** (`_moved`) plus a named `UnexpectedPositionDebit` guard on the removal side, where the sign genuinely is predictable. The realised fees land in the float, which INVARIANT F already counts as backing. **The rule: never predict the SIGN of a balance change from the sign of the request you made.** | Phase 6 | **PROVEN** by executed reproduction (`modifyLiquidity(+999)` took 3.6e17 token0 and 9e16 token1) and by mutation M64 | `test_6_2`; `QueueHook::_moved`, `::unlockCallback` |
| 5.75 | **A "FULL-RANGE" POSITION'S RANGE CAN BE LEFT, BECAUSE `minUsableTick(60)` IS -887220 AND `MIN_TICK` IS -887272.** The fixture's `_positionValue()` computed `getAmount0Delta(sqrtP, hi, L)` with the RAW slot0 price | Out of range the position is entirely one token and its value is `getAmount0Delta(lo, hi, L)`. Unclamped, the estimator **overstated the position by 8.28e18 wei** against a real `redeemAll()` and made INVARIANT F look broken while the ledger was correct to 12 wei. Two wrong explanations were entertained before the instrument was suspected. **The instrument was wrong, not the hook** — rule 10 again (5.27, 5.46): before believing any measurement, ask what the fixture cannot represent. The estimator now clamps, and after the fix it agrees with `redeemAll()` EXACTLY over a 500-call campaign | Standing | **PROVEN** by an A/B against real redemption | `QueueFixture::_positionValue`; `QueueHandler::_positionValue` |
| 5.76 | 🚨 **`LiquidityAmounts.getLiquidityForAmounts` NARROWS EACH LEG TO `uint128` BEFORE TAKING THE MINIMUM, SO A LEG THAT IS NOT BINDING CAN REVERT THE CALL.** The token1 leg is `amount1 · 2⁹⁶ / (sqrtP − lo)`, which diverges as the price nears the lower tick; its `toUint128` reverts `SafeCastOverflow` while the minimum — the only value the caller wanted — is in the thousands | Measured at `sqrtP` a factor of six above `sqrtLower`: depositing 393e18 of token1 reverted while the binding leg was **1,033**. `addToSeat` and `sweepFloatIntoPosition` fail for every depositor at once for as long as the price sits near a boundary. **FIXED by taking the minimum in 256 bits and CLAMPING to `Pool.tickSpacingToMaxLiquidityPerTick`** instead of handing `modifyLiquidity` a number it will reject — safe because the deposit policy already absorbs whatever the position cannot take as float. **The Periphery lens, exactly as written in AGENTS.md §5: one bug in a trusted 20-line helper compromises every caller** | Phase 6 | **PROVEN** by executed reproduction and by mutation M65 | `test_6_3`; `QueueHook::_liquidityForAmounts` |
| 5.77 | 🚨 **A ZERO SPAN IS NOT AN ERROR, AND `FullMath.mulDiv` ANSWERS IT WITH *EMPTY REVERT DATA*.** At `sqrtP == sqrtPriceAt(tickLower)` the position holds no token1, and `_liquidityToCover` asked how much liquidity releases some — a division by zero, reported as a bare `require`, i.e. no selector at all | **`withdraw` AND `_onSeatTransfer` both died.** The evacuation one is the serious half: §B.8 made that path unblockable ON PURPOSE and the buyout leans on it harder than the transfer does, so a holder standing at the tick boundary **could not be bought out** — exactly the incumbent veto Phase 3 went out of its way to remove. Fixed by treating a zero span as "this leg cannot be sourced", contributing nothing to the maximum. **Two lessons: an empty revert is a `require` with no message somewhere below you, and a revert on the evacuation path is a VETO, not an inconvenience** | Phase 6 | **PROVEN**; reached by the campaign, asserted at the fixed line through a harness seam | `test_6_4`; `QueueHook::_liquidityToCover` |
| 5.78 | 🚨 **RE-OPENED 2026-09-02 — THIS ROW IS FALSE AND IT WAS ITSELF A CORRECTION OF AN EARLIER ERROR. `QueueUnderflow` IS REACHABLE THROUGH AN ORDINARY SWAP.** The argument below rests on INVARIANT F, and **Phase 7 added `premiumOwed` to that identity**, so the position now exceeds the SEAT ledger by exactly the unsettled premium. Nothing re-examined the claim when the term was added. Executed: on a pool whose roster has exited, the position still holds 4.9567e16 wei of token0 and quotes 2.573e17 of liquidity it CANNOT trade, and every one-for-zero swap reverts — shortfall measured at `min(amtOut, premiumHeld)`, 4.41e12 on a 1e12 swap and 4.9566884481303539e16 at the binding size. A φ = 0 control is clean at every size, so the brick is PREMIUM-caused, not maturity-caused. **The lesson is the row's own history: it was written as a correction, was right when written, and rotted when a term was added to the identity underneath it — a docstring stating a STRUCTURAL IMPOSSIBILITY is exactly the prose that stops being re-derived (5.86).** The same dead claim lived in FOUR places: this row, `test_6_15`'s docstring, PLAN §C.6 line 66 and its table row at 1604 — all now corrected. Evidence: `Adversarial.t.sol` `test_6_15`, `Maturity.t.sol` `test_M7f`/`M13`/`M13b`/`M13c`. ~~Original claim: **`QueueUnderflow` IS STRUCTURALLY UNREACHABLE THROUGH THE POOL, AND PLAN §C.6's BOUNDARY TEST RESTED ON A FALSE PREMISE.** §C.6 asked for "a swap one wei larger than the queue" to underflow. There is no such swap: a swap takes out only what the POSITION holds, INVARIANT F says the position never exceeds the ledger (surplus measured at **exactly 0 wei** over the campaign), and INVARIANT C says everything below the cursor is empty | So `amtOut <= Σ_{rank >= cursor} a` always. `QueueUnderflow` is not a trader-reachable boundary — it is the LOUD failure that fires when the ledger and the position have come apart, which is what `Rank.t.sol`'s ledger-only-evacuation control demonstrates. An empty queue does not underflow either: the swap is a complete no-op. **§C.6 is corrected in place; do not re-add the boundary test** | Phase 6 | **PROVEN** by execution — a swap fifty times the queue's size fills, and repeated ones keep filling | `test_6_14`, `test_6_15` |
| 5.79 | **A MUTATION HARNESS THAT EDITS PRODUCTION SOURCE MUST RESTORE IT IN A `finally`, AND THIS ONE DID NOT.** `script/mutate.py` restored the file only on the happy path, so an interrupt left a MUTANT on disk and the next run read that mutant as its baseline | It happened in the Phase 6 session: an interrupted run left the `p == lo` guard (5.77) absent, a whole campaign ran against the mutated hook, and the only symptom was a `BAD-PATTERN` on the one mutation that targets that exact line. **Had that mutation not existed, the repository would have silently regressed.** Fixed with `try/finally` plus a report of every file it had to put back. **Corollary, also observed the same session: never run `forge test` or `forge fmt` while `mutate.py` is running** — it holds a mutant on disk for the duration of each case, so a concurrent run reads mutated source and reports a failure unrelated to anything you changed. **A tool that can leave the repository broken is worse than not having the tool** | Standing | **PROVEN** — it occurred and was caught by its own BAD-PATTERN report | `script/mutate.py::run` |
| 5.80 | **THE §E.4 REDEMPTION RESIDUAL IS NOT A PER-SWAP CONSTANT, AND "~0.15 wei per swap" DOES NOT GENERALISE.** The truncation scales with how far the price has been driven from where the liquidity sits, so a pool whose depth has been withdrawn into the float and whose price is then pushed to the tick floor loses **~1e9 wei on a single swap** | What DOES generalise is the ratio, and the denominator is **lifetime inflow, not the current ledger**: the residual accumulates while the ledger is drained by withdrawals, so the last wei on the books is eventually smaller than the residual it must absorb (the campaign reached `owed = 5,337,018,741`, `backing = 0` — a 100% shortfall of five gwei). Measured over the campaign: worst shortfall **under 1 part per billion of lifetime inflow**, worst SURPLUS **exactly 0 wei**. Nothing is stolen and the attacker pays six-figure multiples of the residual in swap input to cause it — but **the LAST holders to withdraw bear it**, and the README says so | Standing | **MEASURED** by the handler after every action, both directions | `Invariant.t.sol` `K` / `SHORTFALL_PPB`; `QueueHandler::_noteSolvency` |
| 5.81 | **THE DEPLOYMENT PATH HAD NEVER EXECUTED — NOT ONE LINE OF IT — AFTER SIX GREEN PHASES.** Every suite reached the pool through `QueueHarness.seed()` (TEST-ONLY) or through `deployCodeTo`, which places a hook at a chosen address. Neither is how the contract reaches a chain. The real path is mine-a-CREATE2-salt → deploy through the canonical proxy → `initialize` → `addToSeat`, and its first execution would have been a live broadcast | Fixed structurally rather than by adding a script: the sequence lives in `script/QueueDeployBase.sol` and is executed by TWO callers — the broadcast script and a test that asserts every beat — with the only difference being who signs (`_as`/`_stopActing`). Also run against a **fork of Unichain Sepolia**, which is what proves the vendored `AddressConstants` is not stale, the CREATE2 proxy is present, and the numbers are identical to the local run | Phase 7 | **PROVEN** by execution, locally and on a live fork | `test/queue/Deploy.t.sol`, `DeployFork.t.sol`, `script/QueueDeployBase.sol` |
| 5.82 | **PLAN §H.3's HOOK FLAG MASK WAS STALE: `0x0840` FOR A CONTRACT WHOSE PERMISSIONS ARE `0x18C0`.** It was the Phase-1 permission set, written down before `afterInitialize` (bind the one pool) and `beforeAddLiquidity` (refuse every external LP) existed | The failure mode is nasty: `BaseHook` validates the address in ITS constructor, which runs FIRST, so mining for a stale mask produces an address that reverts at deploy time with no diagnosis attached to it. **A permission mask must be DERIVED from `getHookPermissions()`, never copied from a document** — `test_7_1` reconstructs the mask from the contract's own struct and asserts it, so the two cannot drift again | Phase 7 | **PROVEN** — §H.3 corrected in place | `QueueDeployBase.FLAGS`; `test_7_1` |
| 5.83 | **TWO POOL-BINDING TESTS WERE LAW 2 VIOLATIONS, AND ONE WAS ASSERTING THE WRONG CONTRACT'S GUARD ENTIRELY.** `test_2_20` and `test_2_21` used a bare `vm.expectRevert()`. Worse, `test_2_21` re-initialized the IDENTICAL key — which `PoolManager` refuses from its own state with `PoolAlreadyInitialized` before the hook is ever called — so it passed whether or not `_afterInitialize` had an `AlreadyBound` guard at all | Demonstrated, not argued: deleting `if (bound) revert AlreadyBound();` left both tests GREEN in their old form, and both go RED now, naming the wrong selector (`WrongPool`) instead of merely "it reverted". The re-bind the HOOK must refuse is a DIFFERENT pool naming the same hook — a fresh fee tier. Kept honest by **M67** | Phase 7 | **PROVEN** by mutation | `test_2_20`, `test_2_21`, `test_7_4`, `mutate.py` M67 |
| 5.84 | **v4 DOES NOT LET A HOOK'S OWN ERROR THROUGH UNTOUCHED.** `Hooks.callHook` bubbles it inside `CustomRevert.WrappedError(target, selector, reason, details)`, so `vm.expectRevert(MyError.selector)` fails against a hook that reverted with exactly `MyError` | This is why bare `vm.expectRevert()` is so tempting on hook callbacks, and it is how 5.83 happened. The reason must be re-wrapped to be asserted: `WrappedError(hookAddr, IHooks.afterInitialize.selector, abi.encodeWithSelector(TheError.selector), abi.encodeWithSelector(Hooks.HookCallFailed.selector))` | Standing | **PROVEN** — three tests now assert the unwrapped selector | `test_7_4`, `test_2_20`, `test_2_21` |
| 5.85 | **THE VIEWER'S HARDCODED CALL SELECTORS ARE A WRITER/READER PAIR, AND THREE OF THE FOUR WERE WRONG.** `frontend/index.html` reads chain state with raw `eth_call` and no library, so it carries four selectors as string literals. The first draft was written from memory: only `ownerOf(uint256)` was right | The failure mode is the worst available: an `eth_call` to a selector that does not exist returns **EMPTY DATA**, so the page renders zeros and looks like a working demo of an empty queue in front of a judge. Closed the way every other writer/reader pair in this repo is closed — `test_7_6` READS the page, extracts the live `const` bindings (not merely "the selector appears somewhere"), and asserts each against the compiler's own | Phase 7 | **PROVEN**; negative control corrupts one selector and goes red naming it | `test/queue/Hygiene.t.sol` `test_7_6` |
| 5.86 | **A DOCUMENTED HAZARD THAT RECURS NEEDS AN INTERLOCK, NOT A LOUDER NOTE.** 5.79's corollary — never run `forge test` while `mutate.py` is live — was written down in Phase 6 and violated in Phase 7 **by the agent who had just read it**, compiling a mutated tree and briefly believing the result | `mutate.py` now publishes `.forge-snapshots/MUTATION_IN_PROGRESS` for the life of the campaign and `test_7_7` refuses to run when it sees one, printing what to do; the campaign's own `forge test` invocations carry `QUEUE_MUTATION_RUN=1` and pass through. The marker lives under `.forge-snapshots/` because that path already carries filesystem permission, so the guard needed no new grant. **Prose is not an interlock** | Phase 7 | **PROVEN** — both directions controlled: marker present → red with instructions; `QUEUE_MUTATION_RUN=1` → green | `script/mutate.py::run`; `Hygiene.t.sol` `test_7_7` |
| 5.87 | **REACHING RANK 2 NEEDS A TRADE WORTH ~1.5x THE POSITION'S OWN `token0`, AND THAT IS THE MECHANISM RATHER THAN A FIXTURE PROBLEM.** A first draft of the demo swept with 400e18 and touched exactly ONE seat. The allocator was correct; the trade was small | The queue IS the pool's liquidity, so reaching rank 2 means removing ranks 0 and 1's whole stock of the outgoing token from a FULL-RANGE position, and a constant-product curve releases 60% of a leg only for a 6.25x price move. Measured on the deployed contract: 2,500e18 in walks ranks 0 and 1 to zero and part-fills rank 2. **This is the honest, measured form of the README's "the back is reached only by trades large enough to sweep the front"** — and it is 5.17 (thin full-range depth) showing up in the demo | Standing | **MEASURED**, locally and on a Unichain Sepolia fork, identical | `test_7_5`; `SWEEP_IN` |
| 5.88 | **RENT SETTLEMENT CONSERVES `escrowTotal`; AN ASSERTION THAT IT FALLS PASSES AGAINST A LEAKING IMPLEMENTATION.** A first draft of the Phase 7 rent beat asserted `escrowTotal` DROPPED by the amount charged. It does not: `_distributeRent` moves the money from the payer's escrow to the recipients' escrow and writes `escrowTotal = escrowTotal - amount + pot` | The correct assertions are the identity, not a direction (5.53 again): the PAYER's own escrow falls by exactly `due`, the escrows BEHIND rise by exactly `due` in total, `escrowTotal` is UNCHANGED, and `unallocatedRent0` is zero while eligible recipients exist. That set is also the business model stated as an assertion — an LP-to-LP transfer that funds itself, taxing no new party | Phase 7 | **PROVEN** — the wrong draft failed against the real contract | `test_7_5` BEAT 6 |
| 5.89 | **THE POOL A DEPLOYED HOOK SERVES WAS NOT READABLE ON CHAIN AT ALL.** `key` is written once by `_afterInitialize`, this contract emits no event of its own, and every field of it — plus `bound` and the two ticks — was `internal`. An integrator (a viewer, a router, an analytics job, another contract) had to scan `PoolManager`'s `Initialize` logs and match on the hook address to learn even which currencies it holds; anything wanting the token DECIMALS in order to render a balance simply could not | Closed by `pool()`, a pure-disclosure `view` returning `(PoolKey, bool isBound, int24 lower, int24 upper)`. **`isBound` is RETURNED rather than inferred from a zero key** because `Currency.wrap(address(0))` is native ETH and therefore a legal `currency0` — a caller testing `currency0 != address(0)` would read an unbound hook as an ETH pool. Everything disclosed is already public (the key is in PoolManager's own event; the ticks are a pure function of `tickSpacing`), so this adds information ergonomics and no attack surface. **A hook that cannot say what it is attached to is not integrable**, and the frontend was hardcoding 18/6 decimals to work around it — which would have rendered balances a million times off on any other deployment, silently | Phase 7 | **PROVEN**; **M68** flips `isBound` and goes red | `QueueHook.pool()`; `test_1_11`, `test_7_8`; `mutate.py` M68 |
| 5.90 | **A VIEWER THAT DECODES BY BYTE OFFSET DEPENDS ON A STRUCT STAYING STATIC, AND NOTHING WAS CHECKING THAT.** `frontend/index.html` has no ABI library, so it slices `currency0` out of word 0 of `pool()`, `currency1` out of word 1 and `isBound` out of word 5. That is correct only while `PoolKey` remains a STATIC struct and is therefore encoded INLINE rather than behind an offset pointer | Add one dynamic member to `PoolKey` upstream and every field the page reads shifts by a word — it would render a plausible-looking WRONG address rather than fail. `test_7_6` checks the selector; `test_7_8` checks the SHAPE behind it, by `staticcall`ing `pool()` and asserting the returndata is exactly 8 words with the fields where the page expects them. **Reasoning about whether a struct is static is not evidence** | Phase 7 | **PROVEN** — asserts the actual returndata, not the declared type | `test_7_8` |
| 5.91 | **A NEGATIVE CONTROL THAT PASSES IS A FINDING ABOUT THE CONTROL, NOT ALWAYS ABOUT THE TEST.** Building `test_1_11`, the first control mutated `_allocate` to index by SEAT ID instead of rank (M5) — and the test stayed GREEN, which briefly looked like the new test having no teeth | It does not: the founding order is the IDENTITY permutation, so `_idAt(ord, i) == i` in any fixture where nothing has been foreclosed, and that mutation is a genuine no-op there. M5 is caught by the rank suites, which permute the order. The replacement controls discriminate properly — M60 (the fill takes the whole balance) makes it revert, and forcing the range back to full makes the narrowness guard fire naming both numbers. **Before concluding a test is weak, check the control is capable of being detected in that fixture at all** (5.27's shape: ask what the fixture cannot represent) | Standing | **PROVEN** — both replacement controls go red, the original is explained | `test_1_11` controls |
| 5.92 | **A GREEN MUTATION CAMPAIGN MEASURES THE DEFECTS SOMEBODY THOUGHT OF, NOT THE DEFECTS PRESENT.** The campaign ran **74/74 RED, zero survivors** — while an expert panel, reading the same source the same afternoon, found three real defects in `recenter()` and one in the clip. All four sailed straight through | The reason is structural, not sloppiness: **the mutations were written by the person who wrote the tests, against the same mental model.** M72 ("recenter runs while the spot is inside the band") and M73 ("recenter plants the band on occupied ticks") each went red on exactly **2 failing tests** — and those two tests were the very ones the panel showed were blind. No mutation said "the occupancy check is INSUFFICIENT" or "the remint deploys nothing", because nobody had thought those thoughts yet. **Zero survivors is evidence the suite covers the hypotheses it encodes. It is not evidence of correctness, and this project has been quoting it as if it were** | Standing | **PROVEN** — campaign green and the defects reproduced by reading, same source, same day | this session; `script/mutate.py` |
| 5.93 | **`recenter()` — DELETED. Three defects, one root cause: THE BAND AND THE WINGS COMPETE FOR THE AT-THE-MONEY TICKS, AND "OVERLAP IS FORBIDDEN" MEANS THE QUEUE CAN NEVER TAKE THEM BACK.** (a) Its occupancy guard read `poolManager.getLiquidity(poolId)` — liquidity ACTIVE AT THE CURRENT TICK — while the destination is a RANGE, so a wing resting inside the new band but not spanning spot was invisible and got swallowed, reopening N5 with no overlapping add ever submitted. (b) The guard was nonetheless CORRECT, which is worse: any wing covering spot blocks it, and price leaving the band IS price entering a wing, so **one wei of liquidity at the money stranded the whole roster.** (c) When it did run it took `min` of both legs to remint a band the price had left — which holds one token — so it deployed ~nothing and `sweepFloatIntoPosition` could not restore it | **Deleting it is what makes the rest sound.** `(tickLower, tickUpper)` is now written exactly once, in `_afterInitialize`, so `_beforeAddLiquidity`'s add-time disjointness test stops being a snapshot of a moving target and becomes a COMPLETE guard: no legal wing can ever become overlapping, because the band cannot move onto it. The honest cost is stated in the pitch rather than hidden — this is a FIXED-RANGE position, the pool's useful life is bounded by price staying near where it started, and `BAND_HALF_WIDTH` became a deployment parameter so the width can be sized to the pair | Standing | **PROVEN** by reading; capital was never at risk (see 5.94) | `QueueHook.sol` "why there is no `recenter()`" |
| 5.94 | **BOTH INSTRUMENTS COVERING `recenter` EARLY-RETURN AT `L == 0`, SO A REMINT THAT DEPLOYED NOTHING READ AS SUCCESS.** `_checkInvariantF` → `_positionValue()` → `QueueFixture.sol:548 if (L == 0) return (0,0);` and `_assertSolvent` → `redeemAll()` → `QueueHook.sol if (liq == 0) return (0,0);` | With `L == 0` INVARIANT F degrades to `Σa + pending == 0 + float`, which HOLDS — because the float now carries the whole inventory. Nobody was short a wei; the pool had simply stopped working. **5.54 (a path asserted-about that was never checked to have DONE anything) compounded by 5.75 (two instruments each reporting zero, and the test reading their agreement as success).** `test_recenter_emptyTicksFollowsThePrice` asserted the ledger, the band move and spot containment, and never once read `positionLiquidity()` | Standing | **PROVEN** — both early returns read at HEAD | deleted with 5.93 |
| 5.95 | **A ONE-LINE FIX THAT LOOKS OBVIOUSLY RIGHT BROKE A PROVEN INVARIANT, AND THE INVARIANT WAS RIGHT.** The clip's tolerance fires when the band's share is ZERO: `_bandStep` returns `(0,0,0)` and `0 + 1 >= amtIn` is TRUE at `amtIn == 1`, so the head is credited a wei the band did not earn. Adding `bandGross != 0` to drop it made `invariant_I1_ledgerEqualsTheGhost` go RED | The invariant is correct: **with no wings there is nowhere else the wei can have gone.** v4 cannot even attribute it (`feeGrowthGlobal` does not update at `L == 0`), so it sits in PoolManager owed to nobody, and Step 2c's degenerate-fill policy exists precisely to stop the ledger under-counting the position there. The real exposure needs a pool that HAS wings, a swap landing entirely in one, and `amtIn == 1` — bounded at a wei per transaction. Closing it needs the hook to measure what its OWN position received rather than infer it from the replay. **Reverted and logged rather than shipped** | Standing | **PROVEN** — reverting the one-liner turns I1 green again | `_queueShare`; this session |
| 5.96 | **THE `+ 1` TOLERANCE IS AN EMPIRICAL FIT AND IS ALREADY OFF BY ONE AT THIS PROJECT'S OWN SPACING.** `_bandStep` does ONE `computeSwapStep`; `Pool.swap` does one per tick-bitmap WORD (its `nextInitializedTickWithinOneWord` stops at a word edge even when nothing in it is initialised) plus one per initialised tick, each re-ceiling `amountIn` and `feeAmount` and re-flooring `amountOut`. Over a 1920-tick band: up to 2 steps at spacing 60 (~2 wei), up to 8 at spacing 1 (~14–16 wei) | **Left at 1 DELIBERATELY, because too tight fails SAFE.** The tolerance simply does not fire, the band numbers are taken instead, and those are the smaller pair (`bandGross ≤ amtIn`, `bandOut ≥ amtOut`) — the queue is under-credited and over-collateralised, residual wei landing in the float. Widening the number to make the identity path fire more often is loosening a tolerance to buy a green, which §3b forbids. The comment no longer states 1 as a proven bound | Standing | **ANALYSIS** (derived from `Pool.swap`'s loop, not executed) | `_queueShare` comment |
| 5.97 | **EVERY GAS NUMBER IN THE PROJECT WAS TAKEN ON A FIXTURE WHERE THE SWEEP DID NOT SWEEP.** `Gas.t.sol` was untouched by the band pivot and built its swap rosters through `_build` → `_open` → `_openRange(minUsableTick, maxUsableTick)` → the test-only `seed()` — **a full-range configuration production cannot create**, since `_afterInitialize` always snaps a band. On it `_tickInBand` is always true, so `_queueShare` took its fast path and `_bandStep`/`computeSwapStep` never executed in any measurement | Re-measured on the production band: the head-only figure and the headline are essentially unchanged (**128,625 vs 87,039 = +41,586 = +48%**), because the common path really is the fast path. **The SWEEP was understated by 79%: 764,200, not the published 426,470.** The slope is identical at 8,070/seat; the whole difference is the INTERCEPT (168k → 485k), which is the swap crossing out of the band. The old "full 32-seat sweep" was a trade that walked a handful of seats and stopped — 5.87's full-range reachability showing up as a broken instrument rather than a mechanism property. The control pool had to be re-ranged too: **a full-range control against a banded subject measures the RANGE, not the hook** | Standing | **MEASURED** both ways | `Gas.t.sol`; `test_5_3c` ceiling raised 500k → 850k with the reason recorded |
| 5.98 | **THE DEMO SIMULATED A DESIGN THAT HAD BEEN DELETED, AND ITS HEADLINE PRESET WAS 60% PAST TOTAL EXHAUSTION.** `frontend/index.html` ran `swapOut` as pure full-range constant product with no band edge, and its allocator credited 100% of the whole-pool output — **literally mutation M70**. Its "Sweeping trade" button sent 2,500 token0 into a band that is emptied by 1,578, and the page rendered "3 swept, 2 untouched" for a trade that in fact empties every seat | The tell is in the diff: the pivot's **only** edit to the hero paragraph was deleting the word "full-range" from the sentence, while the paragraph still claimed *"this is the contract's own arithmetic, not an illustration of it"* and quoted a 0.015% agreement with `test_7_5` measured on the pre-band hook. **An unverified claim of on-chain agreement is worse than no claim**, and it is the first thing a hostile judge would check. Rebuilt on concentrated-liquidity maths; capacity 1,578.5 derived independently two ways and agreeing | Standing | **PROVEN** — old preset reproduced, new model verified numerically | `frontend/index.html`; this session |
| 5.99 | **THE FAIR-ASK RULE IN THE DOCS AND ON THE DEMO WAS THE ZERO-DISCOUNT-RATE LIMIT, AND OVERSTATED THE ASK BY 3.4×.** `BUSINESS.md` §11.4 and `bkFairN` both told a holder "a rational cap on your ask is extra P&L / τ". A buyer paying `P` also pays rent `τP` forever, so the seat is worth **`V = A/(τ + k)`** | The consequence is not cosmetic: it sets **θ = τ/(τ+k) = 0.29**, the fraction of the front's advantage the rent actually MOVES backward. The other 71% is capitalised into the seat price and kept by whoever sells it, and **θ → 1 only as τ → ∞** (τ=25% → 51%, τ=100% → 81%). **No Harberger rent can ever make the tail whole** — which is why a back seat's rent covers only 30% of the fee income it forgoes, and why the shipped τ = 10% is set against every seat but the front | Standing | **ANALYSIS** — closed-form, checkable | `BUSINESS.md` §0.5; `frontend/index.html` |
| 5.100 | **THE MUTATION INTERLOCK GUARDS `forge test`, NOT EDITS TO `src/` — AND A BACKGROUND CAMPAIGN SILENTLY REVERTED ~200 LINES OF WORK MID-SESSION.** 5.86 made `mutate.py` publish a marker so a concurrent `forge test` refuses to run. It does nothing about a concurrent *write*: the campaign holds a mutant on disk and restores the original in a `finally`, so any edit made to that file while it runs is overwritten by the restore | The only symptom was `function recenter` no longer existing and a compiler error pointing at the CALLER. Nothing warned; the build simply described a world where two hours of work had not happened. **Rule: `src/` is READ-ONLY for the duration of a campaign, including for the agent who started it.** **CLOSED 2026-09-01 — the mechanism is built, because prose was not enough (5.86's lesson, again).** `mutate.py` now tracks what IT last wrote to each file; the `finally` restores only over content it put there, and over anything else it REFUSES, writes the pre-campaign original to `<file>.mutate-backup`, and prints what to check. The per-mutation loop raises rather than racing an editor. It caught a real instance the same session: the session opened with `withdrawRent`'s seat-ownership check MISSING from `src/` — a mutant left on disk by a campaign that was SIGKILLed, which `test_4_19` had been failing against | CLOSED | **PROVEN** — reproduced by inspection; fix exercised by interrupting a live campaign | `script/mutate.py`; `test_4_19` |
| 5.101 | **`recenter()` v2: EIGHT TARGETED TESTS GREEN, INVARIANT CAMPAIGN RED, CAUSE UNLOCATED — SO IT DOES NOT SHIP.** The rebuild genuinely fixes all three v1 defects (destination emptiness checked as a RANGE via a tick-bitmap walk plus the liquidity active at the near edge; a ONE-SIDED mint one spacing clear of spot, fully fundable from the token actually held; a half-width margin so the band cannot be ratcheted), and it restores the complete-guard property by asserting an outside LP still cannot mint inside the band AFTER it moves | **The bisection is the transferable part, and it took one 40-second run per hypothesis:** `swap`+`recenter` alone GREEN; adding `sweepFloat` RED; adding `addToSeat` RED; adding `withdraw` green; everything EXCEPT `recenter` GREEN. So it is recenter, and specifically its interaction with the paths that DEPLOY FLOAT INTO THE POSITION. Magnitudes rule out rounding: `I2b` shortfalls ~9.1e20 on inflow 9.1e21 (**10%**), excesses ~9e18, `I1` ledger-vs-ghost ~60,631 wei. **Lead:** after a v2 recenter the band sits BESIDE spot and never contains it, so the pool has ZERO active liquidity at the current tick — a state the contract has never operated in, and one where a deposit can only be absorbed on one side. Work, evidence and leads preserved in `docs/wip/recenter-v2/` | Standing | **PROVEN RED**; fix UNPROVEN | `docs/wip/recenter-v2/` |
| 5.102 | **`_solvent`'s over-backing branch was an ASYMMETRY, not a stronger claim, and only a band move can reveal that.** It asserted `backing == owed` EXACTLY. That held only because every shipped path moves value one way — deposits and swaps credit the ledger exactly, and v4 rounds for the pool, so the hook can only ever end SHORT | A burn/re-mint cycle rounds **both** ways, so the hook can finish holding a few wei MORE than the ledger records: measured **393 wei against an `owed` of 4.0e21**, 1e-19 relative, well inside the existing residual bound `K = 100_000`. Over-backing is the SAFE direction and the excess belongs to whoever withdraws last, exactly as the shortfall residual does. **The assertion was left STRICT** because no shipped path can trip it, and loosening it for an unshipped feature would blind a real claim; if a band move ever ships, the same bound must be applied to both signs. **Making it symmetric is also what let the campaign explore past the abort and find the LARGE divergences in 5.101** — a strict assertion can hide a bigger defect behind a smaller one | Standing | **MEASURED** both ways | `Invariant.t.sol` `_solvent` |
| 5.103 | **AVERAGE PRICING WAS THE HEAD'S FREE LANE, AND NOTHING IN 199 TESTS COULD SEE IT — IT IS AN ECONOMIC PROPERTY, NOT AN ARITHMETIC ONE.** Every seat a swap reached was credited the swap's realised AVERAGE price (`mulDiv(amtIn, take, amtOut)`). That conserves perfectly, so every conservation test, every invariant and 74/74 mutations were green. Simulated over benign / normal / toxic regimes, it made the head beat an ordinary pro-rata LP in **all three** — including the toxic regime that exists precisely to punish whoever is first into a stale price | A position that wins in every state of the world is a subsidy paid by the rest of the book, which is AGENTS.md §5's "free lane": the failure that has killed seven mechanisms here. **A swap sweeps a RANGE of prices**, and being filled first means being filled at the stalest end of it. `Allocation.step` now takes a price curve and credits each seat the segment it actually absorbed; the head loses to an ordinary LP in the toxic regime, and being first became a trade-off. Total unchanged to the wei — value moved, none created. Costs **+223 gas** on a head-only swap (the curve is built lazily and a one-claimant fill never builds one) and +1,875/seat on a walk | Standing | **SIMULATED** (60 paths, mechanism-faithful) for the economics; **PROVEN** for the arithmetic — N6, `Marginal.t.sol`, M74–M80 all red | `docs/research/seat-economics/`; `Allocation.step` |
| 5.104 | **THE MODEL BEHIND `BUSINESS.md` §0.5 LET THE HEAD REFILL FOR FREE ON EVERY TRADE, AND THE ERROR RAN THE HEAD'S WAY EVERY TIME.** It had no inventory and no cursors: seat 1 was available at full size for every trade in the series. The contract EMPTIES a seat, advances a cursor past it, and does not touch it again until the flow reverses — so the head's throughput is capped by its own capital per reversal, not by the trade count | Rebuilt faithfully, the conclusions moved enough that the old numbers were withdrawn rather than adjusted. Two modelling errors inside the rebuild are worth as much as the result: **an arbitrageur sliced into $2k trades erases the price impact that distinguishes the front of the book from the back** (marginal and average pricing became indistinguishable), and **clamping the price walk inside the band produced a pool with almost no LVR** where a pro-rata LP earned +62%/yr and every regime looked benign. A broken measurement that looks like a broken mechanism has cost this project more time than any real bug — this is the fourth instance | Standing | **SIMULATED** — both models runnable, side by side | `docs/research/seat-economics/README.md` |
| 5.105 | **A NEGATIVE CONTROL THAT DIFFERS FROM PRODUCTION IN TWO PLACES DIES OF THE ONE IT WAS NOT TESTING.** `Controls.t.sol`'s mutant reimplements `_allocate` with one line changed. When marginal pricing landed in production the mutant still priced at the average — so N4 (the missing cursor pull-back) began failing at swap 2 on `seat a0` instead of swap 3 on `INVARIANT C cursor1 leads` | It was caught only because the control asserts the **exact revert reason** rather than "it reverted" (LAW 2). Had it asserted `reason.length > 0` it would have stayed green and the cursor rule would have silently lost its only control. **Any change to production's allocator must be mirrored into the mutant in the same commit**, and the mutant now builds the price curve exactly as production does with N3 as its single deviation | Standing | **PROVEN** — reproduced, fixed, all 8 controls red for their own reasons | `Controls.t.sol` |
| 5.106 | **A SIGN IS NOT A MAGNITUDE: THE FIRST DRAFT OF THE MARGINAL-PRICING TEST PASSED UNDER A MUTANT THAT DELETED MARGINAL PRICING.** `test_8_3` asserted `headPrice < swapAverage < tailPrice`. Under AVERAGE pricing that is still true — the floored per-seat shares land a wei below the average and the remainder line puts the last seat a wei above it — so the test passed on pure rounding, proving nothing | 5.53's rule, reproduced exactly and in new code: **a bound in the right direction is not a correctness assertion.** Rewritten to require ≥10 bps of deviation on each side, fifteen orders of magnitude above the rounding it has to clear; measured spread is ~400 bps. **It was found only because the mutation was applied to the hook AND the witness together** — mutating the hook alone dies on the fixture's seat-by-seat comparison, which hides whether the new assertions have any teeth of their own | Standing | **PROVEN** — mutant green before the fix, red after | `Marginal.t.sol` `test_8_3` |
| 5.107 | **`python3 script/mutate.py --help` LAUNCHED THE FULL CAMPAIGN.** Unknown flags were discarded silently (`{a for a in args if not a.startswith("--")}`), so a typo started an 80-mutation run that holds a mutant in `src/` for ~30 minutes and locks the repository against `forge test` via the 5.86 interlock | Fixed: unknown flags and `-h`/`--help` print usage and exit without touching `src/`. **A tool that edits production source must do NOTHING on a typo.** The interrupted run also demonstrated the good half of the design — `kill -INT` let the `finally` run, the marker was removed and the file restored intact | Standing | **PROVEN** — reproduced, then fixed and re-run | `script/mutate.py` |
| 5.108 | **THE 300k GAS BUDGET `MAX_SEATS = 32` WAS DERIVED FROM NO LONGER HOLDS, AND `MAX_SEATS` IS THE WRONG THING TO CHANGE.** Marginal pricing took the queue walk from 8,070 to **9,945 per seat**, and the queue-attributable cost at 32 seats from 278,140 to **342,365** — the budget supports 28 | 32 is not a gas choice: it is the 32 bytes of the packed `order` word, asserted by `test_4_41`. Settling a structural constant with a gas measurement would waste four bytes of a word to protect a number that was itself a proxy. **The budget was raised to 400,000 with the reason recorded in the test, and the constraint that actually binds is asserted separately and did not move**: `test_5_3c` requires a full cold 32-seat sweep to be an ordinary transaction — 826,799 against an 850,000 ceiling. The path almost every trade takes is unaffected (128,625 → 128,848) | Standing | **MEASURED** | `Gas.t.sol` `BUDGET`, `test_5_3`, `test_5_3c` |
| 5.109 | **MARGINAL PRICING DUPLICATED THE BAND-ENTRY RULE INTO A SECOND FUNCTION, AND THE DIVERGENCE WOULD HAVE BEEN SILENT.** `_bandStep` (which replays the in-band fill) and `_initCurve` (which prices it) each wrote out `zfo ? (start < hi ? start : hi) : (start > lo ? start : lo)`. Caught by reading, in the same session it was introduced, before it could diverge | The fifth instance of one rule in two places on this project (5.37, 5.50, 5.52 twice). **What makes this one worse than its predecessors is that no assertion could have found it**: the fill would be REPLAYED from one anchor and PRICED from another, every seat credited a segment of a swap that did not happen, and **conservation would still tie out to the wei** because `wTotal` normalises the total away. Only the seat-by-seat witness or `Marginal.t.sol` could see it. Extracted to `_bandEntry`, used by both; M77/M78 now mutate the single definition and catch 74 and 118 tests where the duplicated pair caught 60 and 64 | Standing | **PROVEN** — the extraction strengthened both mutations, which is the measurement | `_bandEntry` |
| 5.110 | **A CONSTANT PRICE CURVE HANDS THE FIRST SEAT THE ENTIRE INPUT, AND `room == 0` PRODUCES ONE.** `_segmentIn` answers `outAmt >= cv.room` by clamping to the band edge, so with `room == 0` it returns the same value for every argument. `wCum` then equals `wTotal` at the first seat that does not finish the swap, and that seat takes all of `amtIn` while every seat behind it gets nothing | Guarded: `_initCurve` returns with `total == 0`, which `Allocation.step` reads as "no curve" and prices at the average. **Believed UNREACHABLE and written down rather than tested** — the third of §3b's three honest answers. A span too thin to hold one wei of the outgoing token can only have produced `amtOut <= 1`, and a single wei is absorbed by the remainder line before the curve is ever consulted. A mutation on this guard would therefore SURVIVE by construction, so none was added; that is the finding, not an omission | Standing | **ANALYSIS** — reachability argued, not executed | `_initCurve` |
| 5.111 | **A MUTATION WHOSE PATTERN GOES STALE STOPS TESTING ANYTHING AND SAYS SO ONLY IF YOU READ THE SUMMARY.** Changing `Allocation.step` from a one-line ternary to an if/else silently turned M59 into `BAD-PATTERN (0 matches)`. The campaign still reported "0 SURVIVED", which reads like success | **`BAD-PATTERN` is a FAILURE, not a status.** It means that defect was not tested on this run, and the count sits in a different column from the one everybody quotes. M59 was retired in favour of M76 (the same defect against the current source) with the reason recorded in `mutate.py` rather than deleted, because the BAD-PATTERN report is the only reason anyone noticed. **Rule: any edit to a line a mutation targets must re-run that mutation in the same commit** | Standing | **PROVEN** — reproduced by the campaign summary | `script/mutate.py` M59 → M76 |
| 5.112 | **THE BOOK IS ZERO-SUM AGAINST ITS OWN PRO-RATA BENCHMARK, MEASURED TO FOUR DECIMAL PLACES — SO NO ORDERING SCHEME CAN MAKE ALL 32 SEATS BEAT AN ORDINARY LP.** Front-first vs one undivided pro-rata LP, same capital, same flow, same seeds: volume served differs by **+0.0000%** and total P&L by **+0.0000 pts**, in both the benign and normal regimes | This kills the inventory-recycling hypothesis (that front-first serves more volume per dollar because the head is emptied and refilled by reversing flow). Availability is identical: the cursor only advances past EMPTY seats, so the inventory reachable behind it never changes. **The ceiling is all 32 seats EQUAL to a pro-rata LP, and deterministic time-rotation reaches it** — 932-point spread and 29/32 negative under static rank becomes a 13–18 point spread and 0/32 negative. Stop looking for a scheme that beats pro-rata; the search is closed. **AMENDED 2026-09-01 — THE +0.0000% IS A TAUTOLOGY AND THE ROTATION HALF OF THIS ENTRY IS WITHDRAWN.** Replacing `_fill` with an allocator that credits 100% of every swap's input to the head — the maximal free lane — scores `+0.000000%` as well, because availability is identical for EVERY allocator. It is a conservation check and it is blind to the split; it is a valid one-line algebraic refutation of inventory-recycling and it is not a 120-run measurement of anything. The '0/32 negative' rotation result is seed-selected (2/32 and 6/32 on fresh seed ranges with the pool profitable) and counts ensemble MEANS while 27% of individual path-seat outcomes lose. See 5.119 | **AMENDED** | **PROVEN** — negative control executed against the simulator | `docs/research/seat-economics/ROTATION.md` (superseded banner) |
| 5.113 | **BAND WIDTH DOMINATES EVERY OTHER ECONOMIC PARAMETER, INCLUDING THE ALLOCATOR AND τ — AND THE SHIPPED ±10% IS A LOSING LP POSITION ON A VOLATILE PAIR WHATEVER THE HOOK DOES.** Same flow, same seeds, uniform rotation: at ±10% on a 45%-vol pair the pool returns **−18.6%** and 24/32 seats lose; at ±30% the same pool returns **+14.9%** and **0/32** lose | The mechanism is the band dying: ±10% exits in ~18 days, and the terminal traversal is ONE lumpy loss that lands on whoever is at the front when it happens. **Rotation equalises the FLOW and cannot equalise a single terminal event.** Two consequences: `BAND_HALF_WIDTH` must be sized to the pair at deployment (it is already a constructor argument; 960 ticks was sized for a demo), and the rotation CYCLE must complete inside the band's life — at 24h epochs a 32-seat cycle is 32 days against an 18-day band, so it never completes and equalisation silently fails. Epochs are hours, not days | Standing | **SIMULATED** — 30 paths per cell | `report_rotation.py` §3 |
| 5.114 | **A ROTATING RANK BREAKS INVARIANT C, BECAUSE CURSORS ARE RANKS.** `cursor0`/`cursor1` index RANKS, and INVARIANT C is "every seat at rank < cursorX holds aX == 0". Rotate the rank→seat map underneath them and the invariant is false immediately: a cursor can LEAD a funded seat, which is silent theft of rank — exactly what N2 and N4 exist to catch | Proposed remedy, **UNVERIFIED and to be proven before any production code**: store `lastEpoch` and reset both cursors to 0 on the first swap of a new epoch. A lagging cursor costs gas but never money, so 0 is always safe; one SSTORE per epoch, not per swap. Cases that must be checked explicitly rather than assumed: a swap straddling the boundary (`beforeSwap` one side, `afterSwap` the other), two swaps in one block either side of it, the `amtOut == 0` degenerate-fill path which also writes cursors, and composition with foreclosure, which permutes `order` and pulls cursors back | Standing | **ANALYSIS** — reasoned from the source, NOT executed | `NEXT_SESSION_PROMPT.md` §4 Step 0 |
| 5.115 | **A POSITIONAL `abi.encode` CONSTRUCTOR ARGUMENT LIST IS A COPY OF THE CONSTRUCTOR'S SHAPE THAT THE COMPILER CANNOT CHECK, AND IT FAILS SILENTLY.** `QueueDeployBase._ctorArgs` encoded ten arguments for an eleven-argument constructor. It **compiled, deployed, and decoded the missing `premiumBps` out of the bytes following the roster's dynamic tail** — the pool came up with a garbage economic parameter | A typed call would have been a compile error; `abi.encode` is untyped. The only symptom was `test_7_5` reporting the demo's seller short by 2.4e15 wei, which reads like a ledger bug. The list had been hand-copied THREE times (`QueueFixture`, `QueueDeployBase`, `Controls.t.sol`); adding `bandHalfWidth` broke the third and adding `premiumBps` broke two. **Rule: never hand-write a second positional argument list — and past ten parameters, give the constructor a STRUCT.** The `Controls.t.sol` copy is deleted, its extra argument moved to a one-shot setter with an unset sentinel that reverts | Standing | **PROVEN** — reproduced, fixed, `test_7_5` green | `script/QueueDeployBase.sol`, `test/queue/Controls.t.sol` |
| 5.116 | **THE INDIRECTION THAT WAS ADDED TO THE MUTANT WAS LEFT OUT OF THE CHECKER TWENTY LINES BELOW IT.** `Controls.t.sol::_checkR` fed a RANK to `hook.seat()`, which is indexed by SEAT ID, while the mutant `_allocate` directly above carried a comment saying that indirection must not be left out. `QueueFixture._checkInvariantC` already had the fix | Latent only because those controls never foreclose — the moment anything permuted `order` the INVARIANT C check would have read unrelated seats and gone GREEN against a leading cursor, i.e. against silent theft of rank. This is the same defect shape as 5.37/5.50/5.52/5.105, landing inside the INSTRUMENT rather than the mechanism, which is worse: a broken checker cannot report itself | Standing | **PROVEN** — fixed; N2/N4 confirmed still red for the exact reason | `test/queue/Controls.t.sol` |
| 5.117 | **A VIEW THAT REPORTS LESS THAN THE CONTRACT WILL PAY IS A CORRECTNESS BUG, NOT A COURTESY.** `seat()` returned the raw ledger slot while `withdraw` settles accrued premium before checking entitlement — so a holder could withdraw more than the view promised, and every integrator pricing a seat would price it low | Caught by the deploy-path test, which asserted the seller was made whole and found them paid 4.03e18 MORE than `seat()` had reported. Fixed by making the view report the settled value and putting the claim formula in ONE place (`_claims`) that both the settlement and the view read — a view that can disagree with the settlement is the worst version of a rule kept in two places, because the disagreement is invisible until somebody's withdrawal differs from their screen | Standing | **PROVEN** — `test_7_5` red then green | `src/queue/QueueHook.sol` |
| 5.118 | **QUEUE'S PUBLISHED "+48% GAS OVERHEAD" WAS AN ARTIFACT OF COMPARING AGAINST A PLAIN POOL THAT HAD NEVER TRADED.** Measured steady-state on both sides — one swap through each pool in `setUp()` first — a hookless pool costs 69,970 and QUEUE at φ = 0 costs 131,821: **+88%, not +48%.** With the premium, 162,766: **+133%** | The control was paying its own one-time EIP-2200 zero-to-nonzero fee-growth writes, which a real pool pays once in its life. Same class of error as LAW 4's test-body seeding: not a wrong number, an unrepresentative one, and it was wrong in the project's favour. **Rule: a comparison is only honest if BOTH sides are in the same storage state.** Also: a per-seat accumulator mark costs one cold slot ≈ 5,000 gas per seat walked and no packing removes it — the sweep budget moved rather than `MAX_SEATS`, because 32 is the 32 bytes of the `order` word | Standing | **MEASURED** — `test_5_7`, `test_5_8` with a φ = 0 control contract | `test/queue/Gas.t.sol` |
| 5.119 | **A HEADLINE CHECKED ON ONE SEED RANGE IS NOT A RESULT, AND REPRODUCIBILITY IS NOT ROBUSTNESS.** Rotation's "0/32 seats negative whenever the pool is profitable" held on `SEEDS = range(30)` and failed on two of three fresh ranges — 2/32 and 6/32 negative with the pool profitable in both. `report_rotation.py` reproduces its checked-in output byte-for-byte, which proves only that it is deterministic | Compounded by an ensemble-mean framing: `seats()` averages across seeds and then counts `(mean < 0)`, while **27% of individual (path, seat) outcomes lose money and 25 of 30 futures contain a losing seat**. A seat holder lives one path. **Rule: any economic headline must be re-run on at least one independent seed range before it is written down, and per-outcome counts must be reported alongside per-mean ones** | Standing | **PROVEN** — re-run on three fresh ranges | `docs/research/seat-economics/` |
| 5.120 | **CONSERVATION CANNOT SEE A DEFECT THAT MOVES VALUE *WITHIN* THE ACCOUNTING.** Two premium mutations survived the entire suite: compounding the two claims (pays a seat out of the OTHER seats' unsettled claims) and dropping a pot with nobody standing (leaves `premiumOwed` counting wei that have become permanently unclaimable). Both keep `totals() + premiums()` tying out exactly | The general rule this project keeps re-learning (5.92): conservation cannot see WHO got the money. The detectors had to be different in kind — settle the WHOLE roster and watch the pot underflow, and assert the held pot was actually HELD and later actually PAID. Corollary found the same hour: a control must be able to ENTER the branch it mutates — the first compounding control could not, because a `zeroForOne` swap advances only one of the two accumulators | Standing | **PROVEN** — M83/M86/M87, red only after the tests were written | `script/mutate.py`, `test/queue/Premium.t.sol` |
| 5.121 | **A MUTANT ON DISK COMPILES AND PASSES, SO NOTHING IN THE TOOLCHAIN NOTICES IT — AND `git add -A` DURING A CAMPAIGN COMMITS PRODUCTION CODE THAT IS DELIBERATELY WRONG.** Two commits in one session snapshotted a mutated `src/queue/QueueHook.sol` while their own messages said "no production code changed". Caught by reading `git status`, not by any test | **THIS IS 5.79 AND 5.86 RECURRING FOR THE THIRD TIME, AND THE SECOND TIME AGAINST SOMEBODY WHO HAD JUST WRITTEN THE RULE DOWN** — the agent that did it had put "do not run anything while mutate.py is running" into the handoff hours earlier. 5.86's marker interlock could not help: it guards `forge test`, and committing is not running a test. A mutant is a mutation of WORKING code, so it builds, and the one suite that would fail is the one you are forbidden to run. **Prose failed twice; the fix is again a mechanism.** `script/hooks/pre-commit` refuses to commit while the campaign marker exists; install it once per clone with `sh script/install-hooks.sh`. Recovery: `git diff <last-good-commit> -- src/` is the check, and mutate.py's `finally` does restore correctly if it is interrupted with SIGINT rather than killed | Standing | **PROVEN** — the defect happened, the hook was verified to refuse | `script/hooks/pre-commit`, `script/install-hooks.sh` |
| 5.122 | 🚨 **HARBERGER PRICES THE *DURATION* OF AN ABSENCE, AND THE EVACUATION ATTACK HAS NO DURATION — SO THE MECHANISM SOLD AS "SUBORDINATION" WAS NOT ENFORCED BY ANYTHING.** `withdraw` was instant, took no lock, imposed no cooldown and did not touch the order word, so `withdraw(head) -> adverse swap -> addToSeat(head)` ran in ONE transaction, handed the fill to the seats behind, and returned to the front. Measured on two live pools identical but for the evacuation, 8 seats, a 5.8% adverse move sized to a common target price: the head lost **267 bps** of its seat value standing still and **0** evacuating, while ranks 1–7 gave up **10.7% more** inventory (+50.4e18 of 471.8e18) and lost **4.51e18** more. Round trip **403,485 gas**, **0 wei** of dust; the whole strike including the swap fits one call at **556,535 gas**. φ makes it WORSE: the premium is skimmed from the fill the head is receiving, so standing still cost an extra ~12.5 bps | **The lease could not see it: `Rent.owed(price, elapsed, ...)` and `_settleSeat` returns on `elapsed == 0`.** Same seat, same 100e18 self-price, funded meter: **8.219e17 wei over thirty days, EXACTLY ZERO atomically.** Rank is the only thing left for the mechanism to charge, so **`withdraw` now demotes to the tail via `_demoteToTail` whenever it PAID something** — the guard is on what was paid, never on what was asked, because the dust policy can clamp a request to zero and `withdraw(id,0,0)` must not cost a rank. **It does NOT refund the dodge and must not be described as doing so:** the one-shot edge is unchanged at +267 bps, because capital that is not in the pool cannot be filled. What it charges is the future — rank 0 vs rank 7 is ~932 pp/yr on `Premium.t.sol`'s own table, so breakeven is **~1.05 days** of front-seat tenure | Phase 8 | **MEASURED** (the attack, two live pools, mirror-checked against PoolManager's own balances) · **PROVEN** (the remedy; deleting the line goes red in 5 tests across 3 suites, dropping the paid-nothing guard in 3) | `test/queue/Evacuation.t.sol` `test_8_1`–`test_8_8c`; `QueueHook::withdraw` |
| 5.123 | 🚨 **TWO DOORS ARE STILL OPEN AND THE DEMOTION DOES NOT REACH EITHER — DO NOT READ 5.122 AS CLOSING THE CLASS.** (a) **`transfer` is a second evacuation.** `_onSeatTransfer` empties a seat's whole capital on any change of holder and deliberately LEAVES THE RANK ("Rank moves; capital does not"), so a holder with a second address evacuates without ever calling `withdraw`. On an UNPRICED seat it is completely free — `live` is false, nothing is armed. On a priced seat it costs a `FIRM_WINDOW` free option, since the transfer arms `firmPrice = paid = 0` and repricing only extends the deadline. (b) **A promoted seat carries a STALE price.** `_demoteToTail` writes `order` and the two cursors and NO lease; nothing arms a firm quote on promotion; `_setPrice` is holder-only. So the seat promoted into the vacated rank 0 is takeable on the spot at the price its holder set for rank 1 — executed at a **10× discount** | Any remedy has to live in `_onSeatTransfer`/`_payOut` — the ONE funnel every balance-reducing path already goes through — not in `withdraw`. The partial-withdrawal boundary is a trap in both directions: "any withdraw demotes" costs an honest LP their whole rank for 1 wei, and "only a full exit demotes" is evaded by withdrawing 99.99%. Exposure is linear in the balance removed, so the trigger has to be too. **UNCLOSED, and deliberately not built without the owner** — it changes who can take a seat and at what price | OPEN | **PROVEN** — both doors executed | `Evacuation.t.sol` `test_8_6`, `test_8_7` |
| 5.124 | 🚨 **THE PREMIUM'S HOLD BRANCH RATCHETED THE WRONG WAY AND ITS ROUNDING FLOOR DESTROYED MONEY OUTRIGHT.** `_accruePremium` held the WHOLE pot unless `w >= total`. Held pots ADD into the next `total`, so every hold made the next release strictly harder — a book that once stood thinner than the pot it generated could never pay it out however far it recovered. Worse, `premGrowth += mulDiv(total, 2^64, w)` FLOORS, and the release branch wrote `premiumHeld = 0` on the same line: **any pot below `w / 2^64` left the fill, entered `premiumOwed`, and became claimable by nobody, ever** — conserved by INVARIANT F and unreachable. No attacker needed; on this fixture's ~2.5e19 book that is any pot under ~1.3 wei, i.e. any sufficiently small swap. **This is 5.120's second half recurring as a live defect rather than a surviving mutant** | Fixed by releasing INCREMENTALLY: `give = min(total, w)`, hold only `total - give`, and hold everything when `w == 0` or `inc == 0` so a pot that cannot be paid is always DELAYED and never destroyed. It also tightens the overflow argument — `give <= w` gives `inc <= 2^64` directly instead of leaning on `w >= total`. Costs **+273 gas** in steady state and **+20,958** once per pool on a full sweep (one zero-to-nonzero `SSTORE` on `premGrowth`, which is now written where it used to be skipped); `Gas.t.sol`'s absolute sweep budget was re-baselined 1,050,000 → 1,100,000 with the attribution recorded | Phase 8 | **PROVEN** — 4 tests, both branches, all red before the fix; 6 mutations, **all 6 caught, each by its own branch's test** | `QueueHook::_accruePremium`; `Premium.t.sol` `test_7_10`–`test_7_13` |
| 5.125 | **THE MUTATION CAMPAIGN CAUGHT THE TEST AUTHOR COVERING ONE BRANCH OF TWO — AGAIN — AND THEN CAUGHT HIM ASSERTING A DIRECTION INSTEAD OF AN IDENTITY.** With only the token0 ratchet test written, restoring the ratchet in the token0 branch went red and **restoring it in the token1 branch SURVIVED THE WHOLE SUITE**. That is the fifth instance of one-rule-two-branches on this project (5.37, 5.50, 5.52 twice, 5.62). Separately, a mutation that ZEROED the retained remainder sailed through both ratchet tests, because they asserted only `heldAfter < heldBefore` — and zero is certainly smaller | Both fixed: `test_7_13` is the mirror, and both tests now assert the closed form `heldAfter == heldBefore + pot - min(heldBefore + pot, w)`, with `w` read AFTER the fill because that is when `_accruePremium` runs. **5.53 restated: a bound in the right direction is not a correctness assertion.** Also a reminder that a badly-built mutation proves nothing — the first attempt at this one inserted a write that was always overwritten, an equivalent mutant reported as a survivor (5.111) | Standing | **PROVEN** — the survivor was observed, the mirror written, and both branches then went red | `Premium.t.sol` `test_7_12`, `test_7_13` |
| 5.126 | 🚨 **THE PRIORITY PREMIUM WAS INERT IN ONE DIRECTION ON THE POOL THE DEPLOY SCRIPT SHIPS, AND THE SUITE THAT TESTS IT COULD NOT SEE THAT BECAUSE IT RAN AT EQUAL DECIMALS.** `premGrowth` is *incoming-token wei per **OUTGOING-token** wei standing*: numerator and denominator are DIFFERENT TOKENS, so the ratio's natural magnitude moves by `10^(dec_in − dec_out)` — `1e12` one way and `1e-12` the other on 18/6. Every rule expressed as a comparison between them was therefore a units error, including the guard `if (w < total) hold`. Measured on `QueueDeployBase`'s own 18/6 configuration, 8 swaps: **0 of 4 accruals moved the accumulator, 100% stranded, `0 wei` of token0 premium ever reached the roster** — against **0% stranded** on an 18/18 control identical in every other respect. The mirror direction was clean in both. `Premium.t.sol::setUp` ran `dec0 = dec1 = 18` | **No single fixed-point `Q` can serve both directions: it must be LARGE enough that the small-ratio direction does not floor to zero and SMALL enough that the large-ratio direction does not overflow.** So tuning the constant could not fix it. Fixed in two steps, both needed: `premGrowth`/`snap` widened to `uint256` X128 with `unchecked` wrap (Uniswap's `feeGrowthGlobal` pattern) so NO arithmetic condition remains; and the weight changed to `liquidityContributed`, which is ONE unit shared by both directions. The 18/6 pool now delivers **10,199,999,999,999,996 wei — bit-identical to the 18/18 control.** Rejected: keeping X64, which does not remove the defect but RE-INDEXES it — correctness then depends on the pool's liquidity magnitude instead of its decimals (`L` = 2.13e22 strands any pot under 1,155 wei, i.e. an `amtIn` under ~453,000 wei) | Phase 8 | **MEASURED** — executed at 18/6 against an 18/18 control, red before and green after | `test/queue/PremiumDecimals.t.sol`; `QueueHook::_accruePremium`, `standingL` |
| 5.127 | 🚨 **THE GUARD THAT CAUSED 5.126 WAS ALSO DOING A SECOND, GENUINE JOB — CAPPING PAYOUT CONCENTRATION — AND REMOVING IT EXPOSED A FREE LANE.** With the weight as inventory and no bound, `premGrowth += mulDiv(pot, Q, w)` hands the WHOLE pot to whoever holds the last wei of standing. Front-first drains the tail LAST, so the tail seat is **systematically** the only one left standing after a sweeping swap — a strategy, not a knife-edge. Measured: `standing1` fell to **1 wei** on seat 7, which claimed **100% of a 2.667e17 pot** having paid an eighth of it | **The bound and the decimals defect are the same units error seen from two sides**: "don't pay more premium than there is inventory standing" compares a pot in the INCOMING token against a weight in the OUTGOING one, which is why fixing one exposed the other. `liquidityContributed` resolves both — it does not collapse to dust when a seat is drained. Closed further by excluding the seats a fill PAID from the pot they generated (`_settlePremium`), which also restores "the payer does not pay itself" that inventory weighting gave for free | Phase 8 | **MEASURED**, test written and proven RED before the fix | `Premium.t.sol` `test_7_14`; `QueueHook::_settlePremium` |
| 5.128 | **`liquidityContributed` AS THE PREMIUM'S WEIGHT — three defects, one change, and two consequences worth knowing.** It also closes the free ride where a single-token in-range deposit mints ZERO liquidity (`_liquidityForAmounts` takes the min of both legs), contributes no depth, attracts no flow, and collected the full premium anyway. **(a)** It makes the compounding hazard UNEXPRESSIBLE — one weight that a settlement cannot move — so `CompoundingPremiumHook` became an EQUIVALENT MUTANT and was retired rather than left reading as coverage; `test_N7` now asserts the structural property instead. **(b)** `sweepFloatIntoPosition` mints depth from float that seats were ALREADY credited for; it is credited to NOBODY (`liquidityUnattributed`), because that float is capital nobody re-committed, and because apportioning it would make a permissionless function a THIRD writer of `liquidityContributed` and an O(n) full-roster write — a gas-griefing surface | INVARIANT L is EXACT rather than a bound: `standingL + liquidityUnattributed == positionLiquidity + liquidityShortfall`, where the shortfall names the one-unit-per-round-trip residual from `_liquidityForAmounts` sizing DOWN while `_liquidityToCover` sizes UP. Asserting a tolerance there instead would be blind to every defect moving the residual the way the rounding does (5.53) | Phase 8 | **PROVEN** — 4 mutations, all caught; INVARIANT L asserted on live scenarios | `QueueFixture::_checkInvariantL`; `QueueHook::_chargeBurn` |
| 5.129 | ⚠️ **`MAX_SEATS = 32` NO LONGER FITS THE 300k BUDGET IT WAS DERIVED FROM — THE BUDGET NOW PAYS FOR 27.** Phase 8 added a fourth storage slot per seat (the premium's weight) and unpacked the two marks, taking the per-seat walk from **14,778 to 19,280 (+30.5%)** and a full 32-seat sweep from 996,805 to 1,143,352. `MAX_SEATS` stays 32 because it is STRUCTURAL — one byte per rank in a 32-byte `order` word — not because 32 seats are affordable to sweep. **The hot path got CHEAPER in the same change**: a head-only swap at φ = 8500 went 162,766 → 152,947 (−6.0%) and steady state 183,998 → 174,179 (−5.3%), because the accumulator now divides by one weight instead of two and returns early for a seat that contributed no depth | `test_5_3b`'s assertion was WEAKENED deliberately, from `supported >= MAX_SEATS` to `supported >= SHIPPING_SEATS`, and says so in capitals at the site. **This is an OPEN DECISION**: lower `MAX_SEATS`, raise the stated budget, or accept that the deepest expressible roster costs more than 300k to sweep. It is beside 5.108, where `MAX_SEATS` was derived from this budget in the first place | ✅ **RESOLVED 2026-09-02 — see 5.144.** The budget it was measured against did not exist; `MAX_SEATS` stays 32 and `BUDGET` was deleted | **MEASURED** end to end against pristine HEAD, LAW 4 throughout | `Gas.t.sol` `test_5_3`, `test_5_3b` |
| 5.130 | **A MUTATION CAUGHT BY EXACTLY ONE TEST, INCIDENTALLY, IS A RULE NOBODY HAS ASSERTED.** Restoring the blanket demotion rule (`any payout demotes`) over the refined one (`only a withdrawal that reduces the seat's contributed liquidity demotes`) was caught by ONE test in 245 — the interleaving cursor fuzz, and only through its order witness. The rule that "taking profit does not cost a rank" had no directed test at all | Written as `test_8_9`, which fails on the restored blanket rule naming it: `TAKING PROFIT COST THE HOLDER THEIR RANK`. **The count of tests that catch a mutation is itself evidence**: one, from a fuzz side effect, is indistinguishable from luck | Standing | **PROVEN** — mutation re-run against the new test, red | `Evacuation.t.sol` `test_8_9` |
| 5.131 | 🚨 **THE HARNESS SWITCHED THE PREMIUM OFF AND NOTHING SAID SO FOR AN ENTIRE PHASE.** `QueueHarness.seed()` is the one seat-balance writer that lives outside `src/`, and when the premium's denominator became `liquidityContributed` it went on writing `a0`/`a1` and `standing0`/`standing1` while leaving every seeded seat at `liquidity == 0`. `standingL == 0` makes every accrual's weight zero, so **every suite built on `seed()` was silently testing a pool with the priority premium switched off** — green, and describing a contract nobody ships | **This is the 18/18 fixture defect (LAW 1, 5.126) in a different costume: a fixture that quietly disables the mechanism under test reads as evidence that the mechanism is harmless.** It is also the SECOND time this function has been caught out — it already had to learn `standing0`/`standing1` in Phase 7, and the comment naming it "the seventh writer" was right that it would happen again. Caught by `test_7_3` failing with **"the premium did not move"** — a "prove the path was entered" guard (5.54) earning its keep for the second time in one session. **Rule: when a mechanism gains a new denominator, the OUT-OF-`src/` writers are part of the change, and the way you find out is a test that asserts the mechanism actually did something** | Phase 8 | **PROVEN** — the failure was observed, the apportionment added, and every premium suite moved | `QueueHarness::seed`; `Premium.t.sol` `test_7_3` |
| 5.132 | 🚨 **THE INVARIANT CAMPAIGN HAD NEVER RUN AT φ > 0, AND WHEN IT DID IT WENT RED ON THREE COPIES OF INVARIANT F THAT PHASE 7'S AMENDMENT NEVER REACHED.** `Invariant.t.sol` never overrode `_premiumBps()`, so every invariant in it had only ever been checked against a contract with the priority premium switched off. Pointed at the shipping φ it failed immediately: `I1: token1 ledger != ghost` (3 wei on a 7.4e20 ledger) and `I2`/`I2b`: **"the position is OVER-backed"** | **INSTRUMENT, NOT MECHANISM — and it presented as a solvency defect, which is the most expensive way for an instrument to be wrong (5.75).** A premium is withheld from a fill and credited only when the seat is next touched, so in between the wei have entered the queue while no seat's RAW balance claims them; `totals()` is deliberately the raw slot sum. `QueueFixture._checkInvariantF` was amended for exactly this in Phase 7 — **this file carried the same identity THREE times (I1, I2, I2b) and none of them was.** One rule, four places, right in one: the sixth instance of the family (5.37, 5.50, 5.52 twice, 5.73, 5.125). With the `premiums()` terms added, **13/13 green at φ = 8500 with no change to `src/`** — so the mechanism was right and the campaign had simply never been allowed to look at it | Phase 8 | **PROVEN** — red before, green after, no production change | `Invariant.t.sol` I1/I2/I2b |
| 5.133 | ✅ **CLOSED IN PHASE 11 — the three premium-aimed actions 5.133 itself specified now exist and each carries an OUTCOME floor, not a call count: `settlePremiumOnSeat` (`withdraw(id,0,0)` is a pure `_syncSeat`, no money moved, no demotion), `swapToSeatBoundary` (exact-output sized to exhaust ranks `[cursor, r]`, reaching 5.152's `next = m+1`) and `swapSweepingTheWholeBook` (5.170's `sweptBook` branch). Scripted run: 25/10/12 attempts → 5 claims cashed, 3 boundary fills, 4 whole-book sweeps; fuzz run ~1,100 calls each, 0 unexpected reverts. The `_refAllocate` half is deliberately NOT done — porting a 200-line reference allocator into a second file is the one-rule-two-places hazard this repo has paid for nine times, and 5.176 is what that hazard looks like when it lands in an instrument. Front-first (I4) and per-seat composition remain covered by the fixture's witness in the directed suites.** ORIGINAL ENTRY: THE φ > 0 INVARIANT CAMPAIGN NOW RUNS, BUT ITS HANDLER WAS WRITTEN FOR φ = 0 — SO "13/13 GREEN" IS NOT THE COVERAGE IT LOOKS LIKE.** `QueueHandler` picks its actions (swap, deposit, withdraw, transfer, rent) without any notion of the premium: nothing in it deliberately settles a seat between two accruals, drives the `_settlePremium` payer exclusion at a chosen boundary, or aims at the `w == 0` hold. So the campaign now EXERCISES the premium incidentally, through ordinary flow, rather than attacking it | **Record the gap rather than let the number imply otherwise.** The campaign's value on this project has always come from being pointed somewhere new (5.132 is what it found the first time it was pointed here); pointing it at a mechanism its handler cannot aim at is a weaker claim than the pass rate suggests. Closing it means premium-aware handler actions — settle-a-seat, settle-ahead, and a swap sized to land the fill boundary on a chosen rank — and it belongs with the `_refAllocate` work, because both need the same model of what the premium is supposed to do | OPEN | **REASONED** — stated from the handler's source; the campaign itself is green | `test/queue/handlers/QueueHandler.sol`; `Invariant.t.sol` |
| 5.134 | 🚨 **PIPING THE MUTATION CAMPAIGN DESTROYS ITS EXIT CODE, WHICH IS THE ONE MACHINE-READABLE SIGNAL IT HAS.** The campaign was run as `python3 script/mutate.py 2>&1 \| tail -70`. A shell pipeline's status is the LAST command's, so `tail` returned 0 and **masked `mutate.py`'s exit code 1** — the flag it raises when anything other than RED occurred. Six BAD-PATTERNs were caught only by a human reading the summary text | **Rule: never pipe the campaign. Redirect to a file and read `$?`** — `python3 script/mutate.py > /tmp/campaign.log 2>&1; echo $?`, then read the file. This is the same family as 5.111 (a mutation whose pattern goes stale stops testing anything and says so only in a column nobody reads) and it is the reason 5.111 recurred: the report was there, the SIGNAL was thrown away. The repair run for the six cases was executed this way and its exit code was checked | Standing | **PROVEN** — the masking happened and was diagnosed | `script/mutate.py` |
| 5.135 | **SIX MUTATIONS WENT STALE IN ONE PHASE, AND THE THREE FAILURE MODES ARE DIFFERENT — TREATING THEM ALIKE WOULD HAVE LOST TWO OF THEM.** Phase 8 rewrote the lines M5, M62, M63, M82, M86 and M87 target. (a) **M5 became AMBIGUOUS**, not absent: its pattern was the bare line `Seat storage seat_ = q[_idAt(ord, i)];`, which was unique until `_settlePremium` introduced a character-identical line — two matches is a BAD-PATTERN. Repaired by carrying `_allocate`'s loop header, which is unique. (b) **M62/M63/M87 moved**: same defect, same deleted line, different surrounding text. (c) **M82's DEFECT CHANGED ITS EXPRESSION ENTIRELY.** It was "accrue BEFORE the fill", which worked only because the weight WAS the seat's inventory; with `liquidityContributed` a fill does not move the weight, so accrual order decides nothing and the old mutation would have proved nothing even had it matched. The same defect is now "do not exclude the seats the fill paid" — re-pointed, and RED against 14 tests | **A BAD-PATTERN is not a formatting problem. Ask what the mutation was PROVING, then ask whether that property still exists and by what mechanism it is now enforced.** M86 turned out to be the fourth case: its defect (compounding the two claims) is UNEXPRESSIBLE once one weight serves both directions, so it was RETIRED with the reason recorded — per M59's precedent, a retired case with a reason is evidence and a deleted one is a gap. All five repairs RED, 0 SURVIVED, 0 BAD-PATTERN | Phase 8 | **PROVEN** — `python3 script/mutate.py M5 M62 M63 M82 M87` → 5 RED, exit 0 | `script/mutate.py` |
| 5.136 | 🚨 **THE COMPETITOR IN THE HEADLINE COMPARISON WAS HANDICAPPED BY A PARAMETER NOBODY SWEPT, AND EVERY PUBLISHED φ WINDOW WAS COMPUTED AGAINST IT.** `report_shipping.py` swept the managed-wing bar at widths **1%, 2% and 5% only** — never at 10%, the band's OWN width. At `w = 10%` the keeper does **ZERO re-mints**, pays ~0 conversion cost, and scores **+4.45% BENIGN against the published "best" of +2.00% at w = 5%**. Re-run against the keeper's best width, **every published window is EMPTY in every regime**: BENIGN back needs φ ≥ 7455 while the front allows only φ ≤ 6763; NORMAL 4114 against 3486. **The shipped φ = 7,900 sat in an empty window** | **A comparison is only a control if the competitor is allowed its best move (LAW 5).** All three swept widths lose to passive LPing, so "rank 1 beats the keeper" was measuring a cheaper version of a DOMINATED strategy. The structural reason the sweep had to go wider: **as the keeper's width approaches the band's, the keeper BECOMES the passive LP**, so `B₁ → LP` and the front's bar can never sit far below the back's. **The general rule: when a benchmark has a free parameter, sweeping it to where the benchmark DEGENERATES INTO THE NULL STRATEGY is not optional — that is where the answer lives.** Same family as 5.75 and LAW 5's first corollary: the strongest control is the one that could falsify your own headline | Phase 9 | **MEASURED** — 120 paths, SE reported, the w = 10% cell executed | `docs/research/seat-economics/FRONTIER.md` |
| 5.137 | 🚨 **THE PRODUCT'S ENTIRE VALUE IS `c₁·(LP − B₁)` IN CLOSED FORM — AND THREE SESSIONS OF φ SWEEPS WERE MEASURING A QUANTITY THAT CANCELS.** With `c_i` a seat's capital share, `r_i` its φ = 0 return, `LP` the pro-rata LP and `B₁` rank 1's own outside bar, the identity `Σ c_i r_i == LP` gives `needed = Σ_{i≥2} c_i(LP − r_i) = c₁(r₁ − LP)` and `available = c₁(r₁ − B₁)`, so **`SLACK = c₁(LP − B₁)` and `r₁` CANCELS.** Rank 1's own return — the number measured over and over here — does not enter. Neither does `N`, the capital schedule beyond `c₁`, the ordering rule, the premium's weighting, the rent, or φ | **Three consequences, each of which redirects work.** (a) The product is worth exactly as much as rank 1's alternative is BAD; set `B₁ = LP` and the slack is EXACTLY `0.0000 pp` — that is the LAW 5 control and it fires. **So QUEUE clears only for CONSTRAINED capital that cannot take the passive-LP option**, and the size of the product is the size of the constraint. (b) **Depth was never the economic variable — `c₁` was, linearly:** shipped 5-seat (`c₁ = 0.333`) → 0.19 pp of book; 32 EQUAL seats (`c₁ = 0.031`) → 0.018 pp, nothing. That answers 5.129 from the economics rather than the gas. (c) **No re-weighting of the premium can create value, only stop destroying it** — the ceiling is independent of the ordering rule, so premium work must be justified by how much of the ceiling the CURRENT rule throws away, never by a claim to improve the economics | Phase 9 | **DERIVED + CONFIRMED** — long form against closed form agree to 2.4e-16 / 5.2e-18 / 4.8e-17 over 120 seeds | `FRONTIER.md`, `frontier.py`, `frontier_exact.py` |
| 5.138 | ⚠️ **THE CLOSED FORM IS OPTIMISTIC THE MOMENT ANY BACK SEAT IS ALREADY ABOVE `LP`, AND THE FIRST DRAFT SHIPPED THE OPTIMISTIC ONE.** Substituting the identity replaces `Σ_{i≥2} c_i(LP − r_i)` with `c₁(r₁ − LP)`. That is legitimate only if EVERY back seat needs lifting: what must actually be funded is `Σ c_i·max(0, LP − r_i)`, because **φ cannot claw back from a seat already above the bar** — it only ever takes from the seat a fill PAID. In TOXIC, seats 3/4/5 sit above `LP` and the two forms disagree by **0.47 pp** (−0.3680 against +0.1056) | Both forms are now computed side by side in `frontier_exact.py` and the condition is stated at the site. **Corrected result: BENIGN and NORMAL windows DO exist (+0.1915 and +0.1619 pp of book, ~1%/yr) — 98.3% of the calendar by band life — while TOXIC is empty for a simpler reason than the tables implied: rank 1 is ALREADY below its bar at φ = 0 (−3.278% against −2.18%), so there is nothing to redistribute before the first wei moves.** This is 5.53's family — a formula that is right in the regime it was tested in and silently wrong outside it — and it was caught by asking which term could go NEGATIVE, not by looking harder at the number | Phase 9 | **MEASURED** — both forms executed, the disagreement located and quantified | `frontier_exact.py` |
| 5.140 | 🚨 **I CAUGHT THE HANDICAP ON ONE AXIS, PUBLISHED THE "CORRECTED" NUMBER, AND IT WAS STILL HANDICAPPED ON A SECOND AXIS I DID NOT THINK TO ENUMERATE — WITHIN HOURS, IN THE SAME DIRECTION, FOR THE FOURTH TIME.** 5.136 found the keeper bar swept over WIDTH only to 5% and never to the band's own 10%; the corrected `B₁ = +4.45%` went straight into `FRONTIER.md`, `BUSINESS.md` §0, the `README.md` banner and a published memo as "0.19 pp of book ≈ 1.1%/yr, thin but real". **But that keeper is modelled converting AGAINST ITS OWN POOL, and that single term is 61.60 of its 70.23 points of cost — 88%.** It forces the keeper to push its whole $333,333 through a two-thirds-sized copy of its own pool on EVERY re-mint. A real keeper routes through an aggregator: **off-venue at 5 bps it scores +6.20% against the passive LP's +5.02%**, so `LP − B₁` is NEGATIVE, `SLACK = c₁(LP − B₁) < 0`, and **no φ clears in any regime, by identity** | **THE RULE, and it is the more important half of 5.136: "I swept the parameter that was wrong" is NOT the same as "the benchmark is now allowed its best move." ENUMERATE THE AXES BEFORE TRUSTING A MAX.** A benchmark's cost model has as many free parameters as the modeller had assumptions, and the one that dominates is rarely the one being argued about. Corollary, and it is why this recurs: **the handicap always survives in the direction that flatters us**, because a baseline that makes our number look good is the one nobody re-reads. Three earlier instances in the same direction: the gas figure wrong twice (+48%, +133%), the φ sweep "checked in" when it did not exist, "30 to 758 points" rescaled across incompatible rosters (5.141). Retracted in place in all four documents plus the published memo, same day, rather than edited quietly | Phase 9 | **MEASURED** — the off-venue cell executed, 120 paths | `docs/research/seat-economics/FRONTIER.md` retraction banner |
| 5.141 | 🚨 **"30 TO 758 POINTS" WAS MEASURED ON A ROSTER WE DO NOT SHIP AND THEN RESCALED BY THE HEAD OF THE ONE WE DO — A 10.7× MULTIPLICATION ACROSS TWO CONFIGURATIONS THAT OUR OWN DOCBLOCK SAYS ARE NOT INTERCHANGEABLE.** The numbers exist only at `results-addendum.txt` §D, parsed out of `results-tranche.txt`, which is a **32-EQUAL book with a $31,250 head** (`report_tranche.py:27,91`). `report_addendum.py:324` then multiplies that per-dollar advantage by `C = BOOK*5/15 = $333,333`, the SHIPPED head — while `report_shipping.py`'s own docblock lines 4-9 says head size is *the dominant economic parameter* and that the two configurations "are NOT interchangeable". **On the roster `QueueDeployBase` actually deploys the same quantity is −1.1 to +9.7 points** (BENIGN +9.67 ± 0.82, t = 11.8, 100% of paths; NORMAL +1.70 ± 0.28; TOXIC **−1.10**, won on 1% of paths). The published range also silently dropped the two TOXIC rows that sit in the same table — the honest range even on the tranche config was 0.33 to 758 | **A number is not portable between configurations just because both are "the model".** The rescaling was arithmetically valid and economically meaningless, and it was performed by code that sits one file away from a docblock forbidding exactly it. **The check that would have caught it in one line: recompute the headline on the shipped roster directly instead of scaling a proxy.** Related: 5.142 — "80–123%/yr" is cited at `QueueDeployBase.sol:87` and **appears nowhere on disk**; no results file contains a `%/yr` conversion cost, and annualising all 30 published and measured cells produces neither 80 nor 123. UNPROVEN, retracted | Phase 9 | **MEASURED** — shipped-roster figures executed, 120 paths, paired per path with SE and t | `report_addendum.py:106,282,324`; `report_tranche.py:27,91` |
| 5.142 | 🚨 **THE φ SWEEP THAT SET THE SHIPPED CONSTANT WAS RUN ON THE PREMIUM RULE WE DO NOT DEPLOY — PHASE 8'S OWN LESSON, RECURRING AT THE INSTRUMENT LEVEL, IN THE SAME COMMIT THAT TAUGHT IT.** `results-shipping.txt` was produced with `sim.PREM_WEIGHT = 'inventory'`, the PRE-Phase-8 weighting: `report_shipping.py` never sets the flag and `sim.py:66` defaults to it. So the window **[7455, 8312]**, and therefore **φ = 7,900**, describe a mechanism with NO `[start, next]` exclusion in it at all. **The rule we actually ship has never been swept.** Worse, `results-addendum.txt` §E states the gap in as many words — *"`PREM_WEIGHT = 'liquidity'` is implemented and unit-checked. It is NOT the default and no grid has been run on it"* — and **both files landed in commit 2d2a26f**, so the disclosure and the number it invalidates shipped together | **This is 5.126 one level up.** Phase 8's finding was "the premium was inert on the POOL we ship"; this is "the constant was solved on the RULE we do not ship", and the same session produced both. **A simulator flag that defaults to the superseded behaviour is a fixture that silently disables the mechanism under test (5.131), and the defence is the same: a report must PRINT the configuration it ran, not inherit it.** Consequence: every seat-2/TOXIC explanation built on that table — including my own boundary-exclusion hypothesis — was explaining a rule with no exclusion in it, so the exclusion could not have been the cause. φ = 7,900 is now retracted TWICE: wrong weighting here, handicapped `B₁` in 5.140 | Phase 9 | **PROVEN** — flag default read at source, disclosure quoted from the same commit | `sim.py:66`; `report_shipping.py`; `results-addendum.txt` §E |
| 5.143 | **"STRICTLY BETWEEN TWO RULES" IS A CLAIM ABOUT A WEIGHT AND NOT ABOUT A PAYOUT, BECAUSE SHARES RENORMALISE — AND I ASSERTED IT AS A SAFETY PROPERTY OF A FIX I WAS PROPOSING.** I argued that pro-rating the boundary seat's weight by the fraction of inventory it still had standing was "strictly between excluded and full weight" and therefore could not overpay. Measured at n=5, head drained exactly, seat 2's share of the pot: **excluded 0.0%, pro-rated 25.0%, full-L 20.0% — the pro-rated rule pays MORE than the un-excluded one.** Shrinking ONE seat's weight raises every OTHER seat's share, so a rule bounded between two others on the weight is not bounded between them on the payout | **The proposal also reproduced `test_7_14` at 100% of the pot** (measured at four dust levels, identical at all four): the concentration was never caused by the weight being inventory, it is caused by every OTHER seat's weight being zeroed, and pro-rating still zeroes every fully-drained seat — so when the boundary IS the last seat it is the only nonzero weight. **It was a partial return to inventory weighting wearing a refinement's clothes.** Recorded because of who made it: the same person who had, in the preceding hour, told three other agents to apply LAW 5's test to their own proposals. **Vigilance is not the defence (LAW 5, third corollary); a measurement against a case with a known answer is** — here, `test_7_14`, whose answer was already on file | Phase 9 | **MEASURED** — `scratchpad/attack_7_14.py`, four dust levels; sandwich check over 24,177 accruals with a live negative control | proposal WITHDRAWN, not shipped |
| 5.144 | 🚨 **THE GAS BUDGET `MAX_SEATS` WAS "DERIVED FROM" DID NOT EXIST, AND IT HAD BEEN MISLABELLED THROUGH TWO RAISES.** The constant read `BUDGET = 550_000` while its own log line printed "seats the 300k budget supports", and the depth it published — 27 — was `(550,000 − 24,125) / 19,280`. **A real 300,000 budget supports 14.** Provenance (`PLAN.md` §B.9): 32 was chosen as "comfortably inside a 300k budget" against the Phase-0 spike's 6,753 gas/seat (32 × 6,753 = 216,096, rounded up with slack) — and PLAN's own next sentence is "Both halves were wrong." **The budget was back-formed to make 32 look comfortable, then 32 was derived from the budget.** It is circular, and it was never a block-limit fraction, a router stipend, or an L2 assumption | **A BUDGET THAT IS RAISED WHENEVER IT BINDS IS NOT A BUDGET, IT IS A CHANGELOG** — 300k → 400k → 550k, while `test_5_3c`'s ceiling went 500k → 850k → 1,050k → 1,100k → 1,250k. **DELETED rather than raised a fourth time**, which is the only thing that stops the recurrence: this argument was had at 5.108 and again at 5.129 because 5.108's CONCLUSION survived while both its SUPPORTS were false. (i) `test_4_41` was a bare `assertEq(MAX_SEATS, 32)` — it could only fail if somebody edited the constant, asserted nothing about the `order` word, and was cited in three places as the structural proof (now rewritten to assert the coupling and a full-roster round-trip, with an executed negative control). (ii) "Shrinking `MAX_SEATS` buys nothing back — the word is one slot at any width" is true of the WORD and false of the COST: measured, 32 → 27 is **−96,645** on the sweep and **−620,191** on the worst-case deposit. **Replaced by: the slope pinned directly (19,280 ± 150, tolerance derived from the 1-gas spread between the suite's two estimators against the ≥2,100 one added cold slot costs), plus two ABSOLUTE ceilings measured as a fraction of a real block.** Closes 5.129 | Phase 9 | **MEASURED** — LAW 4 throughout; suite green 9/9 both φ | `Gas.t.sol` (`BUDGET` deleted, `test_5_3b`, `test_5_3d`); `Harberger.t.sol` `test_4_41` |
| 5.145 | 🚨 **"THE ROSTER IS FIXED AT DEPLOYMENT" IS NOT THE DEFENCE AGAINST ROSTER-WALK GRIEFING — HARBERGER RENT IS. THE FIXED ROSTER ONLY CAPS THE DAMAGE.** The fixed roster IS a complete defence against one thing: **seats cannot be created**, so the walk length can never grow. `q.push` occurs only in `_mintRoster` (`QueueHook.sol:590`), which is `private` and called only from the constructor (`:569`), bounded by `:581` `if (n > MAX_SEATS) revert RosterTooLarge`. Dust-seat STACKING therefore has no entry point at all and total walk cost is hard-capped at ~617k gas. **But depth WITHIN the roster is attacker-controllable**: `_fundSeat` rewinds BOTH cursors to the funded rank (`:1632-1633`) — it must, or a funded seat ahead of the cursor is skipped, which is INVARIANT C's silent theft of rank — and seats are Harberger-purchasable. So a buyer of low ranks can re-dust them before each victim swap and force a re-walk | **It fails on ECONOMICS, not on code, and that is why the attribution matters: a defence credited to the wrong mechanism is one refactor away from being removed for the wrong reason.** The attacker pays more than the victim and superlinearly — one `addToSeat` is **607,703 at n=8 and 2,667,423 at n=32 (~n^1.9)** against the victim's LINEAR ~19,280 per seat re-walked — and must first buy the seats at incumbents' self-assessed prices, then hold them under continuous self-assessed rent (price it low and the dust seats are bought away; price it high and rent bleeds). Absent re-funding the cursors amortize the prefix to a single walk (`:1401-1402`). **Net: bounded, self-financing to defend, victim exposure capped ~617k above baseline — an expensive trade, not a DoS.** Do not restate the fixed roster as the reason | Phase 9 | **REASONED** from source + **MEASURED** deposit costs; no exploit executed | `QueueHook::_mintRoster`, `_fundSeat`, `_allocate`; `Gas.t.sol` `test_5_6` |
| 5.146 | 🚨 **THE MUTATION INTERLOCK GUARDS ONE DIRECTION ONLY, AND A TEAMMATE'S MANUAL MUTANT PRODUCED A PHANTOM FAILURE THAT THE ORCHESTRATOR REPORTED AS A REAL BUG.** `Hygiene.t.sol`'s interlock refuses `forge test` while `script/mutate.py` publishes its marker (5.86), and `script/hooks/pre-commit` refuses a commit in the same window (5.121). **Neither fires for a manual on-disk mutation.** An agent hand-deleting a line to prove it is load-bearing holds a mutant for seconds at a time and publishes nothing; a concurrent full-suite run by ANOTHER agent lands inside that window and reports the mutant's signature as a defect. Executed accidentally: `test_8_14` was reported up the chain as "the empty-seat branch drops the buyer's deposit", relayed to the user as a real finding, and **it passes on the unmutated tree** — the failure message was character-identical to the agent's own mutation M-C, which is what identified it | **THE DIAGNOSTIC IS THE MESSAGE ITSELF: a failure whose text exactly matches a mutation somebody is running IS that mutation.** Fix is mechanical, not procedural — a manual mutation harness must publish the same marker `mutate.py` does and run itself under an env flag, so the existing interlock covers it. **Prose failed here for the fourth time (5.79, 5.86, 5.121, this): every previous fix built a mechanism for the path that had just burned somebody and left the sibling path open.** Corollary that saved this session: the orchestrator's commits were DOCS-ONLY by policy, so no mutant reached a commit — the docs/code split is doing real work and should not be relaxed for convenience | Phase 9 | **PROVEN** — the phantom was reported, diagnosed from the message text, and `test_8_14` verified PASS on the clean tree | `test/queue/Hygiene.t.sol`; `script/hooks/pre-commit` |
| 5.147 | ⚠️ **`git add <file>` IN A MULTI-AGENT SESSION COMMITS A TEAMMATE'S UNCOMMITTED WORK UNDER YOUR MESSAGE.** Staging by PATH takes whatever the working tree holds, not what you wrote. Commit `8eb3d44` was staged as eight files the orchestrator had authored and its message says so; it also contains **PITFALLS rows 5.144 and 5.145, written by a different agent, still in flight, and mentioned nowhere in the message.** A reader diffing that commit finds content the message does not account for — which is precisely the property `git log` is relied on for here | **This is 5.121's sibling and it survived 5.121's fix.** That hazard was "a mutant on disk gets committed" and the mechanism built for it (`pre-commit` refusing during a campaign) cannot see this one, because nothing is wrong with the content — it is simply not the committer's. **Mitigations, in order of strength: stage HUNKS not paths when a file is shared; or assign single-writer ownership of every file and never stage one you do not own; or `git diff --cached` and READ it before committing, which is the only step that catches it after the fact.** Here the swept rows were good work and the outcome was benign, which is exactly why it is worth writing down — a benign instance is the only cheap one | Phase 9 | **PROVEN** — `git show 8eb3d44 -- PITFALLS.md` contains rows the message does not mention | commit `8eb3d44` |
| 5.148 | 🚨 **A MUTANT THAT READS "0 WEI CAUGHT" MAY BE UNREACHED RATHER THAN UNDETECTED, AND THE TWO ARE INDISTINGUISHABLE FROM THE NUMBER ALONE.** Building the premium witness, two of four mutants first reported a gap of exactly `0 wei` and were nearly written up as SURVIVORS. They were not: the defects live in `_syncSeat`, `seat()` returns `raw + _claims`, and **`_claims` is not `virtual`** — so a `_syncSeat` defect is invisible until a seat with a NON-ZERO pending claim is actually settled. In a head-only run the only seat ever settled is the FRONT one, which is the payer on every fill and is therefore owed zero **whatever the weighting is**. The scenario could not express the bug. Fixed by ending the driver with `withdraw(i, 0, 0)` on every seat — settles, pays nothing, costs no rank (5.122's refined demotion rule makes that safe) — after which the same mutants read **8.60e19, 1.07e20, 2.15e20 and 2.15e20 wei** | **This is 5.54 ("before believing a path is covered, check that it was ENTERED") relocated from tests to MUTATIONS, and it is more dangerous there** — a surviving mutant is a finding, so an unreached one manufactures a false finding and sends the next person hunting a defect that is covered. **The discriminator is cheap: before recording a survivor, prove the mutated line EXECUTED and that its effect could reach an assertion.** Note also what the four gaps demonstrate: every one of these mutants leaves conservation AND INVARIANT F tying out to the wei, so nothing else in the project can see any of them — 5.92 and 5.120 again, and the reason an independent witness had to exist at all | Phase 9 | **PROVEN** — same four mutations, `0 wei` before the driver change and 8.6e19–2.15e20 after | `test/queue/PremiumWitness.t.sol`; `QueueFixture::_drive` |
| 5.149 | **A DERIVED BOUND CAN BE RIGHT ON PAPER AND INCOMPLETE IN CODE, BECAUSE THE RESIDUAL PROPAGATES.** The premium witness's bound was corrected twice. First on paper: `0 ≤ C − W` holds only if the witness COPIES the hook's `inc_j`, which is a witness measuring the contract against itself at the step Phase 8 changed; an independent witness taking the true rational share sees the hook's INNER floor too, giving `−1 − floor(k·L_i/Q) ≤ C − W ≤ k − 1`. **The `−1` is STRUCTURAL, not a knife edge** — a sole unexcluded payee makes every share an integer and the hook lands exactly one wei low, which a head-only swap on a small roster hits every accrual. Then the CODE forced a second correction paper had missed: **the one-wei residual PROPAGATES into the next fill and accumulates across settlements**, so a closed interval must be banked into a per-seat allowance and only an assert-then-resync clears it — and the resync's own extra floor widens the open interval's upper bound from `k−1` to `k` | **What makes this a good outcome rather than a widened tolerance: the author refused to widen.** `test_W0` went red at `k = 0` with a 1-wei gap and the diagnostic printed `k=0 L_i=2e20 w_last=6e20 bound=[-0,+0]` — a zero-width bound, which is what sent them to the propagation rather than to the tolerance. **That is what the "print k, L_i, w and Q on failure" requirement was for, and it paid for itself the first time it fired.** At φ = 0 the allowance is provably `[0,0]`, no resync runs, and all 205 pre-Phase-7 assertions keep their literal `assertEq`s — so the slack exists only where the mechanism does | Phase 9 | **PROVEN** — derivation, then the code's counterexample, then 4 mutants RED outside the corrected bound | `QueueFixture::_refBound`, `_refResyncPremium` |
| 5.150 | 🚨 **`ps` IS THE WRONG CHECK FOR "IS A MUTATION RUNNING", AND TWO AGENTS INDEPENDENTLY GOT IT WRONG IN THE SAME HOUR — ONE OF THEM WHILE QUOTING THE RULE.** A MANUAL mutation harness is a shell loop, not a `mutate.py` process, so **there is nothing for `ps` to find** — `ps aux \| grep mutate.py` returns clean while a mutant sits on disk. Worse, `ps aux \| grep -c "[m]utate.py"` returned **1** and looked like a live campaign: the bracket trick does not survive `zsh -c eval`, so the grep counted its own command line and nearly produced a PHANTOM CAMPAIGN in the other direction. **THE CHECK IS THE MARKER FILE: `ls .forge-snapshots/MUTATION_IN_PROGRESS`.** For a real `mutate.py` run, `pgrep -fl mutate` and its exit code — `pgrep` cannot self-match | **This is the fourth distinct way this project has been bitten by "measure a tree somebody else is writing to" (5.79, 5.86, 5.121, 5.146), and the first where the DEFENCE was wrong rather than absent.** An agent who read 5.79/5.86 at session start, believed they were complying, and ran `ps` all session, was not complying. **A rule that names a hazard without naming the exact command is a rule people will satisfy incorrectly.** Corollary from the same hour: a dead harness leaves the marker AND the mutant, so a held marker with no live process is not proof the campaign is running — check whether the harness is still cycling (file mtime) before deleting a marker, and `git diff src/` before believing anything after you do | Phase 9 | **PROVEN** — both mistakes made and caught, and the self-matching `grep -c` reproduced | `test/queue/Hygiene.t.sol`; `AGENTS.md` §3b |
| 5.151 | 🚨 **THE SAME DEFECT LIVED IN THE SIBLING BRANCH OF THE ONE THAT WAS JUST FIXED, AND NOBODY LOOKED — THE EIGHTH INSTANCE OF ONE-RULE-TWO-BRANCHES.** The INVARIANT L fix (5.1xx) added the missing mirror to `_onSeatTransfer`'s main path. **Its pure-rank EARLY RETURN — `if (a0 == 0 && a1 == 0 && esc == 0)` — never zeroed `s.liquidity` AT ALL**, so a seat holding contributed depth with no balances carried the departing holder's depth, and their share of `standingL`, to the new holder for free. Reachable exactly the way the original was: a withdrawal the float covered burns nothing, leaving contributed liquidity against zero balances. Fixed with the same argument — `if (had != 0) { s.liquidity = 0; standingL -= had; liquidityUnattributed += had; }` — guarded so the genuine pure-rank path still costs nothing | **The orchestrator routed the defect, the fix, and the proven subclass control, and STILL only the branch that was named got looked at.** The instruction was "apply this line"; the correct instruction was "this function has two exits, check both". **When a defect is found in one branch of a function, the deliverable is not the fix — it is an enumeration of that function's other exits with a verdict on each.** Prior instances: 5.37, 5.50, 5.52 (twice), 5.73, 5.125, 5.132 | Phase 9 | **PROVEN** — `test_8_16`, INVARIANT L asserted on both sides | `QueueHook::_onSeatTransfer` |
| 5.152 | ✅ **CLOSED IN PHASE 10 — see 5.170. THE PREMIUM ERASURE IS REAL, EXECUTED IN BOTH DIRECTIONS, AND ATTACKER-CHOOSEABLE.** `_settlePremium` writes `snap := g` for every rank in `[start, last]` where `last = min(next, n-1)`. When a fill EXACTLY exhausts the last seat it reached, `next = m+1` and rank `m+1` was **never visited by the walk** — so its mark advances having never been settled, and its claim on every EARLIER accrual stops being claimable by anyone while staying counted in `premiumOwed`. Measured at 18/6, φ=8500: rank 1 held **71,678,027,990,408,984,124 wei** of token0 pending (and **310,779,106** in the mirror), and after the fill held **0 pending and 0 credited** — neither paid nor kept. **Every conservation assertion in the project is blind to it**, which is why it survived: the wei are still in `premiumOwed`, so INVARIANT F ties out exactly | **PITFALLS 5.124's family: conserved by F, claimable by nobody, forever.** The docblock called this *"deferred, never destroyed"* — **deferral is what a HELD pot does, and nothing here is held.** Exclusion from THIS pot is harmless; moving the MARK is not, because the mark carries the claim on every earlier accrual. **Attacker-chooseable: the boundary needs an exactly-exhausting fill, the swap size is the swapper's, and every router exposes exact-output — so it is a decision, not a coincidence.** A fuzzer will never find it; reaching it required an `_swapExactOut` helper. Griefing rather than extraction — the wei are stranded, nobody gains — and the magnitude is one seat's entire accrued interval, unbounded in principle. **Proposed fix (NOT applied): `_syncSeat(seat_)` inside `_settlePremium`'s existing payer loop, BEFORE `_accruePremium`** — a no-op for every rank the walk already synced, and it cashes the one-past seat's earlier claim before the mark moves. A subclass control could not be written because **`_settlePremium` is not `virtual`** while `_allocate`, `_accruePremium`, `_syncSeat` and `_u128` all are; marking it `virtual` makes the control writable | Phase 9 | **PROVEN** — `PremiumWitness.t.sol` `test_W6`/`test_W7`, both directions, with four non-vacuity guards so a vacuous pass is unavailable | `QueueHook::_settlePremium` :1494-1522 |
| 5.153 | **AN AGENT EXPLAINED AWAY A FAILING TEST WITH A COMFORTABLE STORY, AND THE STORY WOULD HAVE DELETED A NEGATIVE CONTROL SILENTLY.** `test_M1c` failed with `vm.getCode: no matching artifact found` and was reported as *"a stale-artifact hiccup from my own file swap; `forge build --force` would clear it"*. **It was not the cache. The CONTRACT was missing:** a block-replacement while inverting another test had a slice boundary running to `contract MaturityTest` and silently swallowed `FaceValueQueueHook`, the dust-policy control for two other tests. `ls out/Maturity.t.sol/` showed two subclasses where there should have been three | **Caught only because the tree stayed broken long enough to look twice** — otherwise a 35/36 would have been reported with a false reason attached and a negative control deleted from the file. **A MISSING CONTROL DOES NOT FAIL LOUDLY; it just stops being able to catch anything**, which is M59's precedent and 5.148's shape. The rule the agent drew themselves is the right one: **do not offer a diagnosis you have not reproduced, and be most suspicious of the diagnosis that costs you nothing** ("it's the cache, not me"). Recorded with attribution because they caught and reported it against their own earlier message | Phase 9 | **PROVEN** — the missing artifact located, the contract restored, suite 35/36 → 36/36 | `test/queue/Maturity.t.sol` |
| 5.154 | ✅ **CLOSED IN PHASE 10 — the contradiction was RESOLVED AGAINST 5.130's refinement; `withdraw` now demotes on ANY payout. See 5.171. A FOURTH EVACUATION DOOR, EXECUTED — AND IT PUTS TWO SHIPPED RULES IN DIRECT CONTRADICTION, SO IT CANNOT BE PATCHED WITHOUT A DECISION.** 5.122's remedy makes `withdraw` demote **only when it PAID something**, and what it pays is conditional on a BURN — but a payout the FLOAT can cover burns nothing. **So pre-fund the float and a holder withdraws their ENTIRE balance, burns zero, and keeps their rank.** Measured: `500e18 + 125e18` out, **zero burn, rank 1 retained** (`test_8_15`). And the float is nearly free to build — a single-token in-range deposit mints ZERO liquidity, so it lands in the float whole while the depositing seat is credited every wei. It needs a second seat, which a sybil has | **THE COLLISION IS THE FINDING, not the door.** 5.130 exists because "any payout demotes" cost an honest LP their rank for taking profit, and `test_8_9` encodes the refined rule — *taking earnings does not cost a rank, taking DEPTH does* — by defining "earnings" as a withdrawal the float can cover. **Pre-funding the float makes EVERYTHING look like earnings.** So `test_8_9` and `test_8_15` cannot both be satisfied by any rule phrased in terms of what was burned: the same observable means "profit" to one and "evacuation" to the other. **Choosing between them changes who keeps rank, which is an owner decision, so it was recorded and deliberately NOT patched.** **THE OBVIOUS RESOLUTION IS A DEAD END, PROPOSED AND REFUTED BEFORE IT SHIPPED:** "ask whether `liquidityContributed` FELL rather than what was burned" is the SAME QUESTION — `_chargeBurn` opens `if (burned == 0) return false;` then debits `min(burned, s.liquidity)`, so the depth falls **iff** something burned, and the float defeats both phrasings identically. `_settleRankOnTransfer` only looks like a counterexample because a TRANSFER zeroes `s.liquidity` unconditionally — which is exactly why door (a) closed and this one did not. **The only formulation found that separates the cases, UNVERIFIED, is whether the seat's REMAINING LEDGER can still back the depth it is credited with (`s.a0`/`s.a1` after the payout against `s.liquidity`) — and it carries an objection that must be tested FIRST: front-first DELIBERATELY drives a seat to single-token composition and `_liquidityForAmounts` takes the min of the legs, so a fully-FILLED front seat reads zero backing and would be demoted for doing its job. That is 5.126/5.127's units trap, one dimension over** | OPEN | **PROVEN** — executed, `test_8_15`; the contradiction with `test_8_9` verified in both directions | `QueueHook::withdraw`; `Evacuation.t.sol` `test_8_9`, `test_8_15` |
| 5.155 | **TWO MUTATION SURVIVORS IN ONE BATCH, BOTH REAL GAPS, AND ONE OF THEM WAS THE EXACT SIBLING-BRANCH HOLE THE ORCHESTRATOR HAD JUST ROUTED.** Door (a)'s seven mutations: deleting the demotion rule → RED in **11 tests across 5 suites**; deleting the funding call → RED in 7; never setting the funding side channel → RED in 9. **But `<` → `<=` (an exact-depth match demoted) SURVIVED, and deleting the rule on the pure-rank early return SURVIVED.** Both were closed with tests written FOR them (`test_8_12b`, `test_8_14`) and are now RED | **The survivor count is the signal, not the RED count.** Seven cases, five caught loudly, two silent — and the two silent ones were a boundary condition and a second exit of the same function, which is precisely the pair a human reviewer reads past. Note also the correct reading of 5.130 that came with it: a mutation caught by exactly ONE test is only a warning when that catch is INCIDENTAL. Here two cases were caught by one test each (`test_4_14b`, `test_8_13`) and both were **directed** — written for that specific defect — which is the opposite situation and is fine. **"How many tests caught it" is uninterpretable without "were they aimed at it"** | Phase 9 | **PROVEN** — 7 cases run, 2 survivors observed, both gaps closed and re-run RED | `Evacuation.t.sol` `test_8_12b`, `test_8_14` |
| 5.156 | 🛑 **SETTLED: `B₁ ≥ LP` BY CONSTRUCTION, SO `SLACK = c₁·(LP − B₁) ≤ 0` BEFORE ANY MEASUREMENT — THE MECHANISM CANNOT CREATE SURPLUS FOR ITS PARTICIPANTS, IN ANY CONFIGURATION.** `wing.check_null` (`wing.py:151-161`) proves a **STATIC** position spanning exactly the pool's own band with capital `cap` **IS** `cap/BOOK` of the undivided pool: BENIGN `+6.035%` vs `+6.035%`, NORMAL `+0.767%` vs `+0.767%`, TOXIC `−2.181%` vs `−2.181%`, paired differences `−0.000%` at SE `0.000`, max absolute deviation **4.3e-14**. **So rank 1 can always, for free, hold a plain never-touched Uniswap position returning EXACTLY the pro-rata LP return.** Sweeping static widths, the best free alternative converges to `LP` **from ABOVE** (`+6.415%` at w = 8% BENIGN; `−0.732%` at w = 30% TOXIC, t = +67) — though the ex-post argmax over 13 widths inflates those t-stats (5.34), so `B₁ > LP` is NOT established in BENIGN/NORMAL at n = 30. **It does not matter: `B₁ = LP` gives slack exactly `0.000` and `B₁ > LP` gives negative. No reading yields positive** | 🚨 **AND THE `B₁ = LP → slack = 0` CASE WAS NEVER A SANITY CONTROL. IT WAS THE ANSWER, AND IT WAS READ AS A CHECKMARK.** It was built to prove the machinery could reproduce a known value, it returned exactly `0.0000 pp` in all three regimes, and its significance was walked past — three more hours were then spent hunting a `B₁` below `LP` that cannot exist. **That is LAW 5 inverted: the discipline says build a case whose answer is known in advance; the failure mode nobody had written down is BUILDING ONE AND THEN NOT LISTENING TO IT.** The root error was treating `B₁` as "the best keeper bot" — a strategy — rather than **"the best of everything rank 1 may freely do", a set which contains ordinary passive LPing.** A bar must be the supremum of the alternative SET, never a nominated competitor; nominating one is how 5.136 and 5.140 both happened, and this is the third and final instance | Phase 9 | **PROVEN** — an identity, verified numerically at 4.3e-14; the width sweep is MEASURED with its own limitation stated | `docs/research/seat-economics/FRONTIER.md`; `wing.py:151-161` |
| 5.157 | 🚨 **THE PUBLISHED "+0.19 pp SURPLUS" WAS THE ONLY POSITIVE NUMBER IN A 3×7×5 GRID, AND THE SIGN DOES NOT EVEN DEPEND ON THE ARGUMENT WE WERE HAVING.** Sweeping 7 widths × {static, managed × 4 conversion venues} × 3 regimes: `LP − B₁` is **NEGATIVE in all three** — BENIGN `−1.197%` (SE 0.321, t = −3.73), NORMAL `−0.186%`, TOXIC `−1.096%` (t = −69.1), giving `SLACK = −0.399% / −0.062% / −0.365%` of book. **Exactly one cell of the 105 is positive: own-pool conversion, BENIGN, managed-only, w = 10%.** That is the cell the headline came from | **AND THE RETRACTION DOES NOT NEED THE CONVERSION-VENUE ARGUMENT AT ALL, WHICH IS THE SAFER FORM.** STATIC wings pay NO conversion, so no venue assumption can touch them — and in BENIGN a static w = 8% returns **+6.415% against LP's +6.035%**, above the bar under *every* assumption including own-pool. **Rest the retraction on the `check_null` identity, not on a modelling opinion.** Separately, the two assumptions holding the positive cell up are MUTUALLY EXCLUSIVE: the whole result is generated by `retail_per_hr = 15` ≈ 131k trades/year, and **any pair with that flow has other venues, while any pool where own-pool conversion binds has no flow to sell.** Also settled: "80–123%/yr" is NOT RECOVERABLE — all 18 published tranche cells and all 12 measured shipped-head cells were annualised and neither number, nor any pair bracketing them, appears in either set | Phase 9 | **MEASURED** — 105-cell grid, n = 30/regime, LAW 5 control passing in all three at max \|diff\| 5.6e-05 / 5.4e-05 / 4.3e-14 | `wing.py:151-161`; `docs/research/seat-economics/FRONTIER.md` |
| 5.158 | 🚨 **φ = 7,900 IS NOT MERELY UNFOUNDED, IT IS ABOVE THE FRONT'S BOUND UNDER THE RULE THE CONTRACT ACTUALLY RUNS — AND THE ORCHESTRATOR'S DIAGNOSIS OF WHY HAD THE BOUNDARY SEAT'S IDENTITY BACKWARDS.** Swept on `PREM_WEIGHT = 'liquidity_excl'`, the rule that ships: at φ = 8000, BENIGN, **the head returns −2.95% against a managed-wing bar of +1.94%**, and the front constraint binds at **φ ≤ 5610** (NORMAL ≤ 6199). **The window to check is [4725, 5610], not the published [7455, 8312]** — which was solved on `'inventory'`, a rule where the head kept a third of its own premium (5.142). The shipped rule excludes the head on **99.3% / 98.8% / 96.0%** of swaps and hands its whole weight to the back, which is why the back looks excellent (s2 at 8.14%) and **the head has no product** | **TWO DIAGNOSTIC ERRORS BY THE ORCHESTRATOR, BOTH REFUTED BY MEASUREMENT.** (i) *"The boundary seat is seat 2, so the exclusion is over-charging it"* — **the boundary is RANK 0 on ~99% of swaps.** So pro-rating it is overwhelmingly a REBATE TO THE HEAD paid for by the back: BENIGN's back-constraint moves 4725 → 6865 under the proposal. (ii) *"Seat 2's TOXIC failure is caused by the premium's weighting"* — **it is a MARKOUT deficit, not a premium one.** Seat 2's whole TOXIC curve moves 0.09 pp across φ 0→8000 against a 0.3 pp gap, under all four rules, because a 1.4-day band with ~439 swaps has almost no fee flow to redistribute. **A distribution rule was blamed for a P&L gap; no φ and no weighting can close it.** TOXIC opens under NONE of the four rules tested | Phase 9 | **MEASURED** — 4-rule grid; the 120-path control reproduced `results-shipping.txt` cell-for-cell, all three regimes, which is what proved the published grid ran the pre-Phase-8 rule | `docs/research/seat-economics/` scratch grid; `sim.py:66` |
| 5.159 | 🚨 **THE GO/NO-GO WAS BEING READ OFF SAMPLING ERROR — THE CEILING IS HALF THE STANDARD ERROR OF ONE OF ITS OWN INPUTS.** `SLACK = c₁(LP − B₁)` is a difference of two ~+5% numbers. **The BENIGN `LP` bar has a standard error of 0.573 pp over 120 paths, and the entire ceiling being argued about is 0.2875 pp.** A 40-path subsample moved `LP` by 0.565 pp — three times the whole ceiling. **So the SIGN of the slack is not resolvable at 120 paths: both the published "+0.19 pp feasible" and the corrected "−0.39 pp empty" sit inside the noise.** (TOXIC's SE is 0.022 pp and is fine; NORMAL at 0.271 pp is marginal.) An entire session of φ windows, crossovers and per-seat rankings was computed on a grid that cannot resolve its own headline | **THE CONCLUSION SURVIVES AND THE MEASUREMENT DOES NOT, AND THE DISTINCTION IS THE WHOLE POINT.** `SLACK ≤ 0` rests on an **IDENTITY** — `wing.check_null` at 4.3e-14 shows a static position over the band returns exactly `LP`, so `B₁ ≥ LP` by construction (5.156). That is exact and noise-free. **`SLACK < 0` STRICTLY is NOT established, and must not be claimed.** So: *no positive surplus* is proven; *how negative* is unknown and unresolvable at this path count. **The rule: when a quantity is a small difference of two large noisy numbers, the derivation is the evidence and the simulation is decoration — and if the conclusion needs the simulation's sign, the study is not powered to make it.** Anyone re-opening this must first size the path count to resolve `LP − B₁`, or accept that the answer comes from the identity alone | Phase 9 | **MEASURED** — SEs computed on the same 120-path grid that produced every published crossover | `docs/research/seat-economics/` |
| 5.160 | ⚠️ **AMENDED IN PHASE 11 — SEE 5.177. THE CONCLUSION STANDS; THREE OF THE NUMBERS BELOW DO NOT REPRODUCE.** The cited file was the EMPTY git blob until Phase 11 regenerated it. On the regenerated file: the LP bar `+135.9 bps (SE 12.9)` appears nowhere (reproducible value **+50.2 bps, SE 5.7**), the rows below MIX φ blocks, and `−476 bps` is not reproducible (**391.9 bps** at φ=0 computed consistently). The figure for what we SHIP is **132.7 bps**, not 476 — 476 was the two-ended BOOK we do not ship. The falsification control reproduces EXACTLY (+4.487pp / −1.277pp, t = +61.96 / −20.10) and is what the conclusion rests on. **Also see 5.178: this file was generated on a premium basis the contract replaced in Phase 8.** ORIGINAL ENTRY: SETTLED, AND IT CLOSES THE LAST DOOR 5.156 LEFT OPEN: PRIORITY IN AN AMM IS WORTH NEGATIVE MONEY TO THE HOLDER.** 5.156 rests on an OUTSIDE-OPTION argument (`B₁ ≥ LP`, so no unconstrained party will fund rank 1) and explicitly leaves open the one buyer it cannot price — a party whose mandate forbids passive LPing and whose alternative is paying taker cost. **That party is now measured, and it is worse off too.** `results-book.txt` §5, TOXIC-DN — the ONLY regime in which a buyer's mandate is actually fillable (`conv>0 = 1.00`; in BENIGN/NORMAL only ~half the paths convert in the wanted direction and the statistic is undefined): a pro-rata LP buys at **1906.99 = +135.9 bps vs a TWAP taker** (SE 12.9), the SHIPPED rank 0 at **1932.41 = −108.2 bps** (SE 16.5), a two-ended-book rank 0 at **1981.82 = −340.1 bps** (SE 9.9). **Monotone in how much priority you hold.** A plain range order ALREADY beats the taker; adding priority costs about **−476 bps** against the pro-rata position it displaces | **THE REASON IS PHYSICAL, NOT PARAMETRIC, WHICH IS WHY NO CONFIGURATION RESCUES IT.** Pro-rata spreads a fill across the whole move — **a pro-rata range order IS a TWAP**. Front-of-queue concentrates the fill at the FIRST prices of a move, which for a buyer in a down-move are the HIGHEST. **BEING FIRST IN AN AMM QUEUE IS ABSORBING THE ADVERSE SELECTION FIRST.** An end-heavy capital schedule makes it WORSE, not better, because it puts rank 0 further forward. **THE CONTROL THAT SETTLES IT IS ONE THAT COULD FALSIFY THE HEADLINE** (LAW 5, first corollary): a monotone accumulator is long delta, so a genuine mechanism gain must SURVIVE a mirrored drift while a direction bet must FLIP. Paired path-by-path: TOXIC `+4.487 pp` (t = +61.96), TOXIC-DN `−1.277 pp` (t = −20.10). **It flips.** Note the shape of the whole three-session search: the value was hunted in the RETURN column, and the answer was in the EXECUTION-PRICE column all along | Phase 10 | **MEASURED** — 120 paths, 4 regimes × 2 φ, paired differences with SEs; integrity controls in `results-book.txt` §0–§1 (transcription bit-identical 48/48, N=1 known-answer 1.8e-14, `tie_out` on all 1,920 runs) | `docs/research/seat-economics/results-book.txt`; `book_sim.py` |
| 5.161 | **RANK IS DISTANCE FROM SPOT, WHICH UNISWAP ALREADY SELLS FOR FREE.** Front of queue ≈ liquidity nearest spot: fills first and most, worst prices, maximum adverse selection. Back of queue ≈ liquidity far from spot: fills rarely, best prices, minimum adverse selection. The tick structure already offers exactly that choice, at no gas and with no hook. **QUEUE re-implements it inside one position at +119% gas, plus a rank auction, rent and a premium** | The ONE axis on which rank is genuinely new is that it sorts by **trade SIZE** rather than by price — a seat deep in the book is reached only by large swaps. **And trade size is forgeable by SPLITTING A QUANTITY, which is `AGENTS.md` §6's proven-impossible rule wearing a new hat.** So the only novel axis is the one the project already forbade itself from using. Anyone re-opening the value question must first say which of these two statements they think is false | Phase 10 | **ANALYSIS** — consistent with the measured monotonicity in 5.160 and with `results-exec.txt` §C, not separately executed | `docs/research/seat-economics/results-book.txt` §5 |
| 5.162 | 🚨 **THE PROJECT MEASURED THE THING THAT KILLS ITS OWN PREMISE AND PRINTED IT AS A FEATURE, FOR THREE SESSIONS.** `results-exec.txt` §C: rank 1 `recycle = 1.00` in BENIGN ±10/±30 and NORMAL ±10/±30 — the file's own legend reads *"1.0 = it gave back everything it was handed"*. `BUSINESS.md` §0.1 renders the identical fact as a selling point: *"inventory recycles 122–543× over the band's life."* Read alongside `turnover` and `startshare`, the three rows say: **the ranks that FILL (rank 1, ~96% of fills) convert NOTHING, and the ranks that would ACCUMULATE one-way are NEVER FILLED (`startshare` 0.0%). There is no rank that both fills and accumulates** | **When a mechanism has no buyer, ask whether it DELIVERS anything before asking what it is worth.** Three sessions searched the return column for a value proposition while the conversion column had already answered. **A quantity that has been measured, published and re-quoted is not thereby understood** — and a number framed as a feature is the last place anyone looks for a refutation. Cheapest available check, and it costs one minute: for every headline number in a results file, read the legend and ask what the number would be if the mechanism did nothing | Phase 10 | **MEASURED** — the project's own published table, re-read | `docs/research/seat-economics/results-exec.txt` §C; `BUSINESS.md` §0.1 |
| 5.163 | 🚨 **A THIRD CONFIGURATION WAS IN PLAY AND IT NEARLY PRODUCED A FIFTH RETRACTION OF THE SAME SHAPE.** `results-exec.txt` §I is headed *"THE SHIPPED ROSTER"* and is **5 EQUAL seats of $200,000** (c₁ = 0.20). `QueueDeployBase` deploys `mul = SEATS - i`, i.e. **5:4:3:2:1 with a $333,333 head** (c₁ = 0.333). The LP bar in that same file is **+4.79 / +3.36 / +5.73 / +6.22%** across four seed ranges, NOT the **+6.035%** quoted from FRONTIER's separate run. Comparing §I's per-seat returns against +6.035% — which this session was one step from publishing — is exactly 5.140–5.142 | **A HEADING THAT SAYS "SHIPPED" IS NOT A CONFIGURATION CHECK.** Read the roster line, every time. **And prefer a CONFIG-FREE form wherever one exists:** the subsidy needs no simulation at all, since `Σ cᵢrᵢ = LP` gives the back's excess over LP as `c₁(LP − r₁)/(1 − c₁)` — at c₁ = 1/3, **1 pp given up by the front buys the back exactly 0.5 pp**, a 100%-efficient transfer with zero leakage and zero creation. A closed form cannot be quoted against the wrong fixture | Phase 10 | **PROVEN** — the roster line and the LP bars read from the file; the identity is algebra | `results-exec.txt` §I; `script/QueueDeployBase.sol:181` |
| 5.164 | ✅ **CLOSED IN PHASE 11 — see 5.175, which supplies the invariant AND corrects the safety argument recorded below (it quotes BALANCE weighting; `_claims` weights by `s.liquidity`, so the stated reason is dead and the real one is `min(cursor0, cursor1) == 0`).** A SECOND WRITER OF SEAT INVENTORY THAT TOUCHES NO CURSOR — SAFE ONLY BY AN ACCIDENT OF THE WALK ORDER.** `_accruePremium` writes seat inventory and updates no cursor. Under the shipped one-ended rule that is harmless BY CONSTRUCTION: a fill sets `c0 = min(c0, old c1)`, and every seat below the old `c1` holds zero of the outgoing token, so it is weighted zero in the accrual and can never be credited outside `[c0, n)`. **The safety is a property of both directions walking from the same end, NOT of the code.** Modelled with a reversed walk, a seat above the descending cursor could hold the incoming token, collect premium, and then sit outside the reachable window permanently — inventory the allocator cannot reach and `avail` cannot see. Measured in the simulator before repair: balances moved by up to **~0.5% of a $1,000,000 book at φ=8500**, clean at φ=0 (no premium ⇒ no second writer) | **THIS IS A LIVE BUG CLASS IN THE HOOK, NOT A SIMULATOR ARTEFACT — the same two writers exist there.** Any future change to the walk order, the cursor rule, or the set of functions that write `a0`/`a1` re-opens it. **Write down the invariant that is currently free: every site that credits a seat must leave that seat inside the window some cursor can still reach.** `test_7_5` already pins the count of balance writers at six; this is the property those six have to share, and nothing asserts it | Phase 10 | **MEASURED** (in simulation) / **REASONED** (in the hook) — found by a cursors-ignored control that disagreed 48/48 cells before repair | `docs/research/seat-economics/results-book.txt` §0b–0c |
| 5.165 | **THE ALLOCATOR'S CURSOR CARRIES NO ECONOMICS; IT IS PURE GAS.** Six single-line mutations of the walk, 12 cells each: dropping `(1-FEE)` from the `mk_in` leg and replacing the remainder line with average pricing were caught 12/12; not taking the premium off the top 6/12 (the other 6 are φ=0 cells). **Three SURVIVED 0/12 — start at 0 instead of the cursor, never advance the cursor, ignore the cursor in `avail` — and they are ONE finding rather than three gaps:** the walk skips empty seats with `continue`, so a correct cursor changes no number, only gas | Useful when reasoning about cursor bugs, and it sharpens the existing rule: **a LEADING cursor is a correctness bug (it SKIPS a funded seat, which is silent theft of rank); a LAGGING cursor is only a gas cost; and NO cursor at all is economically identical to a correct one.** It also means a cursor mutation surviving a correctness suite is expected and is not, on its own, a coverage gap — which is exactly the reading that would otherwise waste a session | Phase 10 | **MEASURED** — 6 mutations × 12 cells, run against unmutated `sim.py` | `docs/research/seat-economics/results-book.txt` §0d |
| 5.166 | **AN AGENT STASHING FOR A CLEAN BASELINE LOOKS EXACTLY LIKE DESTROYED WORK.** Mid-session `git status` showed the entire working tree — a `src/` fix and ~900 lines of test repair — GONE, with `git log` unchanged. Nothing was lost: an agent had run `git stash` to measure the OLD gas constant against a clean tree (the correct way to re-derive a pinned number) and popped it back moments later. `git stash list` showed the entry on one call and was empty on the next | **Two real hazards, neither hypothetical. (1) A crash between stash and pop leaves the work in a stash nobody is looking for** — `git status` clean, `git log` clean, and the next agent starts from a tree that silently lost a session. 5.79's rule has a sibling: **never leave uncommitted work in a stash across a step that can fail.** **(2) An orchestrator polling `git status` will mis-diagnose it** — the one-line diagnostic is `git stash list`, BEFORE concluding anything was destroyed. **Remedy: an agent needing a clean baseline should use a git WORKTREE or `git show HEAD:path`, never a stash** — a stash is a destructive global operation being used as a local read | Phase 10 | **PROVEN** — observed, diagnosed and recovered in-session | — |
| 5.167 | 🚨 **A NEGATIVE CONTROL STOPPED BEING ABLE TO FAIL BECAUSE THE FIX MADE THE ATTACK IMPOSSIBLE — AND THE TEMPTING REPAIR IS TO RELAX IT.** Re-weighting rent by contributed depth (5.168) killed the flash-loan rent grab outright: `test_4_14`'s rig borrowed `(100_000e18, 0)`, and an in-range SINGLE-TOKEN deposit mints essentially no liquidity, so it now moves no weight at all. **The grab returned EXACTLY the honest share — 51369863013698630 both ways** — and the control's `assertGt(stolen, honest * 10)` failed | **A CONTROL THAT CAN NO LONGER FAIL IS NOT A CONTROL** (LAW 5), and "the attack got weaker" is not a licence to weaken the assertion — that converts a security result into a silent loss of coverage. It was **RE-ARMED against the attack that still exists** (a TWO-token borrowed deposit, which mints real depth) rather than relaxed to fit the one that died, and `_settleAhead` is still load-bearing: the mutant pays >10×. **The general rule: when a fix makes a control vacuous, the control must be re-pointed at the residual attack surface, and the fact that it went vacuous is itself a finding worth recording** — here, that the cheapest form of the grab is now structurally impossible | Phase 10 | **PROVEN** — both numbers measured, control re-armed and RED against the mutant | `test/queue/Harberger.t.sol` `test_4_14`, `_rentGrabAttempt` |
| 5.168 | ✅ **CLOSED (was 5.154's sibling): RENT WAS WEIGHTED BY THE ONE QUANTITY FRONT-FIRST ALLOCATION IS DESIGNED TO DESTROY.** `_distributeRent` split the pot pro-rata by each recipient's `currency0` BALANCE. Above the band every seat has been converted out of currency0, so the weights collapse to dust and **a seat holding ONE WEI took the ENTIRE pot** — executed as an identity, not a bound. Below the band the identical code paid out perfectly, because there every seat is 100% currency0. Both loops now read `liquidity` | **`fundRent`'s own docblock already argued against `a0` for the rent SOURCE; nobody had applied the argument to the SINK.** That is the one-rule-two-places family in its subtlest form — not two copies of a rule, but a rule and its own stated rationale living apart. **A `w == 1` special case was considered and REJECTED: it patches a symptom, no principled threshold exists, and an attacker sits one wei above it.** `test_M9b` INVERTED rather than deleted. Note the correct identity to assert is the SUM, not a per-seat floor — `Allocation`'s remainder line leaves a residue (2 wei measured), so a per-seat `floor(pot·Lᵢ/W)` assertion is WRONG by exactly that residue and widening it would be a tolerance chosen to pass | Phase 10 | **PROVEN** — fixed; both copies of the rule mutated SEPARATELY (7 RED / 21 RED); new `RentDecimals.t.sol` runs 18/6 AND 6/18 | `QueueHook::_distributeRent`; `test/queue/RentDecimals.t.sol` |
| 5.169 | **LAW 1's "BOTH DIRECTIONS" NEEDS RESTATING FOR A SINGLE-CURRENCY FLOW, AND THE UNTESTED DIRECTION IS THE POOL, NOT THE BRANCH.** Rent is currency0-only and has NO mirrored branch, so there is no second branch to test. Every existing rent suite runs `dec0 = 18, dec1 = 6`, which means the rent pot is ALWAYS the 18-decimal token — always a large integer. **The untested configuration is the pool where currency0 is the SMALL-decimal token:** the pot is then a small integer and a floored depth split can round a real recipient to ZERO and strand the money. `RentDecimals.t.sol` runs both and each catches both mutations with distinct messages; the 6-decimal case fails on *"a recipient with real depth was floored to ZERO"* where the 18-decimal case fails on the proportionality assertion | **For a single-currency flow, "both directions" means both DECIMAL ASSIGNMENTS of that currency, not both branches of a mirrored rule.** Also worth recording as a NON-finding, because it was the predicted hazard and it was wrong: the re-weighting was expected to introduce a 5.124-shaped cross-dimension compare, and **it does not** — `Allocation.init(pot, w)` then `mulDiv(pot, take, w)` is tokens × (L/L), the `L`s cancel, and nothing compares a token pot against a liquidity weight. **Checked in the source rather than assumed, and the test was written anyway**, because the argument that equal decimals were adequate has been wrong on this project before and cost a shipped feature | Phase 10 | **PROVEN** — both configurations executed, both mutations RED in both | `test/queue/RentDecimals.t.sol` |
| 5.170 | ✅ **THE BRICKED POOL AND THE PREMIUM ERASURE WERE ONE BUG, AND THE ROOT WAS A CURSOR STANDING IN FOR A RANGE.** `_settlePremium` received `next` — a CURSOR, equal to `lastTouched + 1` when the final seat was exactly exhausted — and used it as the far end of the payer set. So it advanced the premium mark of a seat the walk **never visited**, destroying that seat's claim on every EARLIER accrual (5.152, 7.17e19 wei, both directions). Separately, when excluding the payers left `standingL − lTouched == 0` the fill had reached every standing seat and `_accruePremium` took its HOLD branch — **and a held pot is money the position holds that `standing0/1` does not count**, so accumulating it leaves the ledger unable to source the next fill and `_afterSwap` reverts `QueueUnderflow`: a bricked pool, not a lost wei | **THE FIX FOR BOTH IS ONE IDEA: "the payer does not pay itself" HAS NO MEANING ON A FILL THAT SWEPT THE WHOLE BOOK**, because there is no seat that did not pay. That branch now distributes over the full `standingL` and moves NO marks — full denominator with unmoved marks is the only self-consistent pairing, since moving one without the other strands the money in either direction (`test_M7d`'s lesson). It cannot be gamed into `test_7_14`: that extraction needed exactly ONE unexcluded seat, this fires at ZERO, and the conditions are disjoint. `standingL == 0` still holds the pot, correctly — there is genuinely nobody to pay. **`_allocate` now hoists its loop variable to recover the last rank TOUCHED with no new local** (the function is at the stack limit without `via_ir`). **ELEVEN TESTS ASSERTED THE DEFECT AS THE SPECIFICATION and were INVERTED, not deleted**; the headline is `test_M7f`, which asserted *"after the exit every swap that could reach the pot reverts"* and now asserts the pool still swaps | Phase 10 | **PROVEN** — 308/0/1 at the time of fix; four hand-mutations all RED, zero survivors (early return 13, inverted condition 19, `lTouched` not 0 → 10, cursor restored → 80) | `QueueHook::_settlePremium`, `::_allocate`; `Maturity.t.sol` `test_M7f` |
| 5.171 | ✅ **RESOLVED: THE `test_8_9` / `test_8_15` CONTRADICTION, DECIDED AGAINST THE REFINEMENT.** `withdraw` now demotes on **any payout at all**. 5.130's refined rule — demote only when the withdrawal burned into contributed depth — cannot be repaired, and the reason generalises: **"did `s.liquidity` fall?" is the SAME question as "was anything burned?"** (`_chargeBurn` returns early on `burned == 0`, then debits `min(burned, s.liquidity)`), and **every remaining formulation compares the seat's remaining ledger against the depth it is credited with, which needs a price-dependent valuation of principal — under which an honest front seat is demoted for its MARKOUT LOSSES.** That is 5.130 one level deeper, not a fix for it; the single-token reading of a fully-FILLED front seat is one symptom of it, not the whole | **The blanket rule stands on the merits rather than as a retreat: the product sells SUBORDINATION, and a holder cannot be subordinate and liquid at the same time.** It cannot be griefed (only the seat holder may call `withdraw`); a holder wanting both leaves earnings in the seat, where they go on earning, or re-buys a rank at the posted price. **Both tests INVERTED with scenarios unchanged, because the manoeuvre is the evidence** — and `test_8_15` deliberately KEEPS its "the bypass engaged" assertion (zero depth burned) so it still proves any burn-based rule would be blind there. **New `test_8_10` asserts each LEG separately:** `if (p0 != 0)` alone was caught by exactly ONE test and `if (p1 != 0)` alone by two — a security rule whose only detector is incidental is a rule nobody has asserted, and this is the family's eighth appearance | Phase 10 | **PROVEN** — 311/0/1; mutations: demotion removed 11 RED, `p0`-only 2 RED, `p1`-only 3 RED | `QueueHook::withdraw`; `Evacuation.t.sol` `test_8_9`, `test_8_10`, `test_8_15` |
| 5.172 | 🚨 **SIX MUTATION CASES DID NOT MATCH THE SOURCE, AND THREE HAD BEEN SILENTLY DEAD SINCE PHASE 9.** `script/mutate.py` matches literal source text, so any edit to `src/` can disarm a case. **M32** (Phase 9 added `&& !promoted` to `} else if (old != 0) {`), **M38** (Phase 9's rank guards separated `_settleSeat(seatId)` from `address holder = ...`) and **M41** (Phase 9's `buySeatAndFund` inserted `fundOnTransfer0` between `paidForSeat = price;` and `_moveSeat`) have not run since Phase 9 — **two phases of a free firm-quote window, a buyout skipping the seller's rent settlement, and a firm quote not armed at what the buyer paid, all untested.** M5, M20 and M82 were disarmed by Phase 10's own edits. All six repaired and re-run: **RED, 2–58 failing each** | 🚨 **A TRUNCATED CAMPAIGN'S CLEAN SHEET IS NOT EVIDENCE ABOUT THE CASES IT NEVER REACHED, AND THE SUMMARY LINE ACTIVELY HIDES THAT.** Phase 9 reported `30 RED, 0 SURVIVED, 0 NO-COMPILE, **0 BAD-PATTERN**` — true of the 30 it ran, silent about the 55 it did not, and three of those 55 were already broken. **Fixed with a MECHANISM, not a note (§3b: prose is not an interlock): `mutate.py` now PREFLIGHTS every pattern before running a single case and exits 2 naming each one, so a disarmed case is loud at second zero instead of at a case that may never be reached.** Verified by deliberately breaking a pattern: exit 2, the case named, `src/` untouched, no marker left | Phase 10 | **PROVEN** — 6 found, 6 repaired, 6 re-run RED; preflight's own negative control executed | `script/mutate.py::preflight` |
| 5.173 | **`pgrep -f "script/mutate.py"` MATCHES THE WAITER THAT IS GREPPING FOR IT, SO `until ! pgrep -f ...; do sleep; done` NEVER TERMINATES.** 5.150 recorded that `ps aux \| grep -c "[m]utate.py"` self-matches because the bracket trick does not survive `zsh -c eval`. **The same trap has a second form and it bit in Phase 10:** a wait loop whose own `zsh -c` command line contains the pattern is found by its own `pgrep`, so it waits forever on a campaign that finished long ago. Six such waiters were left spinning, and each additionally kept the others alive | **The condition you are waiting on must not be expressible in the command line that waits for it.** Match on something the waiter cannot contain — here `pgrep -f "Python script/mutate.py"`, which matches the interpreter's argv and not a shell wrapper — or better, do not poll for a process at all: **`ls .forge-snapshots/MUTATION_IN_PROGRESS` is the marker the campaign publishes for exactly this purpose** (5.150), and it cannot self-match. Confirming a process is really gone needs `ps -o command -p <pid>` on the hits, not the hit count | Phase 10 | **PROVEN** — six self-matching waiters observed, diagnosed via `ps -o command`, killed | — |
| 5.174 | 🛑 **SETTLED: THE TRANCHE THESIS IS DEAD TOO, AND IT WAS REFUTED BY A SECTION OF OUR OWN FILE WRITTEN TO TEST IT.** The last surviving framing was that QUEUE splits one position into an EQUITY and a SENIOR tranche by fill order — that tranching never creates return, it allocates RISK, and three sessions had measured only the return axis. The supporting numbers looked overwhelming: markout per dollar rank 1 **−46.72%** against rank 32 **−0.01%**, and at φ=8500 downside deviation **12.56% vs 0.26%**, worst **−37.30% vs −1.59%**, losses on **100% vs 13%** of paths. **Every one of those compares the back to the FRONT, which proves nothing about whether anyone should buy the back.** `results-tranche.txt` §11 — "THE BENCHMARK'S OWN DISPERSION, the missing half of every comparison so far" — supplies the row that matters and states its bar BEFORE the numbers: *"if it is one to two orders of magnitude, LP-like return at a fraction of the LP's downside is the product. If it is ~1x, there is nothing being sold."* **Measured: 0.0× / 1.1× / 1.5× / 1.5× / 1.6× / 1.6×.** In BENIGN ±30% the passive LP is strictly SAFER (its downdev is 0.00%). **And the TAIL is not protected at all: TOXIC ±30% worst is −6.49% for the back against −6.43% for the LP.** A senior tranche whose tail is no better than the benchmark is not a senior tranche | **THE FRONT IS NOT A BENCHMARK, AND "48× SAFER THAN THE FRONT" IS A STATEMENT ABOUT THE FRONT.** The front is an artificially concentrated position nobody has to hold; the alternative every real buyer actually has is the passive LP, and against it the effect is ~1×. **The general lesson, and it is the same one as 5.156 and 5.160: a bar must be the alternative the buyer can freely take, never the other side of your own mechanism.** That error has now been made three times on this project in three different costumes — a nominated keeper bot (5.156), a nominated conversion venue (5.140), and here the mechanism's own opposite tranche. **Also 5.162 for the third time: the falsifying measurement, the pre-registered kill criterion and the honest verdict were ALL already on disk, in the same file as the numbers being used to argue the opposite.** Redefining "one to two orders of magnitude" as "1.6× is fine" would be exactly the over-fitting §2 forbids, and was refused | Phase 10 | **MEASURED** — `results-tranche.txt` §11, 4 independent seed ranges, zero-sum null asserted alongside | `docs/research/seat-economics/results-tranche.txt` §6, §11, §12 |
| 5.175 | ✅ **CLOSES 5.164's ASK — AND 5.164's RECORDED SAFETY ARGUMENT WAS STALE, SO THE CONCLUSION WAS RIGHT FOR THE WRONG REASON.** 5.164 asked for the invariant that makes `_syncSeat`'s cursor-blindness safe: *"every site that credits a seat must leave that seat inside the window some cursor can still reach."* Its stated reason was *"every seat below the old cursor holds zero of the outgoing token, so it is weighted zero in the accrual"* — **true only while the premium was weighted by the seat's BALANCE.** `_claims` weights by `s.liquidity` (`QueueHook.sol:1356`), and a fully drained seat still carries liquidity, so it still accrues in BOTH tokens. The old argument does not hold at all today. **The property that actually holds is `min(cursor0, cursor1) == 0` — INVARIANT W.** Induction on two lines of `_allocate`: a fill raises the OUTGOING cursor to `next` and pulls the INCOMING one back to `start`, which is the outgoing cursor's own old value; both start at 0; every other writer (`_fundSeat`, `_demoteToTail`) only LOWERS. So one cursor is always 0, every fill walks from rank 0 in one direction, and every seat below the other cursor is re-`_syncSeat`'d by the very fill that grew that token's accumulator | **A CONCLUSION THAT SURVIVES A DEAD ARGUMENT IS AN UNPROVEN CONCLUSION, and the re-weighting that killed the argument (5.168's sibling) left it standing in the ledger.** When a weight, a unit or a denominator changes, every safety argument that quoted the OLD one is stale even where the code is still correct — and nothing goes red, because the code is still correct. **Honest limit, stated rather than found later: under the shipped one-ended walk W and C are maintained by the SAME line, so W's only available control is the mutation C's control already uses, and against that mutant C fires first. W is NOT an independent detector today and is not claimed to be one.** Its value is as a tripwire for the exact change 5.164 warns about — a two-ended book or a reversed walk, where both cursors are non-zero by design and a premium credit can strand while C still holds locally | Phase 11 | **PROVEN** — asserted in `_checkInvariantC` (reached by 124 tests: inverting it turns them red) and as `invariant_I10` (256 runs x 16,384 calls). `test_N4b` asserts the NEGATION against the `NO_CURSOR_PULLBACK` mutant, with a paired positive control | `QueueFixture::_checkInvariantW`; `Controls.t.sol` `test_N4b`; `Invariant.t.sol` `invariant_I10` |
| 5.176 | 🚨 **THE INVARIANT CAMPAIGN'S OWN SOLVENCY METER OMITTED `premiums()`, SO EVERY NUMBER IT REPORTED WAS WRONG — AND ONE OF THEM WAS TRUE BY ACCIDENT.** `QueueHandler._noteSolvency` computed `owed = totals + pending` against `backing = positionValue + float`, leaving out `premiumOwed`. That is the IDENTICAL omission `invariant_I2_solvency` names in its own docblock and fixed **for itself**, left standing in the copy that produces the published measurements. Unsettled premium is in the backing and was missing from the owed, so: the position read OVER-BACKED by exactly that amount and `worstOver0/1` reported **3.57e19 / 1.03e19 wei of "surplus"** — against an `Invariant.t.sol` docblock recording *"worst SURPLUS 0 wei, both tokens, exactly"*; and, the half that matters, a too-small `owed` **understates every shortfall** and books some as surpluses. Same script, before → after the repair: surplus **3.57e19 → 0** and **1.03e19 → 0** exactly, shortfall **20 → 47 wei** and **31 → 62 wei**. `SHORTFALL_PPB` is DERIVED from these numbers, so the bound had been derived from an under-measurement | **THE ONE-RULE-TWO-PLACES FAMILY LANDING IN THE INSTRUMENT — its ninth appearance** (5.37, 5.50, 5.52 twice, 5.73, 5.125, 5.168, and `Controls.t.sol`'s own rank→id copy). The tell was available for free and nobody read it: **the docblock stated the expected value (`0 wei, exactly`) and the test PRINTED a different one on every run.** A number that is emitted but never asserted is a number nobody checks — `worstOver*` is logged, and only `_solvent` asserts, so the two could disagree indefinitely. **NO MECHANISM DEFECT IS IMPLIED AND NONE WAS FOUND:** `_solvent` requires exact equality on the over-backed branch and I2 has passed throughout, so the position was never over-backed; only the record of it was. **Rule: when an emitted diagnostic and an asserted bound are computed from two different expressions of the same quantity, they are two copies of a rule and must be built from one** | Phase 11 | **PROVEN** — repaired and re-measured on the identical scripted campaign, all four numbers moved as predicted; 314/0/1 | `test/queue/handlers/QueueHandler.sol::_noteSolvency`; `Invariant.t.sol` `SHORTFALL_PPB` docblock |
| 5.177 | 🚨 **THE EVIDENCE FOR THE PROJECT'S HEADLINE KILLED THESIS WAS THE EMPTY GIT BLOB, AND WHEN REGENERATED IT DOES NOT FULLY REPRODUCE THE ENTRY THAT CITES IT.** `docs/research/seat-economics/results-book.txt` was committed at **0 bytes** (blob `e69de29`, in 7862c1c) — the only empty tracked file in the repo — and it is the evidence 5.160, 5.161, 5.164 and 5.165 all cite. The signature is a run that opened its output with mode `w` (truncating) and then died. Regenerated: 806 lines, every integrity control PASS. **What reproduces:** the VWAPs 1906.99 / 1932.41 / 1981.82, monotonicity of execution price in priority held, and the load-bearing falsification control — TOXIC `+4.487pp` (t = +61.96) against TOXIC-DN `−1.277pp` (t = −20.10), the sign flip that killed the two-ended book. **What does NOT:** 5.160's pro-rata LP bar of *"+135.9 bps (SE 12.9)"* **appears nowhere** — the reproducible figure is **+50.2 bps (SE 5.7)** in BOTH φ blocks; 5.160's three rows **MIX φ BLOCKS** (its VWAPs are φ=0 while its `−108.2` for the shipped rank 0 is the φ=8500 column, −108.6); so the headline **`−476 bps` is not reproducible** — computed consistently it is **391.9 bps at φ=0**, 417.5 at φ=8500. And the figure that describes WHAT WE SHIP is neither: the one-ended rank 0 costs **132.7 bps** against the pro-rata LP at φ=0, 158.8 at φ=8500. **476 was the two-ended BOOK variant we do not ship** | **5.160's CONCLUSION STANDS AND ITS ARITHMETIC DOES NOT, which is the more dangerous shape** — a defensible claim carrying an indefensible number is harder to catch than a wrong claim, and it is what gets quoted. Two compounding causes: **(a)** a generator that writes its output file in mode `w` destroys the previous evidence the instant it starts, so a crash leaves nothing rather than something stale — **write to a temp path and rename on success**; **(b)** a PITFALLS row assembled by reading numbers off a screen rather than off the committed file can silently span two configurations, and once the file is empty nobody can check. **Rule: a PITFALLS row quoting a number must name the FILE, the SECTION and the PARAMETER BLOCK it came from, and any row citing a file that is missing or empty is UNVERIFIED, not PROVEN, whatever its grade says.** Found only because a routine `wc -l` of the results directory showed a zero | Phase 11 | **PROVEN** — regenerated and diffed against 5.160 line by line; the reproducing half and the non-reproducing half both executed | `docs/research/seat-economics/results-book.txt`; `book_report.py` |
| 5.178 | 🚨🚨 **EVERY PUBLISHED φ NUMBER IN THIS PROJECT MEASURES A PREMIUM WEIGHTING THE CONTRACT REPLACED IN PHASE 8 — INCLUDING THE SHIPPED CONSTANT'S OWN DERIVATION. THE REPO ALREADY KNEW, WROTE THE SCRIPT TO FIX IT, AND NEVER RAN IT.** `sim.py:66` sets `PREM_WEIGHT = 'inventory'` (the pot divided over seats' POST-FILL holdings of the outgoing token). The hook divides by **contributed liquidity with the payers excluded** — `_claims` `uint256 wl = s.liquidity` (`QueueHook.sol:1356`), denominator `standingL − excludedL` (`:1154`), payers excluded by mark advancement in `_settlePremium`. That is `sim.py`'s third mode, `'liquidity_excl'`. **`report_shipping.py`, `report_tranche.py` and `book_report.py` never override the default**, so `results-shipping.txt`, `results-tranche.txt` and `results-book.txt` — and therefore the feasible window `[7455, 8312]`, the shipped `PREMIUM_BPS = 7_900` derived as its midpoint, and every "the back beats the LP" fraction — are all on the replaced basis. `report_depth_basis.py` exists to re-measure on the contract's basis, states this in its own header (*"a premium basis the deployed contract no longer implements"*), and **has no committed output because it only prints to stdout and nobody redirected it** | **THE INSTRUMENT AND THE ARTIFACT DIVERGED AND NOTHING WENT RED, because nothing compares them.** LAW 5's second corollary warns that an instrument AGREEING with its artifact is not validation; the inverse is worse and has no detector at all — the hook changed in Phase 8, the simulator did not, and three sessions of economics ran on the stale one. **`sim.py`'s own comment on `liquidity_excl` reads "MY READING of the rule … UNCONFIRMED against the Solidity"** — a recorded, unresolved uncertainty about whether the measuring device matches the thing measured, left standing while headline claims were built on the device. **Two rules. (1) A simulator parameter that mirrors a contract rule must be pinned by a test that FAILS when the contract's rule changes — a knob defaulting to the superseded value is a trap, not an option. (2) A research script whose output is not a committed FILE has not been run, however finished it looks; "the script exists" and "the question is answered" are different states.** The one quantity immune to all of this is the closed form `back's excess = c₁(LP − r₁)/(1 − c₁)`, which follows from `Σcᵢrᵢ = LP` and holds under ANY weighting because the premium is a transfer INSIDE the book — verified against the shipped-roster sweep to four decimals (φ=8500: head −3.32pp, back +1.66pp, zero-sum 0.0000) | Phase 11 | **PROVEN** — the divergence read from both sources at named line numbers; the missing output confirmed absent; contract-basis run started | `sim.py:66`; `QueueHook.sol:1154,1356`; `report_depth_basis.py`; `results-shipping.txt` |
| 5.179 | 🚨 **THE STRATEGY DOCUMENT COMMITTED 5.163 A FIFTH TIME AND §2's FIRST SIN ONCE, IN THE SAME TABLE, TWO PAGES FROM ITS OWN WARNING NOT TO.** `NEXT_SESSION_PROMPT.md` §0b — *"THE NUMBERS WE QUOTE, AND ONLY THESE"* — is wrong on **four independent axes at once**: the ROSTER (32 equal seats of $31,250, c₁=0.031, `results-tranche.txt:4`) against the shipped 5 seats at 5:4:3:2:1, c₁=0.333 (`QueueDeployBase.sol:181`); the RANKS (28 and 32, which do not exist in a 5-seat book); the φ (8500 quoted, **7,900 shipped**, `QueueDeployBase.sol:136`); and the PREMIUM BASIS (5.178). It then claims *"The 5th-percentile excess is POSITIVE in every regime"* and 100% beat-LP in all four rows — **while silently dropping the two ±30 cells that refute it.** `results-tranche.txt:585-586`, BENIGN ±30 φ=8500: ranks 28 and 32 both read **p5 = −4.96%, beat LP = 77%**. The same file's §1 says of ranks 5–32 *"Nobody would fund these"*, so the headline numbers come from the ranks its own assessment disowns | **THE SHIPPED-ROSTER FILE MEASURING THE SAME STATISTIC WAS SITTING UNQUOTED THE WHOLE TIME**: `results-shipping.txt` reads **80.0% / 86.5% / 75.8%** of (path, seat) cells in seats 2–5 beating the LP — not 100% — plus *"ALL of seats 2-5 beat the LP: NOT AT ANY phi — s2 never does"* in TOXIC, and a **TOXIC feasible window that is EMPTY**. **The lesson is not "check the roster" — 5.163 already said that and it did not hold.** It is that **a document telling the next agent WHICH numbers to quote is the highest-leverage place in the repo for a fixture error, and it had no citation discipline at all**: not one row named its source file. **Every quoted number needs file + section + parameter block inline, or the next reader cannot check it and will not.** Prefer the config-free closed form, which cannot be quoted against the wrong fixture (5.163, 5.178) | Phase 11 | **PROVEN** — all four axes verified at named line numbers; both dropped cells read from the file; the closed form checked against the shipped sweep | `NEXT_SESSION_PROMPT.md` §0b; `results-tranche.txt:4,585-586`; `results-shipping.txt:54,66,78,86` |
| 5.180 | ✅ **THE SHIPPED φ WAS RE-DERIVED ON THE CONTRACT'S OWN BASIS AND MOVED 2,800 bps — AND THE TEST SUITE'S MIRROR OF IT HAD ALREADY DRIFTED BEHIND A COMMENT THAT NAMED ITS SOURCE.** Acting on 5.178: `report_shipping_basis.py` imports `report_shipping.py` UNCHANGED and re-runs it at `PREM_WEIGHT = 'liquidity_excl'`, the rule the hook implements. **Control passes** — at φ=0 no premium is withheld so the weight vector is never read, and the φ=0 column is identical in every row, both sides, all three regimes. Result: BENIGN window `[7455, 8312] → [4542, 5670]`, NORMAL `[4114, 8921] → [2560, 6251]`, TOXIC EMPTY either way (s2 never clears the LP). Intersection midpoint **7,900 → 5,100**; the shipped constant sat **2,230 bps above the top of the real window**, where the front seat is below the pro-rata LP, the static wing AND the managed wing at once. **Separately: `Invariant.t.sol::_premiumBps` read `return 8_500; // QueueDeployBase.PREMIUM_BPS` while the script held 7,900** — so the one campaign whose docblock claims *"It now runs at the SHIPPING φ"* was running at a φ nothing has ever shipped | **THE DIVERGENCE WAS NOT ALL BAD NEWS, WHICH IS WHY IT HAD TO BE MEASURED RATHER THAN ASSUMED:** the contract's real weighting is materially BETTER at moving value backward — all of seats 2–5 clear the LP from φ=4,542 in BENIGN (was 7,455) and 2,560 in NORMAL (was 4,114), and at φ=8500 the fraction of (path,seat) cells beating the LP is 99.6% / 94.4% (was 80.0% / 86.5%). Less premium buys more subordination than we thought. **The structural caveat SURVIVES UNCHANGED and that is the consistency check that makes the rest credible:** hold the front to the passive-LP bar instead of the managed-wing bar and it falls at 4,317, under the 4,542 the back needs, so that window is still empty and the constant still exists only because re-anchoring is worth something to the front — the same shape as the old 6,397 vs 7,455. **The mirror drift is the family's tenth appearance and the lesson is narrower than "check your constants": a comment NAMING its source is the failure mode, not the fix** — it reads as an interlock to every reviewer and enforces nothing (5.86). Replaced with `Hygiene.t.sol` `test_7_8`, which reads BOTH source files. That test went red on its first run for a real but unintended reason — it anchored on the identifier and parsed `return 8_500` out of the DOCBLOCK quoting the old line, i.e. it read a number from a comment, **the exact hazard `_assertSelector` in the same file already warns about**; anchored on the function signature instead | Phase 11 | **PROVEN** — sweep run, control passed, constant changed, 315/0/1 (was 314); `src/` untouched so the 85-case mutation campaign is unaffected | `report_shipping_basis.py`; `results-shipping-basis.txt`; `QueueDeployBase.sol:169`; `Hygiene.t.sol` `test_7_8` |
| 5.181 | 🚨 **THE FLAGSHIP APPLICATION IS TAKEABLE BY ANY STRANGER FOR ZERO WEI PLUS GAS, AND THE CONSEQUENCE IS NOT "THE DAO LOSES A SEAT" — IT IS THAT THE SUBSIDY REVERSES ONTO THE PEOPLE IT WAS PAYING.** `buyPrice` quotes zero for a never-priced seat and `buySeat(id, 0, 0)` funds nothing, so `buySeat(frontSeat, 0, 0)` succeeds from any address. Executed end to end: the take costs **0 of both currencies**, `_onSeatTransfer` **evacuates the subsidiser's capital out of the POSITION** (sent directly via `_send(from, p0 + esc, p1)` — NOT credited to `pending`), `positionLiquidity` falls in the same transaction, `_settleRankOnTransfer` demotes the emptied seat to the tail because the buyer replaced no depth — **and the seat that was SECOND is now FIRST.** By `Σ cᵢrᵢ = LP` somebody must sit below the line, so an external LP who bought a *subordinated back seat* is moved into the first-loss position by a stranger, for gas | **THIS IS NOT A CONTRACT DEFECT AND MUST NOT BE PATCHED AS ONE.** `buyPrice`'s docblock states the rule deliberately — *"A seat that has never been priced quotes zero and is free to take. That is not a hole, it is the bootstrap… the deployer's endowment is worth a head start of one transaction and nothing else."* That is defensible for a roster meant to change hands. **What is new is that the emissions-free-incentives pitch asks a protocol to hold the FRONT for months, and nothing anywhere asserted what happens when it does not price the seat.** `test_4_13b` already USES the free take incidentally to empty a seat and asserts nothing about who ends up in front. **The remedy is OPERATIONAL: the deploy script must `setSelfPrice` the subsidiser's seat in the same transaction that funds it, and the DAO then pays rent on that price forever — a real, quantifiable running cost of the application that belongs in the pitch rather than in whoever deploys it.** General lesson: a rule that is correct AND documented for the mechanism can still be fatal for one application built on it, and the docblock defending the rule is exactly where nobody looks for an application-level hazard | Phase 11 | **PROVEN** — `test_4_44`, every link asserted with a non-vacuity guard so a fix turns it red rather than vacuous; found by the panel as REASONED, executed here (their claim that the capital lands in `pending` is wrong — it is sent directly, and the first draft of the test went red on exactly that) | `QueueHook::buyPrice:2318`, `::buySeat:2460`, `::_onSeatTransfer:2163`; `test/queue/Harberger.t.sol` `test_4_44` |
| 5.182 | 🚨 **THERE WAS A THIRD MIRROR OF THE SHIPPED φ, AND FIXING THE SECOND ONE IS WHAT EXPOSED IT — SO THE QUOTED GAS FIGURE WAS MEASURED AT A φ WE DO NOT SHIP.** 5.180 found `Invariant.t.sol::_premiumBps` hardcoded at 8,500 behind a comment naming the deploy script, and pinned it with `Hygiene.t.sol` `test_7_8`. **`Gas.t.sol::_premiumBps` hardcoded 8,500 too** — and `Gas.t.sol` is, by `AGENTS.md` §3b, *"EVERY gas measurement in the project, and the only place they belong"*. So the **"+119% gas for every trader"** caveat, which is quoted in `BUSINESS.md`, `README.md` and the pitch, was measured against a premium the contract no longer ships. Re-pointed and re-measured at φ = 5,100: head-only swap **153,177** against a plain pool's **69,970**, overhead **83,185 = +119%**; the φ=0 control is **141,036 (+102%)** | **THE HEADLINE SURVIVED AND THE REASON IS THE USEFUL PART: the premium's gas cost is the MACHINERY, not the RATE.** Writing the accumulator costs the same whether φ is 5,100 or 8,500 — 153,177 against the old 152,947, a 230-gas difference — so the number did not move even though the constant moved 2,800 bps. **A quantity that is insensitive to the parameter you were worried about is worth proving rather than assuming**, and here proving it cost one run and converted a stale number into a measured one. **The generalisable rule, and it is stronger than "pin your constants": one interlock per known mirror is not a fix, it is a whack-a-mole.** `test_7_8` was written for the mirror that had been FOUND; the third one was found by grepping for the literal afterwards, not by the interlock. Any future `_premiumBps` override is still unpinned — the durable fix is a single shared source the overrides read, which was NOT done here because it touches five suites' fixtures on the eve of a deadline, and that decision is recorded rather than left implicit. Also cleared in the same pass: **17 stale `+133% / 162,766` instances** across `BUSINESS.md` and `README.md`, `199 tests / 74 mutations` (understating the project by two phases), the `recycle 122–543×` sentence 5.162 named as printing our own refutation as a feature, a retracted `~1%/yr` still sitting in the **SAY** box, and a `Ranks 2–5: LP + ~0.25 pp` row wrong by ~3.5× against the contract-basis measurement | Phase 11 | **PROVEN** — re-measured at the shipped φ, both arms; `test_7_8` extended to pin the third mirror and verified capable of firing | `test/queue/Gas.t.sol:58`; `Hygiene.t.sol` `test_7_8`; `BUSINESS.md`, `README.md` |
| 5.184 | **THE FRONTEND CARRIES THE SAME FIVE STALE CLAIMS THE OLD README DID, AND ONE OF THEM IS A GAS FIGURE THAT IS NOW 2.5x WRONG.** `frontend/index.html` still says **"+48%"** gas (measured on this tree: **+119%**), **"183 passed"** (now 319), and **"32 seats / 32 desks"** in four places as the product's shape, when `QueueDeployBase` ships FIVE and `results-depth-basis.txt` shows 16 and 32 have **no feasible premium rate in any regime**. Line 549 reads *"MAX_SEATS = 32 is room for participants, not 32 things to sell"* — which reads as an economic endorsement of a number that is a STORAGE fact (one byte per rank in one word) | **What makes this a hazard rather than a chore: the page is the only artefact a judge or an LP touches without reading a single `.md` file.** A stale number in `README.md` is embarrassing; the same number on the page somebody makes a decision from is the product lying. The viewer's SELECTORS are fine and interlocked (`Hygiene.t.sol::test_7_6` passes, and `leaseOf`'s signature was deliberately left alone in Phase 12 — `unlockAt` was added as a SEPARATE view precisely so the tuple would not move under it). It is only the PROSE that is stale. Deliberately NOT fixed yet: the head-share sweep may move `c₁` and `φ`, and rewriting the page twice is how the three copies of the shipped premium drifted apart in the first place (5.180) | OPEN — scheduled after the economics settle | **PROVEN** — grepped against the live file and the freshly measured gas table | `frontend/index.html` lines 333, 335, 549, 591, 720, 721, 726, 749 |

---

## 6. Forbidden — the hard rules, restated

| # | Rule | Note |
|---|---|---|
| 6.1 | **No off-chain components.** No server, relay, watch-tower, keeper, scanner, sequencer or hosted anything. Corollary: no VRF, no oracle, no external data feed | Owner constraint, and the "unforgeable because it costs real money" story depends on not having one |
| 6.2 | **No mainnet deployment.** Any mainnet. Unichain Sepolia only, and not before Phase 3's gate is green | PLAN §H |
| 6.3 | **Do not attempt to detect toxic flow** | §4.1 |
| 6.4 | **Do not attempt a curve that reduces LVR** | §4.2 |
| 6.5 | **Do not add an admin function, upgrade path or privileged role without asking** | A hook with an owner who can move funds is a hook nobody can trust |
| 6.6 | **Never let untrusted code choose how much memory you allocate** | §1.20 — introduced three times by people who knew about it |
| 6.7 | **Stop and ask the owner** before any change to who receives fees, seat allocation rules, an admin function, or an external dependency | AGENTS.md §4 |
| 6.8 | **Never describe a plan as a result.** Tag every claim PROVEN / MEASURED / REASONED / UNPROVEN, and never silently overwrite a measurement — record what it was and when it changed | AGENTS.md §2; PLAN §F.1 |
| 6.9 | **Do not build a frontend "just in case"; do not let deployment eat the video** | The video is a binary gate; deployment is not |
| 6.10 | Do not build: margin engine / collateral / liquidation crank · compliance or KYC gate · variance-or-LVR-index product · permissioned pool | PLAN §E.19 — each investigated and closed here |

---

## 7. Where the sources disagree — noted, not silently resolved

| # | The conflict | Reading |
|---|---|---|
| 7.1 | **P1 vs P2.** `docs/research/protocol-fee/VERDICT.md` §5 says *"P2 is worse than it looks… Do not do this"* and §6 recommends shipping P1. `experiment.md`, `PROGRESS.md` and the owner decision of 2026-08-26 say **ship P2** | VERDICT was written before the experiment ran. `test_M3` measured the netted allocator closing the gap to 3 wei. **VERDICT §5–§7 is superseded**, but it is not marked as such in the file |
| 7.2 | **Does P2/P3 need a `beforeSwap` permission?** VERDICT §5 says yes (+ a re-mined address and ~15% on the headline gas number). `experiment.md` / PROGRESS say **no** — diff `protocolFeesAccrued` inside `_afterSwap`, legal because accrual happens at `PoolManager.sol:238` before `afterSwap` at `:221` | The later claim is source-traced and measured. VERDICT's cost estimate is stale |
| 7.3 | **VERDICT §7** states *"no test has yet been run with a nonzero protocol fee"* | True when written; false since — 7 tests including 3 mutations were executed the same day |
| 7.4 | **LAW 3.** ~~`AGENTS.md` §3.3 still states the raw-balance form.~~ **RESOLVED 2026-08-26:** `AGENTS.md` §3.3 now carries the amendment inline, and `PLAN.md` §D.1 LAW 3 was amended the same day. `PLAN.md` §E.5, `experiment.md` and `PROGRESS.md` already stated it | The amendment wins (it is executed evidence). All four live statements of LAW 3 now agree: measure on `PoolManager balance − protocolFeesAccrued(currency)`, or against `redeemAll()`. Conservation and solvency remain two separate assertions (2.8) |
| 7.5 | **PLAN §E.5's P1 recommendation.** `queue-exposure.md` §A4 quotes `PLAN.md:1708-1709` as *"Simple, honest, and losing nothing since we control the pool we deploy. **Recommended.**"* That text no longer exists at those lines — PLAN §E.5 has since retracted the justification | The quote is stale, the retraction is current. Line-number citations into PLAN.md are not stable; prefer section references |
| 7.6 | ~~**PLAN's own cross-references are wrong in two places:** `PLAN.md:104` and `:1640` point at **§E.9**.~~ **FIXED 2026-08-26** — both now cite **§E.19**; §E.9 is the NoOp/router section | Closed. Two other sites (`:2079`, `:2154`) always cited §E.19 correctly. Keep the row: it is the record of the error and of why line-number citations into PLAN.md rot (see 7.5) |
| 7.7 | **Criterion 1.10 vs §E.5.** `PLAN.md:991` says *"the hook refuses to **operate**"*; the research says it must be *"refuses to **allocate** — swaps revert, deposits may revert, withdrawals MUST still succeed"* | The literal criterion steered an implementer to `unlockCallback` and permanent loss of funds (3.3). The research wording wins. **PLAN CORRECTED 2026-08-26** — criterion 1.10 now carries both corrections inline and names P2 as the owner decision. `PLAN.md:991` as a *line* reference is superseded; cite §C.1 criterion 1.10 |
| 7.9 | **Three different read orders are documented.** `AGENTS.md` §1's table says AGENTS → PLAN → **BUSINESS** → PROGRESS → PITFALLS → docs/research → archive. `AGENTS.md` §7 "Handoff" and `PLAN.md` §F.4 say AGENTS → PLAN → PROGRESS (→ PITFALLS), with BUSINESS not in the chain at all. `PLAN.md`'s header says AGENTS → PLAN → PROGRESS → PITFALLS → archive | **Unresolved — flagged, not silently picked.** The handoff chain (AGENTS → PLAN → PROGRESS → PITFALLS) is the one two documents agree on and the one the gates depend on; `BUSINESS.md`'s own stated read-time is "before writing the README or video" (`PLAN.md` §I.1), which argues for it being 5th, not 3rd. **The owner should settle the position of `BUSINESS.md`.** Every order does now include `PITFALLS.md` |
| 7.10 | **`BUSINESS.md` limitations used to live at §9 and were not the complete set.** `PITFALLS.md` §5.5 still records that §B.7's withdraw spec is **MEASURED impossible as specified** (face value is an upper bound) | **PARTLY CLOSED 2026-08-31.** Limitations now live at `BUSINESS.md` §14 and the README "does not do" table. Written into the pitch, not merely cross-referenced: 50× leverage both ways, the Ratchet, Harberger floor at 0, no seat market, thin full-range, last-out residual, theme miss, founding endowment, native-ETH hole. Pointer to `PITFALLS.md` §5 remains load-bearing — §5 is still the complete ledger. Face-value-is-redeemable remains barred (5.7). |
| 7.11 | **`PLAN.md` §A.3 says say the technical sentence first in the README.** The 2026-08-31 rewrite leads with a 90-second business version ("people putting up the money buy and sell their place in line") and puts the technical sentence later | **Owner-directed.** Judges and decision-makers were bouncing off engineer-speak. The technical sentence is still in `PLAN.md` §A.3 and in `BUSINESS.md` §16. Do not "fix" the README back to the old lead without asking. |
| 7.8 | **The archive vs the live docs.** `archive/2026-08-26/CLAUDE.md` holds ~25 hard-won facts, several of which concern the retired Assay/predicate design rather than QUEUE | `AGENTS.md` §4 is explicit: **this file and PLAN win; the archive is history.** Facts mined from it above are the platform-level ones that survive the pivot |
